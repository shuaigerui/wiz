
import Foundation

import UIKit

final class EPConversation: UITabBar {

    private enum Layout {
        static let contentHeight: CGFloat = 49
        static let topCornerRadius: CGFloat = 24
    }

@discardableResult
 func captionClearViewFitStoreButton(topForm: Bool) -> UIButton! {
    var replyu: [Any]! = [18, 53]
   withUnsafeMutablePointer(to: &replyu) { pointer in
    
   }
    var mediab: String! = String(cString: [108,108,118,105,100,101,110,99,100,115,112,0], encoding: .utf8)!
       var cameraT: [String: Any]! = [String(cString: [108,111,116,116,105,101,118,105,101,119,0], encoding: .utf8)!:87, String(cString: [116,98,109,108,0], encoding: .utf8)!:42]
       var devicex: Double = 1.0
          var buildU: String! = String(cString: [115,97,116,100,120,0], encoding: .utf8)!
          var callp: Bool = false
          var previousi: Float = 0.0
         withUnsafeMutablePointer(to: &previousi) { pointer in
                _ = pointer.pointee
         }
         cameraT = ["\(cameraT.keys.count)": (3 | Int(previousi > 260992117.0 || previousi < -260992117.0 ? 99.0 : previousi))]
         buildU.append("\(3)")
         callp = buildU.hasSuffix("\(previousi)")
      for _ in 0 ..< 3 {
         devicex -= (Double(cameraT.values.count | Int(devicex > 177950594.0 || devicex < -177950594.0 ? 45.0 : devicex)))
      }
          var template_cS: Double = 4.0
          _ = template_cS
          var rangex: Float = 2.0
         devicex /= Swift.max(2, Double(cameraT.keys.count))
         template_cS -= Double(1)
         rangex += (Float(cameraT.values.count & Int(devicex > 342718744.0 || devicex < -342718744.0 ? 23.0 : devicex)))
      for _ in 0 ..< 1 {
         cameraT["\(devicex)"] = (Int(devicex > 53360406.0 || devicex < -53360406.0 ? 49.0 : devicex) << (Swift.min(labs(1), 3)))
      }
      if !cameraT.keys.contains("\(devicex)") {
         cameraT = ["\(cameraT.count)": (Int(devicex > 209034526.0 || devicex < -209034526.0 ? 48.0 : devicex) % 1)]
      }
      for _ in 0 ..< 2 {
          var bottom6: Int = 4
          var redE: String! = String(cString: [100,105,115,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &redE) { pointer in
                _ = pointer.pointee
         }
          var inputT: Float = 5.0
         withUnsafeMutablePointer(to: &inputT) { pointer in
    
         }
         devicex /= Swift.max(Double(2), 4)
         bottom6 += 1 ^ cameraT.values.count
         redE = "\((Int(inputT > 385669628.0 || inputT < -385669628.0 ? 30.0 : inputT) - cameraT.keys.count))"
         inputT -= Float(cameraT.count)
      }
      mediab.append("\(3 & cameraT.keys.count)")
   if 3 >= replyu.count {
      replyu = [1]
   }
     var update_sLocal_5: Int = 79
     let viewCenter: UIImageView! = UIImageView()
     let matchedRequest: UIImageView! = UIImageView(frame:CGRect(x: 305, y: 15, width: 0, height: 0))
    var processorsTsxSavestate = UIButton(frame:CGRect(x: 132, y: 18, width: 0, height: 0))
    viewCenter.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    viewCenter.alpha = 0.6
    viewCenter.frame = CGRect(x: 262, y: 52, width: 0, height: 0)
    viewCenter.image = UIImage(named:String(cString: [116,104,114,101,101,0], encoding: .utf8)!)
    viewCenter.contentMode = .scaleAspectFit
    viewCenter.animationRepeatCount = 7
    
    matchedRequest.frame = CGRect(x: 148, y: 275, width: 0, height: 0)
    matchedRequest.alpha = 0.1;
    matchedRequest.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    matchedRequest.animationRepeatCount = 5
    matchedRequest.image = UIImage(named:String(cString: [115,97,118,101,100,0], encoding: .utf8)!)
    matchedRequest.contentMode = .scaleAspectFit
    
    processorsTsxSavestate.frame = CGRect(x: 309, y: 11, width: 0, height: 0)
    processorsTsxSavestate.alpha = 0.5;
    processorsTsxSavestate.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    processorsTsxSavestate.titleLabel?.font = UIFont.systemFont(ofSize:13)
    processorsTsxSavestate.setImage(UIImage(named:String(cString: [109,97,112,112,101,100,0], encoding: .utf8)!), for: .normal)
    processorsTsxSavestate.setTitle("", for: .normal)
    processorsTsxSavestate.setBackgroundImage(UIImage(named:String(cString: [103,97,105,110,0], encoding: .utf8)!), for: .normal)

    
    return processorsTsxSavestate

}





    override func sizeThatFits(_ size: CGSize) -> CGSize {
       var otherB: String! = String(cString: [115,107,105,112,105,110,116,114,97,0], encoding: .utf8)!
       var bubbleX: Int = 4
       var optionw: Double = 1.0
      withUnsafeMutablePointer(to: &optionw) { pointer in
    
      }
       var passwordi: Int = 0
      withUnsafeMutablePointer(to: &passwordi) { pointer in
    
      }
         optionw += (Double(Int(optionw > 14042565.0 || optionw < -14042565.0 ? 74.0 : optionw)))
      repeat {
         optionw += Double(1)
         if 4655180.0 == optionw {
            break
         }
      } while (1 == (4 - bubbleX) || 5 == (bubbleX << (Swift.min(labs(4), 3)))) && (4655180.0 == optionw)
      repeat {
         optionw *= (Double(Int(optionw > 1666923.0 || optionw < -1666923.0 ? 7.0 : optionw) + bubbleX))
         if 4414254.0 == optionw {
            break
         }
      } while (passwordi < Int(optionw)) && (4414254.0 == optionw)
      while ((passwordi / (Swift.max(Int(optionw > 71347929.0 || optionw < -71347929.0 ? 83.0 : optionw), 9))) > 4 && (passwordi & 4) > 2) {
          var titles6: Double = 1.0
         optionw += (Double(Int(optionw > 167606169.0 || optionw < -167606169.0 ? 94.0 : optionw)))
         titles6 += Double(2 ^ bubbleX)
         break
      }
      repeat {
         optionw /= Swift.max(5, Double(bubbleX >> (Swift.min(4, labs(2)))))
         if optionw == 4584158.0 {
            break
         }
      } while (5.69 > (Double(optionw * Double(bubbleX)))) && (optionw == 4584158.0)
         bubbleX &= (1 / (Swift.max(3, Int(optionw > 186437793.0 || optionw < -186437793.0 ? 50.0 : optionw))))
         bubbleX -= 3
      if (Double(bubbleX - Int(optionw))) < 4.3 {
          var unixW: String! = String(cString: [112,97,114,115,101,0], encoding: .utf8)!
          var delete_ap: [String: Any]! = [String(cString: [105,112,109,111,118,105,101,0], encoding: .utf8)!:String(cString: [116,114,117,110,0], encoding: .utf8)!, String(cString: [107,105,110,100,115,0], encoding: .utf8)!:String(cString: [110,114,101,102,0], encoding: .utf8)!]
          _ = delete_ap
         optionw += Double(delete_ap.count)
         unixW.append("\(3 & unixW.count)")
      }
      for _ in 0 ..< 3 {
         passwordi -= (2 | Int(optionw > 129191920.0 || optionw < -129191920.0 ? 58.0 : optionw))
      }
      otherB.append("\((bubbleX % (Swift.max(Int(optionw > 258452366.0 || optionw < -258452366.0 ? 44.0 : optionw), 7))))")

        var media = super.sizeThatFits(size)
        media.height = Layout.contentHeight + safeAreaInsets.bottom
        return media
    }

    override func layoutSubviews() {
       var placeholderU: [Any]! = [53, 71, 79]
      placeholderU.append(placeholderU.count)

        super.layoutSubviews()

        layer.cornerRadius = Layout.topCornerRadius
        layer.maskedCorners = [.layerMinXMinYCorner, .layerMaxXMinYCorner]
        layer.masksToBounds = true

        let dim = safeAreaInsets.bottom
        guard dim > 0 else { return }

        let nano = bounds.height - dim - Layout.contentHeight
        for subview in subviews {
            guard NSStringFromClass(type(of: subview)).contains(cornerMapMail([-101,-121,-102,-81,-84,-116,-81,-68,-116,-69,-70,-70,-95,-96,-50],0xCE,false)) else { continue }
            var frame = subview.frame
            frame.origin.y = nano + (Layout.contentHeight - frame.height) / 2
            subview.frame = frame
        }
    }
}
