
import Foundation

import AVFoundation
import UIKit

class EPBaseController: EPFriendChatController {

    private let postItem: EPShopItem
    private var player: AVPlayer?
    private var playerLayer: AVPlayerLayer?
    private var isLiked: Bool

    init(item: EPShopItem) {
        self.postItem = item
        self.isLiked = item.isLiked
        super.init(nibName: nil, bundle: nil)
        hidesBottomBarWhenPushed = true
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError(cornerMapMail([-113,-120,-113,-110,-50,-123,-119,-126,-125,-108,-36,-49,-58,-114,-121,-107,-58,-120,-119,-110,-58,-124,-125,-125,-120,-58,-113,-117,-106,-118,-125,-117,-125,-120,-110,-125,-126,-26],0xE6,false))
    }

@discardableResult
 func fanTitleComponentTableView(lockedLeading: Bool, storeButtons: [String: Any]!) -> UITableView! {
    var sideX: String! = String(cString: [104,109,104,100,0], encoding: .utf8)!
    var clockwise4: Double = 5.0
   withUnsafeMutablePointer(to: &clockwise4) { pointer in
    
   }
      clockwise4 -= (Double(sideX.count << (Swift.min(1, labs(Int(clockwise4 > 151025188.0 || clockwise4 < -151025188.0 ? 93.0 : clockwise4))))))
     let interval_yInt_3: UILabel! = UILabel()
     var sharedHas: UIView! = UIView(frame:CGRect(x: 141, y: 423, width: 0, height: 0))
     var destWork: [Any]! = [81, 84]
     var contentDisplay: UIView! = UIView()
    var eacencVaapi = UITableView()
    eacencVaapi.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    eacencVaapi.alpha = 0.6
    eacencVaapi.frame = CGRect(x: 251, y: 64, width: 0, height: 0)
    eacencVaapi.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    eacencVaapi.delegate = nil
    eacencVaapi.dataSource = nil
    interval_yInt_3.frame = CGRect(x: 227, y: 293, width: 0, height: 0)
    interval_yInt_3.alpha = 0.0;
    interval_yInt_3.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    interval_yInt_3.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    interval_yInt_3.textAlignment = .center
    interval_yInt_3.font = UIFont.systemFont(ofSize:16)
    interval_yInt_3.text = ""
    
    sharedHas.alpha = 0.3;
    sharedHas.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sharedHas.frame = CGRect(x: 23, y: 290, width: 0, height: 0)
    
    contentDisplay.frame = CGRect(x: 260, y: 235, width: 0, height: 0)
    contentDisplay.alpha = 0.3;
    contentDisplay.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    

    
    return eacencVaapi

}






    

    private func setupPlayer() {
       var fnew_r4i: String! = String(cString: [112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
       var mapa: Int = 0
       _ = mapa
       var release_tL: [String: Any]! = [String(cString: [115,112,108,105,116,115,0], encoding: .utf8)!:49]
       var rebuildi: Bool = true
       var toastf: [Any]! = [String(cString: [116,104,101,114,101,117,109,0], encoding: .utf8)!, String(cString: [104,111,108,108,111,119,0], encoding: .utf8)!, String(cString: [111,112,115,0], encoding: .utf8)!]
       var reversez: Double = 5.0
      repeat {
         mapa /= Swift.max(3, mapa ^ release_tL.values.count)
         if mapa == 2931904 {
            break
         }
      } while ((release_tL.count ^ mapa) >= 3 || 5 >= (3 ^ release_tL.count)) && (mapa == 2931904)
         rebuildi = (92 == ((!rebuildi ? release_tL.count : 77) | release_tL.count))
         reversez += Double(mapa)
         toastf = [release_tL.keys.count / (Swift.max(2, 10))]
      while ((toastf.count % 4) < 5) {
         mapa /= Swift.max(4, 2 ^ release_tL.values.count)
         break
      }
      for _ in 0 ..< 2 {
          var bubbled: Double = 3.0
         withUnsafeMutablePointer(to: &bubbled) { pointer in
                _ = pointer.pointee
         }
          var minuteI: [String: Any]! = [String(cString: [108,111,116,116,105,101,105,116,101,109,0], encoding: .utf8)!:String(cString: [98,97,99,107,100,114,111,112,0], encoding: .utf8)!, String(cString: [98,105,116,0], encoding: .utf8)!:String(cString: [98,101,97,114,105,110,103,0], encoding: .utf8)!, String(cString: [97,110,110,111,116,97,116,101,0], encoding: .utf8)!:String(cString: [109,118,115,97,100,0], encoding: .utf8)!]
          var roomsp: String! = String(cString: [114,97,115,116,101,114,105,122,101,0], encoding: .utf8)!
         mapa |= (Int(bubbled > 320343956.0 || bubbled < -320343956.0 ? 24.0 : bubbled) % 3)
         minuteI["\(mapa)"] = minuteI.count
         roomsp = "\(3 | minuteI.values.count)"
      }
          var rowsR: Double = 0.0
          _ = rowsR
         toastf.append((Int(reversez > 159381503.0 || reversez < -159381503.0 ? 41.0 : reversez) * (rebuildi ? 5 : 4)))
         rowsR -= (Double(Int(rowsR > 373038721.0 || rowsR < -373038721.0 ? 27.0 : rowsR) % (Swift.max(7, Int(reversez > 356396115.0 || reversez < -356396115.0 ? 59.0 : reversez)))))
         toastf = [(Int(reversez > 245753666.0 || reversez < -245753666.0 ? 2.0 : reversez) / 1)]
         rebuildi = (reversez + Double(release_tL.keys.count)) <= 9.21
         mapa -= release_tL.count * 2
       var j_animationO: String! = String(cString: [99,104,101,99,107,0], encoding: .utf8)!
         j_animationO.append("\((Int(reversez > 259388337.0 || reversez < -259388337.0 ? 56.0 : reversez) / (Swift.max(3, (rebuildi ? 3 : 1)))))")
      fnew_r4i.append("\(1 | toastf.count)")
   }

        guard let url = EP_PostMedia.resolveVideoURL(postItem.video) else { return }

        let off = AVPlayer(url: url)
        let configuration = AVPlayerLayer(player: off)
        configuration.videoGravity = .resizeAspectFill
        configuration.frame = view.bounds
        view.layer.insertSublayer(configuration, at: 0)

        self.player = off
        self.playerLayer = configuration

        NotificationCenter.default.addObserver(
            self,
            selector: #selector(playerDidFinish),
            name: .AVPlayerItemDidPlayToEndTime,
            object: off.currentItem
        )
    }

@discardableResult
 func deliverPostManager(overlapReverse: Bool) -> Float {
    var homeO: Double = 2.0
    var stringH: String! = String(cString: [116,101,115,116,99,108,101,97,110,0], encoding: .utf8)!
    var y_managerM: Float = 0.0
    _ = y_managerM
       var controllerC: Bool = false
       var houro: String! = String(cString: [111,102,102,115,101,116,0], encoding: .utf8)!
       var fieldZ: Int = 1
      if houro.hasSuffix("\(controllerC)") {
         houro.append("\(3 * houro.count)")
      }
      if controllerC {
          var errorG: String! = String(cString: [105,112,118,0], encoding: .utf8)!
          var hourB: String! = String(cString: [116,111,107,101,110,116,120,0], encoding: .utf8)!
          var appearanceM: Float = 2.0
          var placeholderH: Double = 4.0
          var open6: Int = 2
         controllerC = ((Int(appearanceM > 273926977.0 || appearanceM < -273926977.0 ? 88.0 : appearanceM) / (Swift.max(hourB.count, 8))) > 32)
         errorG.append("\(1)")
         placeholderH += Double(open6)
         open6 |= houro.count
      }
      repeat {
          var achieI: Double = 1.0
          var processedd: Bool = false
          _ = processedd
          var displayw: [Any]! = [String(cString: [105,110,99,114,101,97,115,101,0], encoding: .utf8)!, String(cString: [97,99,99,117,109,117,108,97,116,101,100,95,113,95,53,56,0], encoding: .utf8)!]
         controllerC = (displayw.contains { $0 as? Double == achieI })
         processedd = displayw.count == 67
         if controllerC ? !controllerC : controllerC {
            break
         }
      } while (houro.count == 2) && (controllerC ? !controllerC : controllerC)
      for _ in 0 ..< 2 {
         controllerC = fieldZ > 71
      }
          var sharedN: String! = String(cString: [112,114,105,110,99,105,112,97,108,0], encoding: .utf8)!
          var buildE: String! = String(cString: [97,117,116,111,114,101,118,101,114,115,101,100,0], encoding: .utf8)!
          var revise5: String! = String(cString: [115,112,97,99,101,115,0], encoding: .utf8)!
         houro.append("\(fieldZ >> (Swift.min(labs(2), 2)))")
         sharedN = "\(fieldZ & houro.count)"
         buildE = "\(2 ^ houro.count)"
         revise5.append("\(2 + buildE.count)")
      while (!houro.hasPrefix("\(fieldZ)")) {
          var formattedD: [Any]! = [100, 32]
          _ = formattedD
          var allowedE: [String: Any]! = [String(cString: [100,114,105,118,101,114,115,0], encoding: .utf8)!:String(cString: [115,101,110,100,101,114,0], encoding: .utf8)!, String(cString: [109,111,118,101,110,99,99,101,110,99,0], encoding: .utf8)!:String(cString: [109,112,101,103,112,115,0], encoding: .utf8)!]
          var workj: String! = String(cString: [111,102,102,115,99,114,101,101,110,0], encoding: .utf8)!
          var mappedd: String! = String(cString: [112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,0], encoding: .utf8)!
          var scroll1: [Any]! = [19, 98]
         houro = "\(((String(cString:[80,0], encoding: .utf8)!) == mappedd ? scroll1.count : mappedd.count))"
         formattedD = [1 % (Swift.max(5, formattedD.count))]
         allowedE = ["\(allowedE.keys.count)": 1]
         workj.append("\(houro.count % 1)")
         break
      }
         houro.append("\(fieldZ)")
      while (1 < (5 | fieldZ)) {
         fieldZ |= fieldZ << (Swift.min(4, labs(1)))
         break
      }
      repeat {
         houro = "\(1)"
         if houro == (String(cString:[101,119,107,115,118,107,53,110,0], encoding: .utf8)!) {
            break
         }
      } while (houro == (String(cString:[101,119,107,115,118,107,53,110,0], encoding: .utf8)!)) && ((fieldZ * 4) >= 5 || 4 >= (houro.count * fieldZ))
      y_managerM /= Swift.max((Float((String(cString:[112,0], encoding: .utf8)!) == houro ? (controllerC ? 2 : 1) : houro.count)), 1)
   for _ in 0 ..< 3 {
      homeO += (Double(Int(y_managerM > 233522018.0 || y_managerM < -233522018.0 ? 88.0 : y_managerM)))
   }
   if stringH.contains(stringH) {
       var reuse1: String! = String(cString: [116,111,107,101,110,110,102,116,116,120,0], encoding: .utf8)!
       var b_imageq: Float = 2.0
      withUnsafeMutablePointer(to: &b_imageq) { pointer in
    
      }
       var insufficient7: Int = 5
      if b_imageq == 1.45 {
          var conversations8: [String: Any]! = [String(cString: [97,99,111,110,102,105,103,0], encoding: .utf8)!:32, String(cString: [100,101,112,101,110,100,115,0], encoding: .utf8)!:67, String(cString: [119,114,97,112,112,101,100,0], encoding: .utf8)!:67]
          _ = conversations8
          var stringsq: String! = String(cString: [105,109,97,103,101,115,0], encoding: .utf8)!
         insufficient7 |= reuse1.count
         conversations8["\(b_imageq)"] = conversations8.keys.count
         stringsq = "\((insufficient7 << (Swift.min(labs(Int(b_imageq > 207451513.0 || b_imageq < -207451513.0 ? 48.0 : b_imageq)), 1))))"
      }
         b_imageq /= Swift.max(1, Float(2 ^ insufficient7))
       var sele: String! = String(cString: [108,105,115,116,101,110,101,114,0], encoding: .utf8)!
       var allA: String! = String(cString: [114,117,110,110,105,110,103,0], encoding: .utf8)!
       var namev: Int = 3
          var winitialsN: Int = 5
          _ = winitialsN
          var beforen: [Any]! = [String(cString: [114,101,99,111,110,0], encoding: .utf8)!]
         sele.append("\(namev)")
         winitialsN >>= Swift.min(labs((allA.count + Int(b_imageq > 152364260.0 || b_imageq < -152364260.0 ? 31.0 : b_imageq))), 4)
         beforen.append(insufficient7 / (Swift.max(1, 2)))
      repeat {
          var updatedX: String! = String(cString: [106,99,115,97,109,112,108,101,0], encoding: .utf8)!
         sele = "\(1 << (Swift.min(labs(namev), 1)))"
         updatedX.append("\(reuse1.count << (Swift.min(labs(3), 4)))")
         if sele.count == 4883111 {
            break
         }
      } while (sele.count == 4883111) && (reuse1 == sele)
      while (reuse1.hasPrefix("\(b_imageq)")) {
         reuse1.append("\(allA.count | sele.count)")
         break
      }
         insufficient7 %= Swift.max(3, 5)
      if 4 == (5 - reuse1.count) || (5 % (Swift.max(5, reuse1.count))) == 4 {
          var monthsZ: String! = String(cString: [98,95,54,52,95,114,101,109,111,118,101,0], encoding: .utf8)!
          var backl: Double = 5.0
          var appleQ: String! = String(cString: [102,114,101,101,108,105,115,116,0], encoding: .utf8)!
         b_imageq /= Swift.max(Float(insufficient7 * 2), 5)
         monthsZ.append("\(namev)")
         backl /= Swift.max(Double(monthsZ.count), 1)
         appleQ.append("\(monthsZ.count * reuse1.count)")
      }
      stringH.append("\((stringH == (String(cString:[84,0], encoding: .utf8)!) ? insufficient7 : stringH.count))")
   }
   return y_managerM

}






    private func setupEvents() {
       var launchO: String! = String(cString: [111,112,116,105,111,110,115,0], encoding: .utf8)!
    var pasty: String! = String(cString: [109,120,112,101,103,0], encoding: .utf8)!
    _ = pasty
       var outgoingQ: String! = String(cString: [109,111,100,101,109,118,99,111,115,116,0], encoding: .utf8)!
       _ = outgoingQ
       var incrementt: String! = String(cString: [103,117,105,100,0], encoding: .utf8)!
      while (incrementt.count < outgoingQ.count) {
         incrementt.append("\(incrementt.count & 2)")
         break
      }
          var numberb: Double = 3.0
          var destg: String! = String(cString: [114,101,116,114,121,97,98,108,101,0], encoding: .utf8)!
         incrementt.append("\(2)")
         numberb /= Swift.max(3, (Double(destg == (String(cString:[101,0], encoding: .utf8)!) ? destg.count : incrementt.count)))
         incrementt.append("\(incrementt.count + outgoingQ.count)")
      if incrementt.count > 4 {
          var clockwisem: Float = 1.0
         incrementt.append("\((Int(clockwisem > 194589387.0 || clockwisem < -194589387.0 ? 44.0 : clockwisem) << (Swift.min(labs(2), 5))))")
      }
      if incrementt.count < 1 {
         outgoingQ = "\((outgoingQ == (String(cString:[111,0], encoding: .utf8)!) ? outgoingQ.count : incrementt.count))"
      }
      while (incrementt.contains(outgoingQ)) {
          var oneH: [String: Any]! = [String(cString: [104,119,114,97,110,100,0], encoding: .utf8)!:String(cString: [116,114,97,110,115,105,116,105,111,110,105,110,103,0], encoding: .utf8)!, String(cString: [101,120,112,101,114,105,109,101,110,116,97,108,0], encoding: .utf8)!:String(cString: [98,108,117,114,108,101,115,115,0], encoding: .utf8)!, String(cString: [114,101,97,108,108,111,99,112,0], encoding: .utf8)!:String(cString: [105,112,108,105,109,97,103,101,0], encoding: .utf8)!]
          _ = oneH
          var previous3: Bool = false
          var locked4: Double = 5.0
         withUnsafeMutablePointer(to: &locked4) { pointer in
    
         }
         incrementt = "\((3 & Int(locked4 > 52075072.0 || locked4 < -52075072.0 ? 95.0 : locked4)))"
         oneH = ["\(locked4)": ((previous3 ? 5 : 2) / (Swift.max(Int(locked4 > 128522413.0 || locked4 < -128522413.0 ? 68.0 : locked4), 3)))]
         previous3 = 82.47 > locked4
         break
      }
      launchO.append("\(((String(cString:[113,0], encoding: .utf8)!) == launchO ? launchO.count : pasty.count))")

   if !pasty.hasSuffix(launchO) {
      launchO = "\(launchO.count)"
   }
        backButton.addTarget(self, action: #selector(clickBackButton), for: .touchUpInside)
        likeButton.addTarget(self, action: #selector(onLikeTapped), for: .touchUpInside)
        moreButton.addTarget(self, action: #selector(onMoreTapped), for: .touchUpInside)
    }

@discardableResult
 func trackCellPersistNeverSpring(scriptsCenter: Bool, fireArea: Int) -> [Any]! {
    var hcopy_btE: String! = String(cString: [112,101,114,102,0], encoding: .utf8)!
    var rankd: Bool = false
    var dotm: [Any]! = [UILabel()]
   withUnsafeMutablePointer(to: &dotm) { pointer in
          _ = pointer.pointee
   }
      rankd = 47 < dotm.count
   if 2 > (2 & dotm.count) {
      hcopy_btE = "\(((rankd ? 4 : 3) % (Swift.max(hcopy_btE.count, 1))))"
   }
   return dotm

}






    private func applyContent() {
       var shake6: [String: Any]! = [String(cString: [122,111,111,109,105,110,103,0], encoding: .utf8)!:89, String(cString: [102,114,97,109,101,99,114,99,0], encoding: .utf8)!:96, String(cString: [103,97,105,110,115,0], encoding: .utf8)!:90]
   for _ in 0 ..< 2 {
      shake6["\(shake6.keys.count)"] = shake6.count << (Swift.min(5, shake6.keys.count))
   }

        avatarImageView.image = postItem.avatarImageName.toImage
        nameLabel.text = postItem.userName
        captionLabel.text = postItem.content
        likeButton.isSelected = isLiked
    }

@discardableResult
 func arrayMediaTableIcon(rowIndex: Double, httpLogin: Int) -> Double {
    var creates: [Any]! = [32, 48]
    var screenh: Double = 3.0
    var rankl: Double = 2.0
      rankl -= (Double(Int(screenh > 145093893.0 || screenh < -145093893.0 ? 47.0 : screenh)))
   for _ in 0 ..< 2 {
       var topf: String! = String(cString: [100,111,119,110,108,111,97,100,101,114,0], encoding: .utf8)!
       _ = topf
       var pushA: String! = String(cString: [97,112,112,115,102,108,121,101,114,0], encoding: .utf8)!
       var template_gS: Bool = false
      withUnsafeMutablePointer(to: &template_gS) { pointer in
             _ = pointer.pointee
      }
       var insufficientK: String! = String(cString: [99,111,111,114,100,105,110,97,116,101,115,0], encoding: .utf8)!
       var size_vzW: String! = String(cString: [119,105,100,103,101,116,0], encoding: .utf8)!
         insufficientK = "\(insufficientK.count - 2)"
       var chatf: String! = String(cString: [116,120,116,110,105,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &chatf) { pointer in
    
      }
         insufficientK = "\(topf.count / (Swift.max(7, pushA.count)))"
          var delete_l4: String! = String(cString: [107,95,52,56,95,115,112,101,99,105,102,105,99,0], encoding: .utf8)!
          _ = delete_l4
          var formatterX: Double = 0.0
          var closeX: String! = String(cString: [115,104,111,114,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &closeX) { pointer in
    
         }
         pushA.append("\(2)")
         delete_l4.append("\(((template_gS ? 1 : 5) - pushA.count))")
         formatterX -= (Double(Int(formatterX > 280378524.0 || formatterX < -280378524.0 ? 10.0 : formatterX) - 1))
         closeX.append("\(2 << (Swift.min(3, chatf.count)))")
      for _ in 0 ..< 1 {
         topf = "\(3)"
      }
      repeat {
         topf = "\(((String(cString:[97,0], encoding: .utf8)!) == pushA ? insufficientK.count : pushA.count))"
         if topf.count == 803961 {
            break
         }
      } while (topf.count == 803961) && (chatf == String(cString:[118,0], encoding: .utf8)!)
          var person1: String! = String(cString: [117,98,115,99,114,105,98,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &person1) { pointer in
    
         }
          var dataI: String! = String(cString: [97,100,100,111,112,0], encoding: .utf8)!
          var messagesu: String! = String(cString: [97,99,99,101,112,116,97,98,108,101,0], encoding: .utf8)!
         topf = "\(topf.count)"
         person1.append("\(pushA.count ^ topf.count)")
         dataI = "\(((template_gS ? 3 : 4)))"
         messagesu = "\(((String(cString:[70,0], encoding: .utf8)!) == pushA ? chatf.count : pushA.count))"
      repeat {
         chatf = "\(size_vzW.count / (Swift.max(3, 1)))"
         if chatf.count == 3685510 {
            break
         }
      } while (size_vzW.count <= 3) && (chatf.count == 3685510)
      if topf.count == 2 {
         topf.append("\(((template_gS ? 5 : 4)))")
      }
          var scales: Bool = true
         withUnsafeMutablePointer(to: &scales) { pointer in
    
         }
          var mapW: String! = String(cString: [110,105,110,101,0], encoding: .utf8)!
          _ = mapW
         size_vzW.append("\(((scales ? 2 : 2) << (Swift.min(2, labs((template_gS ? 3 : 1))))))")
         mapW.append("\((insufficientK == (String(cString:[115,0], encoding: .utf8)!) ? (template_gS ? 2 : 3) : insufficientK.count))")
          var fontX: Double = 1.0
          var follow1: String! = String(cString: [117,110,99,111,100,101,100,0], encoding: .utf8)!
          var gainc: String! = String(cString: [97,117,114,97,0], encoding: .utf8)!
          _ = gainc
         chatf = "\((Int(fontX > 328307940.0 || fontX < -328307940.0 ? 11.0 : fontX)))"
         follow1.append("\(chatf.count + insufficientK.count)")
         gainc.append("\(size_vzW.count)")
      while (pushA != size_vzW) {
         size_vzW.append("\(pushA.count)")
         break
      }
       var verticale: String! = String(cString: [116,114,97,110,115,97,99,116,105,111,110,115,0], encoding: .utf8)!
       var followingu: String! = String(cString: [115,105,103,110,101,100,0], encoding: .utf8)!
          var devicey: Double = 4.0
          _ = devicey
          var nnew_az: String! = String(cString: [115,101,113,110,111,0], encoding: .utf8)!
         verticale.append("\((Int(devicey > 214422881.0 || devicey < -214422881.0 ? 2.0 : devicey) / 3))")
         nnew_az.append("\(followingu.count << (Swift.min(verticale.count, 5)))")
      if !followingu.hasSuffix(pushA) {
         pushA.append("\(((String(cString:[109,0], encoding: .utf8)!) == pushA ? followingu.count : pushA.count))")
      }
      rankl += (Double((template_gS ? 2 : 3)))
   }
   repeat {
      creates = [creates.count % 2]
      if 1114739 == creates.count {
         break
      }
   } while (1114739 == creates.count) && (5 <= (creates.count ^ 1))
   return screenh

}






    @objc private func onMoreTapped() {
       var secondsC: [String: Any]! = [String(cString: [112,111,108,108,0], encoding: .utf8)!:21, String(cString: [97,99,113,117,105,114,101,0], encoding: .utf8)!:64, String(cString: [98,101,103,105,110,110,105,110,103,0], encoding: .utf8)!:71]
       var offe: [String: Any]! = [String(cString: [105,116,97,108,105,99,0], encoding: .utf8)!:46, String(cString: [97,114,101,0], encoding: .utf8)!:39]
       var response2: Double = 3.0
      withUnsafeMutablePointer(to: &response2) { pointer in
    
      }
      while (4 > (offe.keys.count * Int(response2 > 39143858.0 || response2 < -39143858.0 ? 14.0 : response2))) {
          var commentM: Double = 3.0
          var text7: Double = 2.0
          var two_: String! = String(cString: [101,120,112,97,110,100,101,100,0], encoding: .utf8)!
         offe = ["\(commentM)": (Int(text7 > 346136685.0 || text7 < -346136685.0 ? 7.0 : text7))]
         two_.append("\(two_.count)")
         break
      }
      repeat {
         response2 += (Double(Int(response2 > 341623782.0 || response2 < -341623782.0 ? 6.0 : response2) % 2))
         if response2 == 669772.0 {
            break
         }
      } while (response2 == 669772.0) && (1 >= (offe.keys.count - 1) && (response2 - 5.42) >= 2.94)
      secondsC["\(secondsC.count)"] = secondsC.keys.count >> (Swift.min(labs(3), 3))

        ep_presentReportSheet { [weak self] _ in
            guard let self, !self.postItem.postId.isEmpty else { return }
            self.ep_hidePost(postId: self.postItem.postId)
            guard let nav = self.navigationController else { return }
            nav.popViewController(animated: false)
            nav.popViewController(animated: true)
        }
    }


    private func setupConstraints() {
       var createdX: [String: Any]! = [String(cString: [112,101,114,115,111,110,115,0], encoding: .utf8)!:34, String(cString: [99,111,114,114,101,99,116,105,110,103,0], encoding: .utf8)!:75, String(cString: [115,116,111,112,112,101,100,0], encoding: .utf8)!:90]
      createdX["\(createdX.keys.count)"] = createdX.keys.count


        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(10)
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.size.equalTo(44)
        }
        
        actionStack.snp.makeConstraints { make in
            make.trailing.equalToSuperview().offset(-16)
            make.bottom.equalTo(avatarImageView.snp.top).offset(-90)
            make.width.equalTo(42)
            make.height.equalTo(100)
        }
        
        likeButton.snp.makeConstraints { make in
            make.size.equalTo(42)
        }

        moreButton.snp.makeConstraints { make in
            make.size.equalTo(42)
        }

        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(16)
            make.bottom.equalTo(captionLabel.snp.top).offset(10)
            make.size.equalTo(48)
        }
        
        nameLabel.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(67)
            make.bottom.equalTo(captionLabel.snp.top)
            make.height.equalTo(28)
        }

        captionLabel.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(67)
            make.trailing.equalToSuperview().offset(-20)
            make.bottom.equalTo(view.safeAreaLayoutGuide).offset(-20)
        }
    }

@discardableResult
 func scanDestructiveFetchThatOff() -> Double {
    var personz: [String: Any]! = [String(cString: [97,110,115,105,0], encoding: .utf8)!:UILabel(frame:CGRect.zero)]
   withUnsafeMutablePointer(to: &personz) { pointer in
          _ = pointer.pointee
   }
    var homeA: String! = String(cString: [103,101,116,114,97,110,100,111,109,0], encoding: .utf8)!
    var http9: Double = 4.0
       var matchedy: [String: Any]! = [String(cString: [97,115,121,109,0], encoding: .utf8)!:String(cString: [112,114,105,109,101,115,0], encoding: .utf8)!, String(cString: [100,101,105,110,105,116,0], encoding: .utf8)!:String(cString: [119,97,108,107,101,114,0], encoding: .utf8)!]
         matchedy = ["\(matchedy.count)": matchedy.keys.count]
      for _ in 0 ..< 1 {
         matchedy["\(matchedy.keys.count)"] = matchedy.keys.count >> (Swift.min(labs(1), 2))
      }
      while ((matchedy.keys.count ^ matchedy.values.count) < 4 || (matchedy.values.count ^ 4) < 2) {
         matchedy = ["\(matchedy.keys.count)": 1]
         break
      }
      http9 -= (Double((String(cString:[66,0], encoding: .utf8)!) == homeA ? personz.values.count : homeA.count))
       var yesterdayU: Float = 0.0
       var t_productsZ: Bool = false
       _ = t_productsZ
       var pendingd: [Any]! = [17, 65]
      withUnsafeMutablePointer(to: &pendingd) { pointer in
             _ = pointer.pointee
      }
       var visibleq: [Any]! = [String(cString: [102,97,97,110,105,100,99,116,0], encoding: .utf8)!, String(cString: [100,105,97,108,111,103,117,101,0], encoding: .utf8)!, String(cString: [99,111,108,111,110,0], encoding: .utf8)!]
       _ = visibleq
          var pwd5: [String: Any]! = [String(cString: [115,121,115,116,101,109,100,101,112,101,110,100,101,110,116,0], encoding: .utf8)!:99, String(cString: [117,110,99,111,109,112,114,101,115,115,0], encoding: .utf8)!:57, String(cString: [100,105,102,102,120,0], encoding: .utf8)!:4]
          var successS: Float = 3.0
          _ = successS
          var window_23: Float = 2.0
         t_productsZ = (Float(visibleq.count) + yesterdayU) >= 56.39
         pwd5 = ["\(yesterdayU)": ((t_productsZ ? 2 : 5) << (Swift.min(labs(Int(yesterdayU > 136903224.0 || yesterdayU < -136903224.0 ? 91.0 : yesterdayU)), 1)))]
         successS -= (Float(Int(yesterdayU > 192295580.0 || yesterdayU < -192295580.0 ? 92.0 : yesterdayU)))
         window_23 += (Float(pwd5.keys.count | Int(window_23 > 69248258.0 || window_23 < -69248258.0 ? 46.0 : window_23)))
         visibleq = [visibleq.count]
         t_productsZ = ((pendingd.count | (!t_productsZ ? pendingd.count : 94)) < 94)
          var peerV: Double = 1.0
          _ = peerV
         visibleq = [pendingd.count]
         peerV -= (Double(3 * (t_productsZ ? 5 : 1)))
      if t_productsZ {
          var contentq: String! = String(cString: [114,103,98,110,0], encoding: .utf8)!
          var reuseE: Double = 4.0
         yesterdayU /= Swift.max((Float(3 << (Swift.min(labs(Int(reuseE > 322089840.0 || reuseE < -322089840.0 ? 79.0 : reuseE)), 1)))), 3)
         contentq.append("\((3 ^ Int(reuseE > 253977084.0 || reuseE < -253977084.0 ? 59.0 : reuseE)))")
      }
      homeA.append("\((Int(yesterdayU > 14901632.0 || yesterdayU < -14901632.0 ? 50.0 : yesterdayU)))")
   if !homeA.hasPrefix("\(personz.values.count)") {
      personz = ["\(http9)": homeA.count]
   }
   return http9

}






    @objc private func playerDidFinish() {
       var reverseW: String! = String(cString: [97,115,115,111,99,108,105,115,116,0], encoding: .utf8)!
   while (reverseW == reverseW) {
       var fillv: Double = 4.0
       var max_clr: String! = String(cString: [109,118,101,120,0], encoding: .utf8)!
         max_clr.append("\(max_clr.count)")
       var postr: Double = 4.0
       var authorizationK: Double = 5.0
      repeat {
          var toastX: Double = 0.0
         max_clr = "\((Int(fillv > 23187241.0 || fillv < -23187241.0 ? 8.0 : fillv) & 3))"
         toastX += (Double(Int(fillv > 145688448.0 || fillv < -145688448.0 ? 13.0 : fillv) + 2))
         if max_clr == (String(cString:[48,53,56,51,0], encoding: .utf8)!) {
            break
         }
      } while ((postr / 4.90) < 5.30) && (max_clr == (String(cString:[48,53,56,51,0], encoding: .utf8)!))
          var img6: String! = String(cString: [97,110,110,111,117,110,99,101,109,101,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &img6) { pointer in
    
         }
          var storedP: Double = 4.0
         authorizationK *= Double(img6.count)
         storedP -= (Double(max_clr == (String(cString:[102,0], encoding: .utf8)!) ? max_clr.count : Int(authorizationK > 245152544.0 || authorizationK < -245152544.0 ? 75.0 : authorizationK)))
      if 1.87 >= (fillv - authorizationK) {
          var video7: String! = String(cString: [98,111,117,110,100,97,114,105,101,115,0], encoding: .utf8)!
          _ = video7
          var sheety: String! = String(cString: [122,111,110,101,0], encoding: .utf8)!
          var edito: String! = String(cString: [99,114,108,115,0], encoding: .utf8)!
          var animateW: String! = String(cString: [105,110,108,105,110,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &animateW) { pointer in
    
         }
          var commentsF: String! = String(cString: [114,115,116,114,105,112,0], encoding: .utf8)!
         fillv /= Swift.max(Double(commentsF.count), 1)
         video7.append("\((edito.count / (Swift.max(4, Int(authorizationK > 240226999.0 || authorizationK < -240226999.0 ? 18.0 : authorizationK)))))")
         sheety.append("\(sheety.count)")
         edito = "\(commentsF.count)"
         animateW.append("\((Int(postr > 16934806.0 || postr < -16934806.0 ? 97.0 : postr) << (Swift.min(1, labs(Int(authorizationK > 303973251.0 || authorizationK < -303973251.0 ? 86.0 : authorizationK))))))")
      }
      for _ in 0 ..< 3 {
          var sheetk: String! = String(cString: [107,97,110,110,97,0], encoding: .utf8)!
          var handle8: String! = String(cString: [115,117,98,112,101,108,0], encoding: .utf8)!
          var workk: Float = 5.0
          var testm: Int = 2
          var areah: String! = String(cString: [104,101,120,105,110,116,0], encoding: .utf8)!
         postr -= Double(1 << (Swift.min(3, handle8.count)))
         sheetk.append("\((Int(workk > 253236795.0 || workk < -253236795.0 ? 79.0 : workk) & testm))")
         workk /= Swift.max(Float(areah.count), 5)
         testm |= (Int(fillv > 126353163.0 || fillv < -126353163.0 ? 13.0 : fillv) | max_clr.count)
         areah.append("\(handle8.count)")
      }
      reverseW = "\(reverseW.count)"
      break
   }

        player?.seek(to: .zero)
        player?.play()
    }

@discardableResult
 func testPermissionProgress(originCodes: [String: Any]!, beforeSelected: [String: Any]!, remainingPresented: Double) -> Bool {
    var fireN: String! = String(cString: [116,111,107,101,110,105,122,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &fireN) { pointer in
          _ = pointer.pointee
   }
    var halfU: [Any]! = [73, 76]
   withUnsafeMutablePointer(to: &halfU) { pointer in
          _ = pointer.pointee
   }
    var containerd: Bool = false
   repeat {
      halfU = [halfU.count << (Swift.min(fireN.count, 5))]
      if halfU.count == 3637074 {
         break
      }
   } while (halfU.count == 3637074) && (1 < (halfU.count & fireN.count) || 1 < (halfU.count & fireN.count))
       var compatW: String! = String(cString: [103,114,97,110,116,0], encoding: .utf8)!
       _ = compatW
      while (compatW.count < compatW.count) {
         compatW.append("\(compatW.count << (Swift.min(labs(2), 5)))")
         break
      }
       var morez: Double = 2.0
       var interval_hqE: String! = String(cString: [108,97,121,101,114,99,111,110,116,101,120,116,0], encoding: .utf8)!
       _ = interval_hqE
         morez += (Double(Int(morez > 65867672.0 || morez < -65867672.0 ? 69.0 : morez)))
         interval_hqE = "\(compatW.count & 1)"
      halfU = [fireN.count]
   repeat {
      fireN.append("\((2 - (containerd ? 5 : 1)))")
      if fireN.count == 1032466 {
         break
      }
   } while (fireN.contains("\(halfU.count)")) && (fireN.count == 1032466)
   return containerd

}






    override func viewDidAppear(_ animated: Bool) {
       var valueT: String! = String(cString: [115,117,109,115,113,0], encoding: .utf8)!
    var seconds9: String! = String(cString: [99,97,114,114,121,0], encoding: .utf8)!
      valueT.append("\(valueT.count + seconds9.count)")

      seconds9 = "\(seconds9.count & 3)"
        super.viewDidAppear(animated)
        player?.play()
    }

@discardableResult
 func translateComponentRegular(appearanceFlag: Double, buttonTable: Bool, availableResolve: Double) -> Float {
    var nickx: String! = String(cString: [98,105,110,100,101,114,0], encoding: .utf8)!
    var error3: Double = 3.0
    var folderK: Float = 5.0
    _ = folderK
      nickx.append("\((Int(error3 > 27778918.0 || error3 < -27778918.0 ? 57.0 : error3)))")
      nickx.append("\(nickx.count)")
   if 2 <= (Int(error3 > 58690713.0 || error3 < -58690713.0 ? 7.0 : error3) - nickx.count) {
      error3 -= Double(nickx.count / 1)
   }
       var control0: [String: Any]! = [String(cString: [115,101,116,115,0], encoding: .utf8)!:String(cString: [114,101,99,101,110,116,101,114,0], encoding: .utf8)!, String(cString: [105,100,115,0], encoding: .utf8)!:String(cString: [118,116,97,115,107,113,117,101,117,101,0], encoding: .utf8)!]
       var wasd: String! = String(cString: [115,101,113,117,101,110,99,101,115,0], encoding: .utf8)!
       _ = wasd
         control0[wasd] = 3
      repeat {
         wasd = "\(control0.count - wasd.count)"
         if wasd == (String(cString:[109,100,119,114,104,0], encoding: .utf8)!) {
            break
         }
      } while (wasd == (String(cString:[109,100,119,114,104,0], encoding: .utf8)!)) && ((3 >> (Swift.min(4, control0.keys.count))) < 1)
       var numberS: Bool = true
       var visiblei: Bool = false
      withUnsafeMutablePointer(to: &visiblei) { pointer in
             _ = pointer.pointee
      }
          var attributesT: Double = 1.0
         wasd = "\(control0.keys.count << (Swift.min(labs(1), 1)))"
         attributesT += (Double((numberS ? 4 : 4) ^ 1))
          var indexR: Bool = false
         withUnsafeMutablePointer(to: &indexR) { pointer in
                _ = pointer.pointee
         }
         visiblei = control0.keys.count == 87
         indexR = (visiblei ? indexR : !visiblei)
         visiblei = (((!numberS ? wasd.count : 86) ^ wasd.count) <= 86)
      folderK -= (Float((String(cString:[68,0], encoding: .utf8)!) == wasd ? wasd.count : control0.count))
   return folderK

}






    override func viewDidLoad() {
       var itemT: [Any]! = [10, 28, 12]
    _ = itemT
    var xnew_h2S: [Any]! = [81, 33]
   repeat {
       var descG: Bool = true
      withUnsafeMutablePointer(to: &descG) { pointer in
    
      }
       var confirmx: String! = String(cString: [112,114,111,106,101,99,116,0], encoding: .utf8)!
      while (!descG) {
         confirmx.append("\(((descG ? 4 : 2) / 2))")
         break
      }
      if confirmx.count >= 5 && !descG {
         confirmx = "\((confirmx.count ^ (descG ? 3 : 4)))"
      }
      repeat {
         confirmx = "\(3)"
         if confirmx == (String(cString:[114,95,111,115,0], encoding: .utf8)!) {
            break
         }
      } while (confirmx == (String(cString:[114,95,111,115,0], encoding: .utf8)!)) && (confirmx.contains("\(descG)"))
      repeat {
          var profile_: Double = 0.0
         confirmx = "\((2 << (Swift.min(2, labs((descG ? 1 : 4))))))"
         profile_ /= Swift.max((Double(Int(profile_ > 264911059.0 || profile_ < -264911059.0 ? 16.0 : profile_) ^ 2)), 4)
         if confirmx.count == 952873 {
            break
         }
      } while (4 > confirmx.count) && (confirmx.count == 952873)
      if 3 < confirmx.count {
         confirmx.append("\(confirmx.count)")
      }
         confirmx = "\(3)"
      itemT = [(3 << (Swift.min(5, labs((descG ? 1 : 3)))))]
      if 4705363 == itemT.count {
         break
      }
   } while ((itemT.count - 5) <= 1) && (4705363 == itemT.count)

      xnew_h2S.append(1)
        super.viewDidLoad()

        bgView.isHidden = true
        view.backgroundColor = .black

        setupPlayer()
        setupUI()
        setupConstraints()
        setupEvents()
        applyContent()
    }


    override func viewWillDisappear(_ animated: Bool) {
       var rebuildz: String! = String(cString: [114,101,97,100,113,0], encoding: .utf8)!
      rebuildz.append("\(rebuildz.count)")

        super.viewWillDisappear(animated)
        player?.pause()
        if isMovingFromParent {
            NotificationCenter.default.removeObserver(self)
        }
    }

@discardableResult
 func directionalAlertResistanceVelocity(dirWelcome: [Any]!, areaText: [String: Any]!) -> String! {
    var previewB: Float = 0.0
    _ = previewB
    var thresholdk: Bool = true
   withUnsafeMutablePointer(to: &thresholdk) { pointer in
    
   }
    var int_eo: String! = String(cString: [104,97,115,104,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &int_eo) { pointer in
    
   }
   repeat {
      thresholdk = 91.36 >= previewB
      if thresholdk ? !thresholdk : thresholdk {
         break
      }
   } while (previewB == 3.53 || (3.53 * previewB) == 2.6) && (thresholdk ? !thresholdk : thresholdk)
   while (int_eo.count >= 3 && !thresholdk) {
      thresholdk = int_eo.hasSuffix("\(thresholdk)")
      break
   }
   return int_eo

}






    @objc private func onLikeTapped() {
       var videoy: Double = 3.0
      videoy += (Double(Int(videoy > 59530588.0 || videoy < -59530588.0 ? 41.0 : videoy)))

        isLiked.toggle()
        likeButton.isSelected = isLiked
    }

@discardableResult
 func documentFinishTranslucentSign(endSetting: Double, frame_rClockwise: Double) -> Double {
    var reportF: Double = 0.0
    var top7: String! = String(cString: [116,97,103,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &top7) { pointer in
    
   }
    var deviceU: Double = 3.0
      deviceU -= (Double(Int(reportF > 151815901.0 || reportF < -151815901.0 ? 59.0 : reportF) - 2))
      top7 = "\((top7.count >> (Swift.min(3, labs(Int(deviceU > 179023364.0 || deviceU < -179023364.0 ? 96.0 : deviceU))))))"
   while (5.34 == deviceU) {
      top7 = "\((Int(deviceU > 64184177.0 || deviceU < -64184177.0 ? 39.0 : deviceU)))"
      break
   }
   return reportF

}






    

    private func setupUI() {
       var deletedt: [Any]! = [53, 53]
    var allk: Double = 2.0
   repeat {
      allk -= (Double(deletedt.count + Int(allk > 145442382.0 || allk < -145442382.0 ? 55.0 : allk)))
      if 1611397.0 == allk {
         break
      }
   } while (1611397.0 == allk) && (1.50 > allk)

       var clear7: String! = String(cString: [105,110,102,108,97,116,101,0], encoding: .utf8)!
       var remain8: [Any]! = [String(cString: [102,114,105,101,110,100,108,121,0], encoding: .utf8)!, String(cString: [97,109,98,105,101,110,116,0], encoding: .utf8)!, String(cString: [114,101,99,97,108,99,117,108,97,116,105,111,110,0], encoding: .utf8)!]
       var submitp: String! = String(cString: [100,105,115,99,111,110,110,101,99,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &submitp) { pointer in
    
      }
       var providerK: String! = String(cString: [112,97,121,109,97,115,116,101,114,0], encoding: .utf8)!
       var blockedD: String! = String(cString: [114,101,118,97,108,105,100,97,116,105,111,110,0], encoding: .utf8)!
       _ = blockedD
         clear7 = "\(2)"
      while ((3 | submitp.count) <= 3) {
          var quarterG: Float = 0.0
          var offZ: String! = String(cString: [98,108,111,119,102,105,115,104,0], encoding: .utf8)!
         submitp = "\(clear7.count << (Swift.min(5, providerK.count)))"
         quarterG -= (Float((String(cString:[76,0], encoding: .utf8)!) == clear7 ? submitp.count : clear7.count))
         offZ.append("\(2)")
         break
      }
          var attributesr: String! = String(cString: [99,111,110,116,97,105,110,115,0], encoding: .utf8)!
         clear7.append("\(clear7.count)")
         attributesr = "\(clear7.count << (Swift.min(labs(3), 3)))"
      while (submitp != String(cString:[80,0], encoding: .utf8)! && providerK == String(cString:[107,0], encoding: .utf8)!) {
         submitp = "\(clear7.count)"
         break
      }
         blockedD.append("\(clear7.count + 3)")
         submitp = "\(providerK.count + 1)"
         providerK = "\(blockedD.count)"
      if clear7 != providerK {
         providerK.append("\(1)")
      }
      deletedt = [(Int(allk > 133001390.0 || allk < -133001390.0 ? 13.0 : allk) | 1)]
      remain8 = [(Int(allk > 370447504.0 || allk < -370447504.0 ? 13.0 : allk) / 3)]
        view.addSubview(backButton)
        view.addSubview(avatarImageView)
        view.addSubview(nameLabel)
        view.addSubview(captionLabel)
        view.addSubview(actionStack)
    }


    override func viewDidLayoutSubviews() {
       var nanosecondv: [Any]! = [[38, 78, 24]]
      nanosecondv.append(nanosecondv.count)

        super.viewDidLayoutSubviews()
        playerLayer?.frame = view.bounds
    }


    @objc private func clickBackButton() {
       var setting2: Float = 4.0
    var interitem9: String! = String(cString: [103,117,97,114,97,110,116,101,101,115,0], encoding: .utf8)!
    _ = interitem9
       var moren: Double = 0.0
       var constraintv: Double = 2.0
       var rectj: String! = String(cString: [109,97,107,101,110,97,110,0], encoding: .utf8)!
       var gain8: String! = String(cString: [110,111,110,100,99,0], encoding: .utf8)!
         moren += (Double(rectj.count / (Swift.max(2, Int(moren > 112183385.0 || moren < -112183385.0 ? 29.0 : moren)))))
         rectj.append("\((rectj == (String(cString:[70,0], encoding: .utf8)!) ? Int(constraintv > 321038424.0 || constraintv < -321038424.0 ? 38.0 : constraintv) : rectj.count))")
      if 4 >= (Int(moren > 123584131.0 || moren < -123584131.0 ? 47.0 : moren) - rectj.count) && (rectj.count + 4) >= 3 {
          var window_lrY: [String: Any]! = [String(cString: [109,105,108,108,101,114,0], encoding: .utf8)!:24, String(cString: [115,101,112,97,114,97,116,101,100,0], encoding: .utf8)!:17]
          var likery: Int = 1
         withUnsafeMutablePointer(to: &likery) { pointer in
                _ = pointer.pointee
         }
         rectj.append("\(rectj.count - likery)")
         window_lrY = ["\(window_lrY.count)": window_lrY.count]
      }
      repeat {
          var tapJ: Double = 0.0
         withUnsafeMutablePointer(to: &tapJ) { pointer in
                _ = pointer.pointee
         }
          var quarter3: String! = String(cString: [115,105,103,110,105,110,103,0], encoding: .utf8)!
         moren /= Swift.max(5, Double(2 + quarter3.count))
         tapJ -= (Double(Int(constraintv > 132279379.0 || constraintv < -132279379.0 ? 59.0 : constraintv)))
         if moren == 2839593.0 {
            break
         }
      } while (3.41 >= (moren / (Swift.max(4.28, 3))) || (moren / (Swift.max(Double(rectj.count), 4))) >= 4.28) && (moren == 2839593.0)
      repeat {
         moren += (Double(Int(moren > 102782404.0 || moren < -102782404.0 ? 38.0 : moren) | 2))
         if 4144163.0 == moren {
            break
         }
      } while (4144163.0 == moren) && (constraintv == 4.65)
      while (3.84 <= (constraintv / (Swift.max(moren, 7)))) {
         moren -= (Double(2 + Int(constraintv > 244570739.0 || constraintv < -244570739.0 ? 55.0 : constraintv)))
         break
      }
         rectj.append("\((Int(constraintv > 121945821.0 || constraintv < -121945821.0 ? 6.0 : constraintv) << (Swift.min(rectj.count, 5))))")
      if 5 > (rectj.count + 5) || (Double(rectj.count) + constraintv) > 2.19 {
         constraintv -= (Double(rectj == (String(cString:[88,0], encoding: .utf8)!) ? Int(constraintv > 79978862.0 || constraintv < -79978862.0 ? 67.0 : constraintv) : rectj.count))
      }
         gain8 = "\((Int(constraintv > 218973817.0 || constraintv < -218973817.0 ? 15.0 : constraintv)))"
      interitem9.append("\((rectj == (String(cString:[84,0], encoding: .utf8)!) ? rectj.count : Int(constraintv > 34936148.0 || constraintv < -34936148.0 ? 13.0 : constraintv)))")
   while (4.98 > (4.48 - setting2) && (setting2 - setting2) > 4.48) {
      setting2 -= (Float(Int(setting2 > 35461962.0 || setting2 < -35461962.0 ? 32.0 : setting2)))
      break
   }

        navigationController?.popViewController(animated: true)
    }

    
    private let backButton: UIButton = {
       var insetk: Float = 0.0
       var nick_: Int = 0
      repeat {
         nick_ -= 3 & nick_
         if nick_ == 1592788 {
            break
         }
      } while (nick_ <= 4) && (nick_ == 1592788)
      repeat {
         nick_ |= nick_ + nick_
         if 4675444 == nick_ {
            break
         }
      } while (4675444 == nick_) && (4 == (3 * nick_) && (nick_ * 3) == 3)
      for _ in 0 ..< 1 {
         nick_ >>= Swift.min(labs(nick_ + 2), 2)
      }
      insetk /= Swift.max(2, (Float(Int(insetk > 181629252.0 || insetk < -181629252.0 ? 93.0 : insetk) - nick_)))

        let button = UIButton(type: .custom)
        button.setImage(cornerMapMail([115,127,125,125,127,126,79,114,113,115,123,16],0x10,false).toImage, for: .normal)
        return button
    }()

    private let avatarImageView: UIImageView = {
       var allI: Int = 5
   while ((2 << (Swift.min(4, labs(allI)))) >= 1 || 3 >= (allI << (Swift.min(labs(2), 2)))) {
       var gainq: Bool = true
       var sel2: Float = 2.0
       var firsts: String! = String(cString: [100,112,114,105,110,116,0], encoding: .utf8)!
       var codes7: String! = String(cString: [99,111,110,99,114,101,116,101,0], encoding: .utf8)!
         gainq = (30 >= ((gainq ? firsts.count : 30) / (Swift.max(8, firsts.count))))
      while (!gainq) {
         firsts = "\((Int(sel2 > 366020845.0 || sel2 < -366020845.0 ? 66.0 : sel2) << (Swift.min(3, labs(1)))))"
         break
      }
      if Float(firsts.count) == sel2 {
         firsts.append("\((2 | (gainq ? 4 : 1)))")
      }
         firsts = "\((firsts.count - (gainq ? 4 : 5)))"
          var calendarZ: String! = String(cString: [116,104,97,116,0], encoding: .utf8)!
          var updateds: String! = String(cString: [112,114,101,0], encoding: .utf8)!
          var timeoutd: Double = 2.0
         sel2 /= Swift.max((Float(Int(sel2 > 41849614.0 || sel2 < -41849614.0 ? 50.0 : sel2))), 5)
         calendarZ.append("\(calendarZ.count)")
         updateds = "\(3)"
         timeoutd /= Swift.max(2, Double(updateds.count / 2))
         codes7.append("\(firsts.count * 1)")
         sel2 += (Float(2 * Int(sel2 > 221118485.0 || sel2 < -221118485.0 ? 6.0 : sel2)))
         gainq = codes7.count <= 20
         gainq = codes7.count < 25
      while (gainq) {
          var class_hJ: String! = String(cString: [100,101,113,117,101,117,101,100,0], encoding: .utf8)!
          var localizationA: Bool = false
          _ = localizationA
          var closee: Int = 1
          var resourcej: String! = String(cString: [103,101,116,104,111,115,116,98,121,110,97,109,101,0], encoding: .utf8)!
         gainq = (76 >= (resourcej.count | (localizationA ? resourcej.count : 76)))
         class_hJ = "\(((localizationA ? 4 : 2) + 3))"
         closee -= codes7.count
         break
      }
      if firsts != String(cString:[80,0], encoding: .utf8)! || codes7.count <= 3 {
          var dayd: Double = 2.0
          var dir1: String! = String(cString: [116,103,101,116,0], encoding: .utf8)!
          var s_playerM: String! = String(cString: [115,116,115,100,0], encoding: .utf8)!
          var thumbh: String! = String(cString: [117,112,115,104,105,102,116,101,100,0], encoding: .utf8)!
          _ = thumbh
          var confirmS: Double = 2.0
         firsts.append("\((Int(sel2 > 248186868.0 || sel2 < -248186868.0 ? 41.0 : sel2) >> (Swift.min(3, labs((gainq ? 1 : 3))))))")
         dayd -= (Double(codes7 == (String(cString:[120,0], encoding: .utf8)!) ? codes7.count : Int(sel2 > 250836445.0 || sel2 < -250836445.0 ? 38.0 : sel2)))
         dir1.append("\(2)")
         s_playerM.append("\((thumbh == (String(cString:[105,0], encoding: .utf8)!) ? thumbh.count : firsts.count))")
         confirmS += (Double(3 - Int(dayd > 31449380.0 || dayd < -31449380.0 ? 14.0 : dayd)))
      }
         codes7 = "\((Int(sel2 > 117687378.0 || sel2 < -117687378.0 ? 94.0 : sel2) % (Swift.max(3, (gainq ? 4 : 2)))))"
      allI /= Swift.max((3 + Int(sel2 > 251213643.0 || sel2 < -251213643.0 ? 82.0 : sel2)), 5)
      break
   }

        let view = UIImageView()
        view.contentMode = .scaleAspectFill
        view.clipsToBounds = true
        view.layer.cornerRadius = 24
        view.layer.masksToBounds = true
        return view
    }()

    private let nameLabel: UILabel = {
       var kindT: Float = 2.0
   withUnsafeMutablePointer(to: &kindT) { pointer in
    
   }
    var jsony: String! = String(cString: [110,108,109,101,97,110,115,0], encoding: .utf8)!
      kindT += (Float(Int(kindT > 5783366.0 || kindT < -5783366.0 ? 71.0 : kindT) ^ 1))

        let label = UILabel()
      kindT += (Float(jsony == (String(cString:[50,0], encoding: .utf8)!) ? jsony.count : Int(kindT > 306163346.0 || kindT < -306163346.0 ? 60.0 : kindT)))
        label.font = .systemFont(ofSize: 20, weight: .semibold)
        label.textColor = .white
        return label
    }()

    private let captionLabel: UILabel = {
       var remainD: String! = String(cString: [105,110,116,101,114,97,116,105,111,110,0], encoding: .utf8)!
    _ = remainD
    var docN: String! = String(cString: [97,114,101,113,117,101,115,116,0], encoding: .utf8)!
      remainD = "\(docN.count % 3)"

        let label = UILabel()
   if 5 <= remainD.count {
      remainD = "\(remainD.count / 1)"
   }
        label.font = .systemFont(ofSize: 14, weight: .medium)
       var mailx: Bool = true
       var class_v0: String! = String(cString: [114,101,109,97,114,107,0], encoding: .utf8)!
       var layoutw: String! = String(cString: [116,97,98,108,101,108,105,109,0], encoding: .utf8)!
         class_v0 = "\(1)"
      for _ in 0 ..< 3 {
         class_v0 = "\(3)"
      }
         class_v0 = "\(((mailx ? 3 : 2) * layoutw.count))"
         mailx = !layoutw.hasSuffix("\(mailx)")
      while (4 == class_v0.count) {
         layoutw = "\((layoutw == (String(cString:[108,0], encoding: .utf8)!) ? class_v0.count : layoutw.count))"
         break
      }
      while (4 <= class_v0.count) {
         class_v0 = "\(((String(cString:[109,0], encoding: .utf8)!) == class_v0 ? (mailx ? 1 : 2) : class_v0.count))"
         break
      }
       var badgesY: Double = 2.0
       _ = badgesY
       var present3: Double = 4.0
         mailx = (layoutw.count < (Int(badgesY > 343559580.0 || badgesY < -343559580.0 ? 91.0 : badgesY)))
      while (2 >= class_v0.count && mailx) {
         class_v0 = "\(layoutw.count)"
         break
      }
         present3 *= (Double((mailx ? 2 : 2) & class_v0.count))
      docN.append("\((layoutw == (String(cString:[71,0], encoding: .utf8)!) ? docN.count : layoutw.count))")
        label.textColor = .white
        label.numberOfLines = 0
        return label
    }()

    private lazy var actionStack: UIStackView = {
       var size_bj: Int = 1
      size_bj &= 2 ^ size_bj

        let field = UIStackView(arrangedSubviews: [likeButton, moreButton])
        field.axis = .vertical
        field.alignment = .center
        field.spacing = 8
        return field
    }()

    private lazy var likeButton: UIButton = {
       var reuse4: [String: Any]! = [String(cString: [105,112,113,102,0], encoding: .utf8)!:65, String(cString: [103,110,114,101,0], encoding: .utf8)!:99]
    var migratef: Bool = false
   repeat {
      migratef = reuse4["\(migratef)"] != nil
      if migratef ? !migratef : migratef {
         break
      }
   } while (migratef ? !migratef : migratef) && ((2 * reuse4.count) >= 4)

        let outgoingButton = UIButton(type: .custom)
      reuse4["\(migratef)"] = ((migratef ? 5 : 1) | 1)
        outgoingButton.setImage(cornerMapMail([-16,-9,-11,-3,-57,-12,-15,-13,-3,-104],0x98,false).toImage, for: .normal)
        outgoingButton.setImage(cornerMapMail([34,37,39,47,21,38,35,33,47,46,74],0x4A,false).toImage, for: .selected)
        return outgoingButton
    }()

    private lazy var moreButton: UIButton = {
       var horizontalF: String! = String(cString: [108,111,103,105,99,97,108,0], encoding: .utf8)!
      horizontalF.append("\(2 + horizontalF.count)")

        let outgoingButton = UIButton(type: .custom)
        outgoingButton.setImage(cornerMapMail([5,26,6,1,42,24,26,7,16,117],0x75,false).toImage, for: .normal)
        return outgoingButton
    }()
}
