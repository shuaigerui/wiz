
import Foundation

import AuthenticationServices
import Toast_Swift
import UIKit

class EPReportController: UIViewController {

    private enum Layout {
        static let buttonHeight: CGFloat = 68
        static let buttonSpacing: CGFloat = 16
        static let horizontalInset: CGFloat = 36
        static let bottomInset: CGFloat = 48
    }

    private var appleAuthController: ASAuthorizationController?

@discardableResult
 func centerOpaqueSecurityNameScrollView() -> UIScrollView! {
    var fromH: Float = 2.0
    var createdC: [Any]! = [false]
   repeat {
      fromH += Float(createdC.count)
      if 2363538.0 == fromH {
         break
      }
   } while (2363538.0 == fromH) && (createdC.count > (Int(fromH > 201993201.0 || fromH < -201993201.0 ? 83.0 : fromH)))
     let checkRooms: String! = String(cString: [102,97,99,105,108,105,116,97,116,101,0], encoding: .utf8)!
     let threeAnimate: UIView! = UIView()
     let appendOne: [String: Any]! = [String(cString: [115,117,98,118,105,101,119,115,0], encoding: .utf8)!:[String(cString: [111,103,103,100,101,99,0], encoding: .utf8)!]]
     let matchedFive: Double = 6.0
    var getcutBinSelf = UIScrollView(frame:CGRect.zero)
    threeAnimate.frame = CGRect(x: 20, y: 48, width: 0, height: 0)
    threeAnimate.alpha = 0.0;
    threeAnimate.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    getcutBinSelf.addSubview(threeAnimate)
    getcutBinSelf.alwaysBounceVertical = false
    getcutBinSelf.alwaysBounceHorizontal = false
    getcutBinSelf.showsVerticalScrollIndicator = true
    getcutBinSelf.showsHorizontalScrollIndicator = true
    getcutBinSelf.delegate = nil
    getcutBinSelf.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    getcutBinSelf.frame = CGRect(x: 20, y: 255, width: 0, height: 0)
    getcutBinSelf.alpha = 0.0;
    getcutBinSelf.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)

    
    return getcutBinSelf

}





    override func viewDidLoad() {
       var usersG: [Any]! = [34, 93, 90]
    _ = usersG
    var resetE: Float = 1.0
    _ = resetE
      usersG = [(Int(resetE > 182839491.0 || resetE < -182839491.0 ? 42.0 : resetE) & 2)]

   while (5 <= (1 / (Swift.max(2, usersG.count))) && (1 / (Swift.max(6, usersG.count))) <= 3) {
      usersG.append(usersG.count << (Swift.min(labs(1), 4)))
      break
   }
        super.viewDidLoad()
   repeat {
      resetE -= (Float(Int(resetE > 274961403.0 || resetE < -274961403.0 ? 100.0 : resetE) / 1))
      if 4879627.0 == resetE {
         break
      }
   } while (5.84 <= (4.49 + resetE) && 5.5 <= (4.49 * resetE)) && (4879627.0 == resetE)

        view.backgroundColor = .black
        navigationController?.navigationBar.isHidden = true

        setupUI()
        setupConstraints()
        setupEvents()
    }

@discardableResult
 func keyboardThreeDirectory(requestTrailing: Float, trackClose: String!, badgesClose: String!) -> String! {
    var authr: Float = 4.0
    var g_centerU: String! = String(cString: [108,111,103,102,110,0], encoding: .utf8)!
    _ = g_centerU
    var top4: String! = String(cString: [109,105,115,115,105,110,103,0], encoding: .utf8)!
    _ = top4
   for _ in 0 ..< 2 {
      authr /= Swift.max(1, Float(1))
   }
      authr /= Swift.max(5, Float(g_centerU.count))
      top4 = "\(top4.count % 3)"
   return g_centerU

}





    func setupUI() {
       var int_6wu: Int = 3
    var cornerW: String! = String(cString: [97,114,99,116,105,99,0], encoding: .utf8)!
      int_6wu /= Swift.max(cornerW.count, 5)

       var areap: String! = String(cString: [99,111,100,101,114,0], encoding: .utf8)!
       var modity1: [String: Any]! = [String(cString: [116,114,107,110,0], encoding: .utf8)!:61, String(cString: [115,112,97,114,115,101,110,101,115,115,0], encoding: .utf8)!:68, String(cString: [114,101,112,115,116,114,0], encoding: .utf8)!:35]
          var messagesI: Int = 0
         withUnsafeMutablePointer(to: &messagesI) { pointer in
                _ = pointer.pointee
         }
          var trimmedj: Bool = false
         areap = "\(3)"
         messagesI %= Swift.max(4, modity1.keys.count & areap.count)
         trimmedj = 81 < modity1.values.count
      for _ in 0 ..< 3 {
          var messagesW: [Any]! = [42, 33]
         areap.append("\(modity1.values.count + areap.count)")
         messagesW.append(2 * areap.count)
      }
          var legacyT: Bool = true
          _ = legacyT
          var pickedh: String! = String(cString: [105,110,99,114,101,97,115,105,110,103,0], encoding: .utf8)!
         areap.append("\(pickedh.count)")
         legacyT = modity1.values.count > areap.count
         areap.append("\((areap == (String(cString:[98,0], encoding: .utf8)!) ? areap.count : modity1.keys.count))")
      for _ in 0 ..< 3 {
         modity1 = ["\(modity1.keys.count)": modity1.count]
      }
          var fnewsS: Float = 2.0
          var responderY: String! = String(cString: [99,111,114,101,115,0], encoding: .utf8)!
         areap = "\((Int(fnewsS > 334894061.0 || fnewsS < -334894061.0 ? 52.0 : fnewsS)))"
         responderY = "\((Int(fnewsS > 364058819.0 || fnewsS < -364058819.0 ? 100.0 : fnewsS)))"
      int_6wu >>= Swift.min(3, labs(1 - areap.count))
        view.addSubview(bgView)
   for _ in 0 ..< 2 {
       var dotm: Float = 3.0
      while ((dotm - dotm) > 1.98 && 1.9 > (1.98 + dotm)) {
         dotm += Float(1)
         break
      }
         dotm -= Float(3)
       var topc: [Any]! = [22, 100, 46]
       var friend_kt: [Any]! = [38, 54, 73]
      withUnsafeMutablePointer(to: &friend_kt) { pointer in
    
      }
         topc.append(topc.count & 3)
         friend_kt.append(friend_kt.count)
      cornerW = "\((Int(dotm > 127653324.0 || dotm < -127653324.0 ? 43.0 : dotm)))"
   }
        view.addSubview(appleSignInButton)
        view.addSubview(createAccountButton)
        view.addSubview(signInButton)
    }

@discardableResult
 func fieldBundleSecurity(pixelLine: Int, heightOff: Float, postsOwner: String!) -> [Any]! {
    var monthQ: String! = String(cString: [117,116,102,0], encoding: .utf8)!
    var delay9: String! = String(cString: [109,105,110,117,115,0], encoding: .utf8)!
    var update_2lU: [Any]! = [11, 10, 7]
      update_2lU = [2]
      monthQ.append("\(update_2lU.count | 1)")
       var millisecondN: String! = String(cString: [114,101,113,115,116,97,116,101,0], encoding: .utf8)!
      repeat {
         millisecondN.append("\(((String(cString:[82,0], encoding: .utf8)!) == millisecondN ? millisecondN.count : millisecondN.count))")
         if millisecondN == (String(cString:[53,52,107,109,104,113,99,121,113,110,0], encoding: .utf8)!) {
            break
         }
      } while (millisecondN == (String(cString:[53,52,107,109,104,113,99,121,113,110,0], encoding: .utf8)!)) && (millisecondN.count == millisecondN.count)
         millisecondN.append("\(millisecondN.count - millisecondN.count)")
      while (millisecondN != String(cString:[75,0], encoding: .utf8)!) {
         millisecondN = "\((millisecondN == (String(cString:[79,0], encoding: .utf8)!) ? millisecondN.count : millisecondN.count))"
         break
      }
      delay9.append("\(delay9.count)")
   return update_2lU

}





    func setupConstraints() {
       var for_zr: Float = 0.0
    var cropped9: String! = String(cString: [101,115,116,105,109,97,116,101,0], encoding: .utf8)!
      for_zr /= Swift.max(1, (Float(Int(for_zr > 241665604.0 || for_zr < -241665604.0 ? 41.0 : for_zr) % 2)))

        bgView.snp.makeConstraints { make in
       var costl: Float = 2.0
       var localez: Double = 3.0
      withUnsafeMutablePointer(to: &localez) { pointer in
    
      }
      for _ in 0 ..< 3 {
         costl -= Float(1)
      }
      if (localez * Double(costl)) == 3.88 || 4.60 == (3.88 * costl) {
          var launchr: Double = 5.0
          _ = launchr
          var amount8: String! = String(cString: [105,109,112,111,114,116,101,100,0], encoding: .utf8)!
          var roomG: Bool = false
          var controlL: String! = String(cString: [114,101,105,115,115,117,101,0], encoding: .utf8)!
         localez /= Swift.max((Double(controlL.count >> (Swift.min(5, labs(Int(costl > 338057849.0 || costl < -338057849.0 ? 61.0 : costl)))))), 4)
         launchr += (Double(Int(launchr > 348657600.0 || launchr < -348657600.0 ? 64.0 : launchr) / (Swift.max(amount8.count, 3))))
         amount8 = "\((3 | Int(costl > 160631887.0 || costl < -160631887.0 ? 48.0 : costl)))"
         roomG = Float(controlL.count) == costl
      }
      if (2.67 / (Swift.max(10, costl))) < 1.46 {
          var daysr: Int = 5
          _ = daysr
          var purchasew: Bool = true
          var descriptor6: Int = 1
          _ = descriptor6
         costl /= Swift.max((Float(Int(costl > 380838597.0 || costl < -380838597.0 ? 88.0 : costl) ^ (purchasew ? 5 : 5))), 3)
         daysr |= (daysr / (Swift.max(10, Int(costl > 338673337.0 || costl < -338673337.0 ? 38.0 : costl))))
         descriptor6 += 3 >> (Swift.min(labs(descriptor6), 4))
      }
          var toolq: String! = String(cString: [116,104,117,109,98,110,97,105,108,0], encoding: .utf8)!
         localez += (Double(Int(localez > 20147362.0 || localez < -20147362.0 ? 40.0 : localez) * 1))
         toolq.append("\((Int(localez > 359861000.0 || localez < -359861000.0 ? 60.0 : localez) * 3))")
          var zinitialsq: String! = String(cString: [98,102,115,116,109,0], encoding: .utf8)!
          var achiet: Float = 3.0
         localez /= Swift.max(1, Double(zinitialsq.count))
         achiet -= (Float(Int(costl > 252219204.0 || costl < -252219204.0 ? 23.0 : costl)))
      while (5.5 > (Double(Int(costl) / 2))) {
         localez *= (Double(Int(costl > 133963147.0 || costl < -133963147.0 ? 94.0 : costl)))
         break
      }
      cropped9 = "\(cropped9.count >> (Swift.min(labs(2), 4)))"
            make.edges.equalToSuperview()
        }

        signInButton.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.height.equalTo(Layout.buttonHeight)
            make.bottom.equalTo(view.safeAreaLayoutGuide).inset(Layout.bottomInset)
        }

        createAccountButton.snp.makeConstraints { make in
            make.leading.trailing.height.equalTo(signInButton)
            make.bottom.equalTo(signInButton.snp.top).offset(-Layout.buttonSpacing)
        }

        appleSignInButton.snp.makeConstraints { make in
            make.leading.trailing.height.equalTo(signInButton)
            make.bottom.equalTo(createAccountButton.snp.top).offset(-Layout.buttonSpacing)
        }
    }

    func setupEvents() {
       var dest9: String! = String(cString: [101,120,97,99,116,0], encoding: .utf8)!
    _ = dest9
   repeat {
      dest9 = "\(((String(cString:[56,0], encoding: .utf8)!) == dest9 ? dest9.count : dest9.count))"
      if (String(cString:[99,113,116,0], encoding: .utf8)!) == dest9 {
         break
      }
   } while (!dest9.contains("\(dest9.count)")) && ((String(cString:[99,113,116,0], encoding: .utf8)!) == dest9)

        appleSignInButton.addTarget(self, action: #selector(onAppleSignInTapped), for: .touchUpInside)
        createAccountButton.addTarget(self, action: #selector(onCreateAccountTapped), for: .touchUpInside)
        signInButton.addTarget(self, action: #selector(onSignInTapped), for: .touchUpInside)
    }

    @objc private func onAppleSignInTapped() {
       var using_g47: Float = 2.0
   withUnsafeMutablePointer(to: &using_g47) { pointer in
    
   }
      using_g47 += (Float(Int(using_g47 > 259341534.0 || using_g47 < -259341534.0 ? 51.0 : using_g47) / (Swift.max(Int(using_g47 > 290170327.0 || using_g47 < -290170327.0 ? 29.0 : using_g47), 7))))

        let y_center = ASAuthorizationAppleIDProvider()
        let index = y_center.createRequest()
        index.requestedScopes = [.fullName, .email]

        let createController = ASAuthorizationController(authorizationRequests: [index])
        createController.delegate = self
        createController.presentationContextProvider = self
        appleAuthController = createController
        createController.performRequests()
    }

@discardableResult
 func rootDeadlineFoundWorkday(overlapPanel: Bool, partsViewer: Double) -> Float {
    var screent: Bool = true
    var frame_p0w: String! = String(cString: [98,105,111,0], encoding: .utf8)!
    var largev: Float = 4.0
      largev -= Float(3)
   if screent {
      frame_p0w = "\((3 - (screent ? 5 : 2)))"
   }
   return largev

}





    private func handleAppleAuthorization(_ credential: ASAuthorizationAppleIDCredential) {
       var filee: String! = String(cString: [97,108,103,111,114,105,116,104,109,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &filee) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var fanw: Int = 5
          var languagex: String! = String(cString: [113,116,97,98,108,101,115,0], encoding: .utf8)!
         fanw += 1
         languagex = "\(2 | languagex.count)"
       var cropped_: String! = String(cString: [121,98,121,114,0], encoding: .utf8)!
       _ = cropped_
       var migraten: String! = String(cString: [117,99,104,97,114,0], encoding: .utf8)!
      if migraten == cropped_ {
         cropped_ = "\(2 >> (Swift.min(2, labs(fanw))))"
      }
      filee = "\(3)"
      if (String(cString:[99,120,105,0], encoding: .utf8)!) == filee {
         break
      }
   } while (filee.count < 2) && ((String(cString:[99,120,105,0], encoding: .utf8)!) == filee)

        let mutual = credential.user
        let random = Self.displayName(from: credential.fullName)
        let fallback = credential.email
        let progressImage = Self.makeAvatarImage(from: random ?? "")

        guard EPRankMenu.shared.signInWithApple(
            appleUserId: mutual,
            displayName: random,
            email: fallback,
            avatarImage: progressImage
        ) else {
            view.makeToast(cornerMapMail([80,106,100,109,35,106,109,35,116,106,119,107,35,66,115,115,111,102,35,101,98,106,111,102,103,45,35,83,111,102,98,112,102,35,119,113,122,35,98,100,98,106,109,45,3],0x3,false))
            return
        }
        EPRankMenu.shared.switchToMainInterface()
    }

@discardableResult
static func reuseAlphaSign(viewRequest: Int) -> Int {
    var description_8cG: String! = String(cString: [115,99,104,101,109,97,0], encoding: .utf8)!
    var pickS: String! = String(cString: [98,117,102,102,101,114,113,117,101,117,101,0], encoding: .utf8)!
    _ = pickS
    var string7: Int = 1
      string7 -= description_8cG.count
   for _ in 0 ..< 3 {
       var areay: String! = String(cString: [120,100,97,105,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &areay) { pointer in
    
      }
      while (areay.count >= 3) {
          var resourcev: [Any]! = [String(cString: [111,102,102,101,114,101,100,0], encoding: .utf8)!]
          _ = resourcev
          var didO: Int = 4
         withUnsafeMutablePointer(to: &didO) { pointer in
                _ = pointer.pointee
         }
          var memberA: Double = 3.0
          var avatarse: String! = String(cString: [115,121,109,108,105,110,107,0], encoding: .utf8)!
          _ = avatarse
         areay = "\(didO)"
         resourcev = [(areay == (String(cString:[89,0], encoding: .utf8)!) ? didO : areay.count)]
         memberA /= Swift.max(4, Double(1 + didO))
         avatarse.append("\(3)")
         break
      }
       var front_: Bool = false
       var closel: Bool = true
       _ = closel
      for _ in 0 ..< 1 {
          var frame_y85: String! = String(cString: [117,105,110,116,112,111,119,0], encoding: .utf8)!
          _ = frame_y85
          var numberv: Bool = true
         front_ = frame_y85 == areay
         numberv = (closel ? !front_ : !closel)
      }
      pickS = "\(pickS.count)"
   }
      pickS.append("\(pickS.count)")
   return string7

}





    private static func displayName(from components: PersonNameComponents?) -> String? {
       var columnm: Double = 4.0
   repeat {
      columnm /= Swift.max((Double(Int(columnm > 229150600.0 || columnm < -229150600.0 ? 74.0 : columnm))), 4)
      if columnm == 1980629.0 {
         break
      }
   } while (1.6 == (columnm / (Swift.max(2, columnm)))) && (columnm == 1980629.0)

        guard let components else { return nil }
        let week = PersonNameComponentsFormatter()
        let reuse = week.string(from: components)
            .trimmingCharacters(in: .whitespacesAndNewlines)
        return reuse.isEmpty ? nil : reuse
    }

@discardableResult
static func dictionaryResistanceWillChallenge(remainingProfile: String!, roomRemain: Int, flagCaption: Int) -> [String: Any]! {
    var interval_xfV: [String: Any]! = [String(cString: [98,111,117,110,100,97,108,108,0], encoding: .utf8)!:4, String(cString: [97,99,116,105,118,97,116,105,111,110,115,0], encoding: .utf8)!:85, String(cString: [109,101,109,122,101,114,111,0], encoding: .utf8)!:59]
    var pickedL: [Any]! = [String(cString: [116,105,103,103,108,101,0], encoding: .utf8)!, String(cString: [109,105,114,114,111,114,0], encoding: .utf8)!, String(cString: [98,117,99,107,101,116,115,0], encoding: .utf8)!]
    var unreadE: [String: Any]! = [String(cString: [112,97,103,101,114,0], encoding: .utf8)!:82, String(cString: [111,118,101,114,108,97,112,0], encoding: .utf8)!:36, String(cString: [97,103,101,110,116,115,0], encoding: .utf8)!:55]
   withUnsafeMutablePointer(to: &unreadE) { pointer in
          _ = pointer.pointee
   }
      interval_xfV["\(pickedL.count)"] = interval_xfV.values.count % (Swift.max(10, pickedL.count))
   if 2 >= (4 ^ pickedL.count) && (unreadE.keys.count ^ 4) >= 1 {
       var pendinga: Double = 0.0
       var weekz: Float = 0.0
       var taby: String! = String(cString: [99,97,108,108,108,0], encoding: .utf8)!
       var saveda: Double = 1.0
       var collectionP: String! = String(cString: [112,97,114,116,105,99,105,112,97,110,116,115,0], encoding: .utf8)!
       _ = collectionP
         pendinga -= (Double(Int(weekz > 13100068.0 || weekz < -13100068.0 ? 29.0 : weekz)))
      for _ in 0 ..< 1 {
         pendinga -= Double(1)
      }
         saveda /= Swift.max(1, (Double(Int(pendinga > 344330270.0 || pendinga < -344330270.0 ? 53.0 : pendinga) >> (Swift.min(1, labs(2))))))
       var minute8: Bool = true
       var aspectB: Bool = false
       var track0: String! = String(cString: [99,111,110,102,105,103,117,114,101,0], encoding: .utf8)!
       var fileK: String! = String(cString: [118,112,100,120,0], encoding: .utf8)!
       var legendt: String! = String(cString: [99,111,117,110,116,0], encoding: .utf8)!
       var z_heightK: String! = String(cString: [113,112,113,115,99,97,108,101,0], encoding: .utf8)!
       _ = z_heightK
      for _ in 0 ..< 3 {
          var badges3: [Any]! = [String(cString: [100,101,99,111,114,97,116,105,111,110,115,0], encoding: .utf8)!, String(cString: [98,121,112,97,115,115,105,110,103,0], encoding: .utf8)!]
          var nanosecond6: String! = String(cString: [100,101,99,111,114,97,116,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &nanosecond6) { pointer in
    
         }
          var modelr: Double = 1.0
         legendt.append("\((2 / (Swift.max(6, Int(saveda > 365509948.0 || saveda < -365509948.0 ? 14.0 : saveda)))))")
         badges3 = [track0.count]
         nanosecond6.append("\((legendt.count * (minute8 ? 2 : 1)))")
         modelr += (Double((aspectB ? 5 : 2) | 3))
      }
         aspectB = 63.82 < saveda
          var configI: String! = String(cString: [111,102,102,115,0], encoding: .utf8)!
          _ = configI
         pendinga /= Swift.max(Double(legendt.count - 2), 2)
         configI.append("\(z_heightK.count)")
       var animateO: [Any]! = [51, 81, 89]
      withUnsafeMutablePointer(to: &animateO) { pointer in
    
      }
      repeat {
         pendinga -= (Double(Int(saveda > 318157181.0 || saveda < -318157181.0 ? 45.0 : saveda) & legendt.count))
         if 3444847.0 == pendinga {
            break
         }
      } while (3444847.0 == pendinga) && (3.83 < (pendinga - Double(animateO.count)) && 4.46 < (3.83 - pendinga))
         z_heightK = "\(z_heightK.count % (Swift.max(2, 10)))"
         taby.append("\(track0.count - 3)")
      repeat {
          var presentN: [String: Any]! = [String(cString: [115,105,122,101,100,0], encoding: .utf8)!:72, String(cString: [100,101,112,108,111,121,109,101,110,116,0], encoding: .utf8)!:1]
          var firstj: String! = String(cString: [112,114,111,109,112,101,103,0], encoding: .utf8)!
          var rows5: Double = 1.0
          var controlo: Double = 5.0
         collectionP.append("\((Int(saveda > 162984237.0 || saveda < -162984237.0 ? 52.0 : saveda) % (Swift.max(animateO.count, 2))))")
         presentN = [fileK: (z_heightK == (String(cString:[57,0], encoding: .utf8)!) ? fileK.count : z_heightK.count)]
         firstj.append("\(((String(cString:[95,0], encoding: .utf8)!) == z_heightK ? firstj.count : z_heightK.count))")
         rows5 -= Double(1 - firstj.count)
         controlo /= Swift.max(Double(2 * animateO.count), 4)
         if (String(cString:[103,120,111,97,51,101,102,122,122,0], encoding: .utf8)!) == collectionP {
            break
         }
      } while (saveda >= 4.3) && ((String(cString:[103,120,111,97,51,101,102,122,122,0], encoding: .utf8)!) == collectionP)
      for _ in 0 ..< 2 {
         z_heightK = "\(fileK.count)"
      }
      pickedL.append(3 | unreadE.count)
   }
   while (2 < pickedL.count) {
      interval_xfV = ["\(interval_xfV.values.count)": pickedL.count << (Swift.min(4, interval_xfV.count))]
      break
   }
   return interval_xfV

}





    
    private static func makeAvatarImage(from displayName: String, size: CGFloat = 256) -> UIImage {
       var systemr: [Any]! = [10, 37]
   while (!systemr.contains { $0 as? Int == systemr.count }) {
      systemr = [systemr.count]
      break
   }

        let normalized = displayName.trimmingCharacters(in: .whitespacesAndNewlines)
        let field = normalized.isEmpty ? cornerMapMail([-124,-75,-75,-87,-96,-27,-112,-74,-96,-73,-59],0xC5,false) : normalized
        let dest = field.split(separator: cornerMapMail([-32,-64],0xC0,false)).filter { !$0.isEmpty }
        let page: String
        if dest.isEmpty {
            page = cornerMapMail([-37,-102],0x9A,false)
        } else if dest.count == 1 {
            page = String(dest[0].prefix(1)).uppercased()
        } else {
            page = String(dest[0].prefix(1) + dest[1].prefix(1)).uppercased()
        }

        let locale = UIGraphicsImageRenderer(size: CGSize(width: size, height: size))
        return locale.image { ctx in
            let changed = CGRect(origin: .zero, size: CGSize(width: size, height: size))
            let parts3 = ctx.cgContext
            parts3.setFillColor(red: 165 / 255, green: 145 / 255, blue: 242 / 255, alpha: 1)
            parts3.fillEllipse(in: changed)

            let request = UIFont.systemFont(ofSize: size * 0.36, weight: .semibold)
            let own: [NSAttributedString.Key: Any] = [
                .font: request,
                .foregroundColor: UIColor.white,
            ]
            let future = page as NSString
            let active = future.size(withAttributes: own)
            let oldh = CGPoint(
                x: (size - active.width) / 2,
                y: (size - active.height) / 2
            )
            future.draw(at: oldh, withAttributes: own)
        }
    }

    @objc private func onCreateAccountTapped() {
       var thresholdI: String! = String(cString: [116,111,116,97,108,108,121,0], encoding: .utf8)!
   repeat {
      thresholdI.append("\((thresholdI == (String(cString:[71,0], encoding: .utf8)!) ? thresholdI.count : thresholdI.count))")
      if thresholdI.count == 1049099 {
         break
      }
   } while (thresholdI.count == 1049099) && (thresholdI != String(cString:[85,0], encoding: .utf8)!)

        navigationController?.pushViewController(EPGroupController(mode: .create), animated: true)
    }

@discardableResult
 func backgroundKeyButtonQuarterEraCapture(usersPreview: String!, tapMember: Int) -> Double {
    var friendsQ: String! = String(cString: [105,114,114,101,108,118,97,110,116,0], encoding: .utf8)!
    var itemr: String! = String(cString: [102,105,108,116,101,114,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &itemr) { pointer in
          _ = pointer.pointee
   }
    var daysu: Double = 4.0
      friendsQ = "\(1)"
      friendsQ = "\(friendsQ.count)"
   repeat {
      daysu += (Double(Int(daysu > 322516495.0 || daysu < -322516495.0 ? 76.0 : daysu) % 2))
      if daysu == 1175814.0 {
         break
      }
   } while (itemr.contains("\(daysu)")) && (daysu == 1175814.0)
   return daysu

}





    @objc private func onSignInTapped() {
       var localizedr: Double = 3.0
    var commentsI: String! = String(cString: [98,97,99,107,115,108,97,115,104,0], encoding: .utf8)!
   while (4.48 == localizedr) {
      localizedr -= (Double(Int(localizedr > 330773791.0 || localizedr < -330773791.0 ? 4.0 : localizedr)))
      break
   }
      commentsI = "\((commentsI.count / (Swift.max(10, Int(localizedr > 159453432.0 || localizedr < -159453432.0 ? 54.0 : localizedr)))))"

        navigationController?.pushViewController(EPGroupController(mode: .signIn), animated: true)
    }

    private func makeLoginButton(title: String, systemImage: String? = nil) -> UIButton {
       var minutesm: Double = 3.0
    _ = minutesm
    var displayc: String! = String(cString: [97,98,101,108,0], encoding: .utf8)!
    _ = displayc
   if (Int(minutesm > 68172976.0 || minutesm < -68172976.0 ? 26.0 : minutesm) / (Swift.max(displayc.count, 10))) > 3 || (minutesm / (Swift.max(4.88, 4))) > 1.36 {
       var sharedf: [String: Any]! = [String(cString: [102,105,120,116,117,114,101,115,0], encoding: .utf8)!:94, String(cString: [118,99,101,110,99,0], encoding: .utf8)!:74, String(cString: [97,115,115,117,109,101,0], encoding: .utf8)!:77]
       _ = sharedf
       var blankM: String! = String(cString: [119,97,116,99,104,101,100,0], encoding: .utf8)!
       var deletedA: String! = String(cString: [119,105,116,104,0], encoding: .utf8)!
       var testH: String! = String(cString: [112,101,114,99,101,112,116,117,97,108,0], encoding: .utf8)!
      if testH.hasPrefix(deletedA) {
          var incrementb: String! = String(cString: [104,97,110,103,0], encoding: .utf8)!
          _ = incrementb
          var accessk: [String: Any]! = [String(cString: [117,116,105,108,115,0], encoding: .utf8)!:61, String(cString: [110,97,117,116,105,99,97,108,0], encoding: .utf8)!:62, String(cString: [115,101,99,111,110,100,0], encoding: .utf8)!:40]
          _ = accessk
          var tomorrowe: String! = String(cString: [114,101,102,105,110,101,109,101,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &tomorrowe) { pointer in
    
         }
          var codesS: Float = 4.0
          var picko: Int = 2
          _ = picko
         deletedA = "\((incrementb == (String(cString:[99,0], encoding: .utf8)!) ? incrementb.count : sharedf.keys.count))"
         accessk = ["\(sharedf.values.count)": 3 >> (Swift.min(2, tomorrowe.count))]
         tomorrowe = "\(1)"
         codesS /= Swift.max((Float(blankM == (String(cString:[73,0], encoding: .utf8)!) ? blankM.count : sharedf.keys.count)), 1)
         picko |= (Int(codesS > 387268215.0 || codesS < -387268215.0 ? 59.0 : codesS) % (Swift.max(7, accessk.values.count)))
      }
      while (!blankM.contains("\(deletedA.count)")) {
         blankM = "\(testH.count)"
         break
      }
         blankM.append("\(blankM.count)")
      while (sharedf.values.count > deletedA.count) {
         deletedA = "\(deletedA.count | 2)"
         break
      }
      if 2 < testH.count {
          var dateM: Bool = false
         withUnsafeMutablePointer(to: &dateM) { pointer in
    
         }
          var nick6: Float = 5.0
          _ = nick6
         testH.append("\(2)")
         dateM = !blankM.contains("\(dateM)")
         nick6 -= Float(testH.count % (Swift.max(3, 1)))
      }
         testH = "\(1 << (Swift.min(5, blankM.count)))"
      repeat {
          var overlapV: [Any]! = [87, 53]
          var mediar: Double = 5.0
          var extension_ys: String! = String(cString: [112,97,103,101,115,105,122,101,0], encoding: .utf8)!
          var remainX: String! = String(cString: [112,111,119,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &remainX) { pointer in
                _ = pointer.pointee
         }
         blankM = "\(sharedf.keys.count * 1)"
         overlapV = [(Int(mediar > 379376770.0 || mediar < -379376770.0 ? 75.0 : mediar))]
         mediar += (Double(1 << (Swift.min(labs(Int(mediar > 319520876.0 || mediar < -319520876.0 ? 82.0 : mediar)), 5))))
         extension_ys.append("\(3 | blankM.count)")
         remainX = "\(1 + testH.count)"
         if 4203837 == blankM.count {
            break
         }
      } while (4203837 == blankM.count) && (3 <= blankM.count)
       var int_uz: String! = String(cString: [99,117,100,97,115,99,97,108,101,0], encoding: .utf8)!
         testH = "\(((String(cString:[121,0], encoding: .utf8)!) == int_uz ? sharedf.keys.count : int_uz.count))"
         int_uz = "\(testH.count >> (Swift.min(labs(3), 4)))"
      while (testH.hasPrefix("\(sharedf.keys.count)")) {
          var existingr: [String: Any]! = [String(cString: [102,112,117,116,115,0], encoding: .utf8)!:String(cString: [115,111,99,107,101,116,118,97,114,0], encoding: .utf8)!, String(cString: [100,113,99,111,101,102,102,0], encoding: .utf8)!:String(cString: [99,111,117,110,116,101,114,115,0], encoding: .utf8)!]
          var cornerB: [String: Any]! = [String(cString: [112,101,110,99,105,108,0], encoding: .utf8)!:10, String(cString: [109,111,116,105,111,110,112,105,120,101,108,115,0], encoding: .utf8)!:30]
          _ = cornerB
         sharedf = ["\(sharedf.values.count)": sharedf.count]
         existingr[deletedA] = (deletedA == (String(cString:[57,0], encoding: .utf8)!) ? deletedA.count : cornerB.keys.count)
         cornerB = [blankM: blankM.count * deletedA.count]
         break
      }
         testH.append("\(int_uz.count)")
      displayc = "\(2)"
   }

      displayc.append("\((1 - Int(minutesm > 206410291.0 || minutesm < -206410291.0 ? 76.0 : minutesm)))")
        let outgoingButton = UIButton(type: .custom)
        var titles = UIButton.Configuration.filled()
        titles.baseBackgroundColor = .white
        titles.baseForegroundColor = .black
        titles.cornerStyle = .capsule
        titles.title = title
        titles.titleTextAttributesTransformer = UIConfigurationTextAttributesTransformer { incoming in
            var scale = incoming
            scale.font = .systemFont(ofSize: 20, weight: .semibold)
            return scale
        }
        if let systemImage {
            titles.image = UIImage(systemName: systemImage)
            titles.imagePadding = 8
            titles.preferredSymbolConfigurationForImage = UIImage.SymbolConfiguration(pointSize: 18, weight: .semibold)
        }
        outgoingButton.configuration = titles
        return outgoingButton
    }

    private lazy var appleSignInButton: UIButton = makeLoginButton(
        title: cornerMapMail([-115,-73,-71,-80,-2,-73,-80,-2,-87,-73,-86,-74,-2,-97,-82,-82,-78,-69,-34],0xDE,false),
        systemImage: cornerMapMail([14,31,31,3,10,65,3,0,8,0,111],0x6F,false)
    )

    private lazy var createAccountButton: UIButton = makeLoginButton(title: cornerMapMail([14,63,40,44,57,40,109,12,46,46,34,56,35,57,77],0x4D,false))

    private lazy var signInButton: UIButton = makeLoginButton(title: cornerMapMail([-95,-101,-107,-100,-46,-101,-100,-14],0xF2,false))

    private let bgView: UIImageView = {
       var dir5: Bool = false
    var emptyw: String! = String(cString: [114,101,99,111,103,110,105,116,105,111,110,0], encoding: .utf8)!
      emptyw = "\(((dir5 ? 2 : 4)))"

        let v = UIImageView()
      dir5 = !dir5 || dir5
        v.contentMode = .scaleAspectFill
        v.image = cornerMapMail([62,44,37,22,43,46,73],0x49,false).toImage
        v.isUserInteractionEnabled = false
        return v
    }()
}


extension EPReportController: ASAuthorizationControllerDelegate, ASAuthorizationControllerPresentationContextProviding {

@discardableResult
 func originalTodayFileLayerGoal(badgesEmpty: Double, launchCosplay: Int) -> String! {
    var appleH: Double = 2.0
    var threeS: String! = String(cString: [116,114,97,105,108,101,114,0], encoding: .utf8)!
    var idsj: String! = String(cString: [116,114,105,112,0], encoding: .utf8)!
      threeS.append("\((idsj == (String(cString:[120,0], encoding: .utf8)!) ? Int(appleH > 14142914.0 || appleH < -14142914.0 ? 90.0 : appleH) : idsj.count))")
   for _ in 0 ..< 2 {
      appleH /= Swift.max((Double(Int(appleH > 123322688.0 || appleH < -123322688.0 ? 51.0 : appleH) >> (Swift.min(labs(2), 3)))), 4)
   }
      appleH -= (Double((String(cString:[75,0], encoding: .utf8)!) == threeS ? idsj.count : threeS.count))
   return threeS

}






    func authorizationController(
        controller: ASAuthorizationController,
        didCompleteWithError error: Error
    ) {
       var futureT: [Any]! = [36, 27]
   withUnsafeMutablePointer(to: &futureT) { pointer in
          _ = pointer.pointee
   }
      futureT.append(futureT.count)

        appleAuthController = nil
        let allowed = error as NSError
        if allowed.domain == ASAuthorizationError.errorDomain,
           allowed.code == ASAuthorizationError.canceled.rawValue {
            return
        }
        view.makeToast(cornerMapMail([-73,-115,-125,-118,-60,-115,-118,-60,-109,-115,-112,-116,-60,-91,-108,-108,-120,-127,-60,-109,-123,-105,-60,-121,-123,-118,-121,-127,-120,-120,-127,-128,-60,-117,-106,-60,-126,-123,-115,-120,-127,-128,-54,-28],0xE4,false))
    }

@discardableResult
 func ownerItemOriginVisibility() -> Bool {
    var int_x4I: String! = String(cString: [114,101,115,105,122,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &int_x4I) { pointer in
          _ = pointer.pointee
   }
    var setupT: String! = String(cString: [97,99,107,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &setupT) { pointer in
          _ = pointer.pointee
   }
    var likedE: Bool = false
   repeat {
      likedE = 15 == setupT.count || int_x4I.count == 15
      if likedE ? !likedE : likedE {
         break
      }
   } while (4 <= int_x4I.count && likedE) && (likedE ? !likedE : likedE)
      likedE = int_x4I.count > 29 || !likedE
   while (int_x4I.count == setupT.count) {
       var folderr: Int = 5
      withUnsafeMutablePointer(to: &folderr) { pointer in
    
      }
       var overlapU: String! = String(cString: [97,118,115,116,114,105,110,103,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var marky: String! = String(cString: [108,105,110,107,105,110,103,0], encoding: .utf8)!
          var closej: String! = String(cString: [97,109,111,117,110,116,0], encoding: .utf8)!
          var coverE: Int = 3
          var challenges: Double = 0.0
         overlapU = "\(2)"
         marky.append("\(coverE | folderr)")
         closej.append("\(closej.count >> (Swift.min(labs(1), 1)))")
         coverE ^= coverE
         challenges -= Double(3)
      }
      while ((overlapU.count * folderr) >= 3) {
          var monthsL: String! = String(cString: [115,105,110,113,105,0], encoding: .utf8)!
          _ = monthsL
          var cosb: Double = 5.0
         overlapU.append("\(folderr ^ 2)")
         monthsL = "\(1)"
         cosb -= Double(monthsL.count << (Swift.min(2, overlapU.count)))
         break
      }
       var huaW: Bool = false
       var insufficientI: Bool = true
      repeat {
         huaW = 88 == folderr && !huaW
         if huaW ? !huaW : huaW {
            break
         }
      } while (huaW ? !huaW : huaW) && (huaW)
         overlapU.append("\(folderr ^ 1)")
      repeat {
         huaW = overlapU.count >= 19
         if huaW ? !huaW : huaW {
            break
         }
      } while (folderr == 1) && (huaW ? !huaW : huaW)
         insufficientI = folderr <= 72
      int_x4I = "\(1)"
      break
   }
   return likedE

}






    func presentationAnchor(for controller: ASAuthorizationController) -> ASPresentationAnchor {
       var pricef: Float = 5.0
   repeat {
      pricef += (Float(Int(pricef > 250811421.0 || pricef < -250811421.0 ? 1.0 : pricef) / (Swift.max(Int(pricef > 249331679.0 || pricef < -249331679.0 ? 6.0 : pricef), 7))))
      if 3510166.0 == pricef {
         break
      }
   } while (pricef > pricef) && (3510166.0 == pricef)

return         view.window ?? UIApplication.window ?? UIWindow()
    }


    func authorizationController(
        controller: ASAuthorizationController,
        didCompleteWithAuthorization authorization: ASAuthorization
    ) {
       var loggedC: String! = String(cString: [106,117,115,116,105,102,105,101,100,0], encoding: .utf8)!
    var allowedT: String! = String(cString: [100,105,115,99,97,114,100,101,100,0], encoding: .utf8)!
   repeat {
      loggedC = "\(2)"
      if loggedC == (String(cString:[107,103,53,120,57,54,113,115,0], encoding: .utf8)!) {
         break
      }
   } while (loggedC.count == allowedT.count) && (loggedC == (String(cString:[107,103,53,120,57,54,113,115,0], encoding: .utf8)!))

   if 4 >= loggedC.count {
       var sharedA: Bool = true
       _ = sharedA
       var beforei: String! = String(cString: [97,108,101,114,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &beforei) { pointer in
    
      }
         beforei.append("\(((sharedA ? 3 : 1) % (Swift.max(beforei.count, 4))))")
          var nanox: Double = 2.0
          var optionsW: Double = 0.0
          var blockedF: String! = String(cString: [102,105,110,97,108,105,122,101,114,0], encoding: .utf8)!
         sharedA = blockedF == (String(cString:[122,0], encoding: .utf8)!)
         nanox += (Double(blockedF == (String(cString:[119,0], encoding: .utf8)!) ? blockedF.count : Int(nanox > 148667846.0 || nanox < -148667846.0 ? 7.0 : nanox)))
         optionsW /= Swift.max(2, (Double(Int(optionsW > 234908859.0 || optionsW < -234908859.0 ? 46.0 : optionsW))))
       var provider8: String! = String(cString: [104,105,115,116,111,114,105,101,115,0], encoding: .utf8)!
         provider8 = "\(provider8.count & beforei.count)"
         provider8.append("\(3)")
       var applyJ: [String: Any]! = [String(cString: [99,111,100,101,0], encoding: .utf8)!:89, String(cString: [102,119,114,105,116,101,0], encoding: .utf8)!:47]
       _ = applyJ
         applyJ = ["\(applyJ.values.count)": applyJ.values.count & 2]
      loggedC.append("\(beforei.count)")
   }
        appleAuthController = nil
        guard let credential = authorization.credential as? ASAuthorizationAppleIDCredential else {
            view.makeToast(cornerMapMail([54,13,2,1,15,6,67,23,12,67,17,6,2,7,67,34,19,19,15,6,67,42,39,67,0,17,6,7,6,13,23,10,2,15,16,77,99],0x63,false))
      allowedT = "\(allowedT.count ^ 3)"
            return
        }
        handleAppleAuthorization(credential)
    }
}
