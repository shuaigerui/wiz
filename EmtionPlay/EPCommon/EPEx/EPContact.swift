
import Foundation

import UIKit

enum SS_UserAvatarMedia {

    private static let folderName = cornerMapMail([98,68,82,69,118,65,86,67,86,69,68,55],0x37,false)

    static func mediaDirectory() -> URL {
       var mappedQ: [Any]! = [28, 72, 44]
   if (mappedQ.count << (Swift.min(labs(3), 5))) < 3 || (3 << (Swift.min(4, mappedQ.count))) < 5 {
      mappedQ.append(3)
   }

return         FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]
            .appendingPathComponent(folderName, isDirectory: true)
    }

    private static func ensureDirectory() {
       var backB: Int = 0
    var paddingR: [Any]! = [84, 34, 73]
   withUnsafeMutablePointer(to: &paddingR) { pointer in
          _ = pointer.pointee
   }
   while (3 > (2 / (Swift.max(8, backB)))) {
      paddingR.append(1)
      break
   }

   repeat {
       var emptyf: [Any]! = [80, 70, 32]
       var n_badgeq: Int = 4
      repeat {
          var previewp: Double = 1.0
          var placeholder2: [String: Any]! = [String(cString: [122,101,114,111,101,115,0], encoding: .utf8)!:57, String(cString: [98,111,114,100,101,114,108,101,115,115,0], encoding: .utf8)!:83, String(cString: [117,110,102,111,99,117,115,101,100,0], encoding: .utf8)!:44]
         withUnsafeMutablePointer(to: &placeholder2) { pointer in
                _ = pointer.pointee
         }
         emptyf = [emptyf.count << (Swift.min(labs(3), 5))]
         previewp -= Double(placeholder2.count ^ emptyf.count)
         placeholder2["\(previewp)"] = (Int(previewp > 148425626.0 || previewp < -148425626.0 ? 15.0 : previewp) + placeholder2.keys.count)
         if emptyf.count == 307467 {
            break
         }
      } while (emptyf.count == 307467) && (n_badgeq > emptyf.count)
      while ((emptyf.count / (Swift.max(4, 2))) > 4) {
         n_badgeq >>= Swift.min(5, labs(n_badgeq + emptyf.count))
         break
      }
         emptyf.append(n_badgeq * 3)
         emptyf = [3 / (Swift.max(10, n_badgeq))]
         n_badgeq += emptyf.count
      for _ in 0 ..< 2 {
         n_badgeq ^= n_badgeq
      }
      backB -= 2
      if 4837562 == backB {
         break
      }
   } while (5 == (backB + paddingR.count) && (paddingR.count + backB) == 5) && (4837562 == backB)
        let back = mediaDirectory()
        try? FileManager.default.createDirectory(at: back, withIntermediateDirectories: true)
    }

    
    static func saveAvatar(_ image: UIImage, userId: Int64) -> String? {
       var urlx: String! = String(cString: [114,111,116,97,116,105,110,103,0], encoding: .utf8)!
    _ = urlx
   repeat {
      urlx.append("\(2)")
      if 1246997 == urlx.count {
         break
      }
   } while (urlx == urlx) && (1246997 == urlx.count)

return         saveAvatar(image, userId: "\(userId)")
    }

    
    static func saveAvatar(_ image: UIImage, userId: String) -> String? {
       var horizontalg: String! = String(cString: [105,110,116,101,114,115,101,99,116,105,110,103,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      horizontalg.append("\(horizontalg.count)")
   }

        ensureDirectory()
        guard let data = image.jpegData(compressionQuality: 0.88) else { return nil }
        let renderer = "\(cornerMapMail([-8,-17,-58,-103],0x99,false))\(userId)"
        let nanosecond = mediaDirectory().appendingPathComponent("\(renderer).jpg")
        do {
            try data.write(to: nanosecond, options: .atomic)
            return renderer
        } catch {
            return nil
        }
    }

    
    static func removeSavedAvatarIfPresent(userId: Int64) {
       var followingi: Float = 2.0
   repeat {
       var sheetZ: String! = String(cString: [108,111,110,103,109,117,108,97,119,0], encoding: .utf8)!
       var push1: Bool = false
       var permissionj: String! = String(cString: [111,117,116,112,117,116,0], encoding: .utf8)!
       var response9: Double = 1.0
      repeat {
         permissionj.append("\(2)")
         if permissionj == (String(cString:[108,122,100,118,100,106,0], encoding: .utf8)!) {
            break
         }
      } while (!push1) && (permissionj == (String(cString:[108,122,100,118,100,106,0], encoding: .utf8)!))
      if !push1 || 5 < permissionj.count {
          var totala: String! = String(cString: [112,97,105,110,116,105,110,103,0], encoding: .utf8)!
          var sessionl: Bool = true
          _ = sessionl
          var task9: Bool = false
          var emptyb: [Any]! = [String(cString: [105,103,104,108,105,103,104,116,115,0], encoding: .utf8)!, String(cString: [100,114,97,103,103,97,98,108,101,0], encoding: .utf8)!]
          var yearsH: Bool = true
         permissionj.append("\(3)")
         totala.append("\(3)")
         sessionl = (21 <= (permissionj.count | (!sessionl ? permissionj.count : 21)))
         task9 = totala == (String(cString:[76,0], encoding: .utf8)!)
         emptyb.append((permissionj.count + (yearsH ? 3 : 4)))
      }
      while (5 <= sheetZ.count || push1) {
         push1 = sheetZ.count > 50
         break
      }
         push1 = (((push1 ? permissionj.count : 51) * permissionj.count) >= 51)
         sheetZ = "\((Int(response9 > 258145241.0 || response9 < -258145241.0 ? 44.0 : response9) | permissionj.count))"
         sheetZ = "\(2)"
         permissionj.append("\(sheetZ.count)")
          var placeholderA: Double = 1.0
          var devicer: String! = String(cString: [115,117,98,109,105,116,116,101,100,0], encoding: .utf8)!
         permissionj = "\((Int(placeholderA > 44494372.0 || placeholderA < -44494372.0 ? 48.0 : placeholderA) * (push1 ? 4 : 3)))"
         devicer = "\((Int(response9 > 37148896.0 || response9 < -37148896.0 ? 39.0 : response9)))"
      if response9 < 5.99 {
         response9 /= Swift.max(3, Double(3))
      }
      repeat {
         sheetZ = "\(3)"
         if 4317466 == sheetZ.count {
            break
         }
      } while (4317466 == sheetZ.count) && (1.13 <= (response9 / (Swift.max(Double(sheetZ.count), 8))) || (sheetZ.count ^ 2) <= 4)
       var deletedK: [String: Any]! = [String(cString: [99,111,109,112,111,110,101,110,116,0], encoding: .utf8)!:69, String(cString: [104,117,102,102,121,117,118,101,110,99,100,115,112,0], encoding: .utf8)!:45, String(cString: [97,108,116,101,114,101,100,0], encoding: .utf8)!:6]
       var stats_: [String: Any]! = [String(cString: [102,105,108,101,114,101,97,100,115,116,114,101,97,109,0], encoding: .utf8)!:89.0]
      if push1 {
         sheetZ = "\(permissionj.count)"
      }
         deletedK["\(response9)"] = ((push1 ? 1 : 2) & Int(response9 > 342878051.0 || response9 < -342878051.0 ? 21.0 : response9))
         stats_[permissionj] = sheetZ.count
      followingi -= Float(sheetZ.count & 1)
      if followingi == 1517633.0 {
         break
      }
   } while (followingi == 1517633.0) && (2.26 <= (followingi + 2.64))

        removeSavedAvatarIfPresent(userId: "\(userId)")
    }

    static func removeSavedAvatarIfPresent(userId: String) {
       var emptyz: String! = String(cString: [110,101,116,101,113,0], encoding: .utf8)!
    var ratiox: [String: Any]! = [String(cString: [115,113,108,105,116,101,0], encoding: .utf8)!:26, String(cString: [105,115,114,101,97,100,111,110,108,121,0], encoding: .utf8)!:40, String(cString: [105,100,102,118,0], encoding: .utf8)!:32]
      ratiox = ["\(ratiox.count)": (emptyz == (String(cString:[56,0], encoding: .utf8)!) ? emptyz.count : ratiox.keys.count)]

      ratiox = ["\(ratiox.values.count)": emptyz.count | 2]
        let renderer = "\(cornerMapMail([-8,-17,-58,-103],0x99,false))\(userId)"
        guard let url = imageFileURL(baseName: renderer) else { return }
        try? FileManager.default.removeItem(at: url)
    }

    static func imageFileURL(baseName: String) -> URL? {
       var ensureo: Bool = false
   repeat {
      ensureo = (ensureo ? !ensureo : !ensureo)
      if ensureo ? !ensureo : ensureo {
         break
      }
   } while (ensureo) && (ensureo ? !ensureo : ensureo)

        for ext in [cornerMapMail([-69,-95,-74,-47],0xD1,false), cornerMapMail([126,100,113,115,20],0x14,false), cornerMapMail([17,15,6,97],0x61,false)] {
            let shop = mediaDirectory().appendingPathComponent("\(baseName).\(ext)")
            if FileManager.default.fileExists(atPath: shop.path) { return shop }
        }
        return nil
    }

    static func uiImage(baseName: String) -> UIImage? {
       var currento: String! = String(cString: [116,104,101,0], encoding: .utf8)!
      currento = "\(currento.count)"

        guard let url = imageFileURL(baseName: baseName) else { return nil }
        return UIImage(contentsOfFile: url.path)
    }
}
