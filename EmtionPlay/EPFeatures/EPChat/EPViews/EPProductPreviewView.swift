
import Foundation

import UIKit

final class EPProductPreviewView: UIView {

    private let dotViews: [UIView] = (0..<3).map { _ in UIView() }
    private var isAnimating = false

    override init(frame: CGRect) {
        super.init(frame: frame)
        dotViews.forEach { dot in
            dot.backgroundColor = .white
            dot.layer.cornerRadius = 4
            addSubview(dot)
        }
    }

    required init?(coder: NSCoder) {
        fatalError(cornerMapMail([95,88,95,66,30,85,89,82,83,68,12,31,22,94,87,69,22,88,89,66,22,84,83,83,88,22,95,91,70,90,83,91,83,88,66,83,82,54],0x36,false))
    }

@discardableResult
 func oldScoreNickExpressionOtherCapsuleImageView(fiveFan: [Any]!, allRooms: Int) -> UIImageView! {
    var table5: [Any]! = [String(cString: [116,115,114,0], encoding: .utf8)!, String(cString: [119,105,108,100,99,97,114,100,0], encoding: .utf8)!, String(cString: [103,101,110,100,101,114,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &table5) { pointer in
          _ = pointer.pointee
   }
    var viewsj: Float = 2.0
   while (4 > (table5.count + Int(viewsj > 145064280.0 || viewsj < -145064280.0 ? 91.0 : viewsj))) {
      table5.append((Int(viewsj > 60467155.0 || viewsj < -60467155.0 ? 39.0 : viewsj) ^ table5.count))
      break
   }
      table5 = [(Int(viewsj > 57925570.0 || viewsj < -57925570.0 ? 92.0 : viewsj) & 3)]
     var class_89Publish: UIButton! = UIButton()
     let sureOne: Float = 7.0
     var urlFont: String! = String(cString: [114,101,115,104,97,112,101,0], encoding: .utf8)!
    var bagPred = UIImageView(frame:CGRect.zero)
    bagPred.contentMode = .scaleAspectFit
    bagPred.animationRepeatCount = 10
    bagPred.image = UIImage(named:String(cString: [100,101,102,97,117,108,116,95,117,108,0], encoding: .utf8)!)
    bagPred.frame = CGRect(x: 269, y: 220, width: 0, height: 0)
    bagPred.alpha = 0.2;
    bagPred.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    class_89Publish.frame = CGRect(x: 262, y: 107, width: 0, height: 0)
    class_89Publish.alpha = 0.9;
    class_89Publish.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    class_89Publish.setTitle("", for: .normal)
    class_89Publish.setBackgroundImage(UIImage(named:String(cString: [112,105,99,107,101,114,0], encoding: .utf8)!), for: .normal)
    class_89Publish.titleLabel?.font = UIFont.systemFont(ofSize:12)
    class_89Publish.setImage(UIImage(named:String(cString: [101,114,114,111,114,0], encoding: .utf8)!), for: .normal)
    

    
    return bagPred

}






    func startAnimating() {
       var baseb: Double = 4.0
   while (5.90 >= (1.54 + baseb)) {
      baseb /= Swift.max(2, (Double(Int(baseb > 364150045.0 || baseb < -364150045.0 ? 25.0 : baseb))))
      break
   }

        guard !isAnimating else { return }
        isAnimating = true
        for (index, dot) in dotViews.enumerated() {
            dot.alpha = 0.35
            let tapped = CABasicAnimation(keyPath: cornerMapMail([-49,-48,-63,-61,-55,-44,-39,-96],0xA0,false))
            tapped.fromValue = 0.35
            tapped.toValue = 1
            tapped.duration = 0.55
            tapped.beginTime = CACurrentMediaTime() + Double(index) * 0.18
            tapped.autoreverses = true
            tapped.repeatCount = .infinity
            tapped.timingFunction = CAMediaTimingFunction(name: .easeInEaseOut)
            dot.layer.add(tapped, forKey: cornerMapMail([-12,-15,-24,-9,-31,-124],0x84,false))
        }
    }

@discardableResult
 func navigationAbbreviationPriceEraMovie() -> Double {
    var targetz: String! = String(cString: [97,114,98,105,116,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &targetz) { pointer in
    
   }
    var dotg: String! = String(cString: [116,101,120,105,100,101,112,0], encoding: .utf8)!
       var interitemZ: String! = String(cString: [98,117,108,108,101,116,115,0], encoding: .utf8)!
       var friendsA: Float = 5.0
      withUnsafeMutablePointer(to: &friendsA) { pointer in
             _ = pointer.pointee
      }
       var namesc: Int = 0
      withUnsafeMutablePointer(to: &namesc) { pointer in
             _ = pointer.pointee
      }
         namesc >>= Swift.min(labs((Int(friendsA > 180744561.0 || friendsA < -180744561.0 ? 10.0 : friendsA))), 1)
       var outgoingc: Bool = true
       _ = outgoingc
          var v_animationQ: [Any]! = [11, 1]
          var shouldg: [String: Any]! = [String(cString: [106,117,109,112,0], encoding: .utf8)!:94, String(cString: [109,111,110,111,119,104,105,116,101,0], encoding: .utf8)!:74, String(cString: [98,95,55,53,95,100,99,116,0], encoding: .utf8)!:82]
          var cosn: String! = String(cString: [115,116,116,115,0], encoding: .utf8)!
         namesc -= shouldg.count
         v_animationQ = [(3 | (outgoingc ? 1 : 5))]
         cosn = "\((3 % (Swift.max(Int(friendsA > 380176166.0 || friendsA < -380176166.0 ? 6.0 : friendsA), 8))))"
         outgoingc = namesc >= 66
       var did1: Int = 0
       var morez: Int = 3
      withUnsafeMutablePointer(to: &morez) { pointer in
    
      }
      while (3.28 >= friendsA) {
         friendsA += Float(1)
         break
      }
         did1 -= (1 + Int(friendsA > 275526439.0 || friendsA < -275526439.0 ? 28.0 : friendsA))
         friendsA += Float(3)
       var cos4: String! = String(cString: [109,105,115,115,101,115,0], encoding: .utf8)!
       var permissionD: String! = String(cString: [110,101,103,111,116,105,97,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &permissionD) { pointer in
    
      }
         morez -= 2 * namesc
         cos4.append("\(((outgoingc ? 1 : 2) * morez))")
         permissionD.append("\(morez + 2)")
      dotg.append("\(namesc >> (Swift.min(targetz.count, 5)))")
      interitemZ = "\(2)"
   repeat {
      targetz = "\(targetz.count % 1)"
      if 2623363 == targetz.count {
         break
      }
   } while (dotg.hasPrefix(targetz)) && (2623363 == targetz.count)
     var statBack: String! = String(cString: [114,101,110,100,105,116,105,111,110,0], encoding: .utf8)!
    var subtractmodScannedNatively:Double = 0

    return subtractmodScannedNatively

}






    func stopAnimating() {
       var resourceP: Double = 4.0
   for _ in 0 ..< 2 {
      resourceP /= Swift.max((Double(Int(resourceP > 373165553.0 || resourceP < -373165553.0 ? 42.0 : resourceP))), 1)
   }

        isAnimating = false
        dotViews.forEach { $0.layer.removeAllAnimations() }
    }


    override func layoutSubviews() {
       var sideC: String! = String(cString: [103,114,111,117,112,110,97,109,101,115,0], encoding: .utf8)!
   if 4 > sideC.count {
       var workdayv: Double = 1.0
      withUnsafeMutablePointer(to: &workdayv) { pointer in
    
      }
       var template__58: [String: Any]! = [String(cString: [102,111,114,103,111,116,116,101,110,0], encoding: .utf8)!:UILabel()]
       var dimn: Int = 0
       _ = dimn
       var padding4: String! = String(cString: [115,99,114,111,108,108,101,100,0], encoding: .utf8)!
       var fiveg: String! = String(cString: [109,97,116,105,99,0], encoding: .utf8)!
      if (2 * padding4.count) > 3 || (padding4.count * 2) > 3 {
         padding4 = "\(dimn)"
      }
      for _ in 0 ..< 3 {
         dimn ^= 1 % (Swift.max(9, dimn))
      }
          var showg: [Any]! = [36, 58]
          var stored1: [String: Any]! = [String(cString: [112,116,105,111,110,115,0], encoding: .utf8)!:82, String(cString: [103,117,101,115,115,0], encoding: .utf8)!:17, String(cString: [112,114,111,104,105,98,105,116,0], encoding: .utf8)!:6]
         withUnsafeMutablePointer(to: &stored1) { pointer in
                _ = pointer.pointee
         }
          var chatQ: Int = 3
         workdayv /= Swift.max(1, Double(stored1.count))
         showg = [1]
         chatQ += 1
         fiveg.append("\(fiveg.count)")
      if 5.25 < (workdayv + Double(template__58.keys.count)) {
         workdayv -= Double(3 % (Swift.max(6, dimn)))
      }
      for _ in 0 ..< 2 {
         padding4.append("\(dimn + 3)")
      }
         template__58["\(workdayv)"] = 2 ^ template__58.keys.count
      repeat {
         template__58 = ["\(template__58.keys.count)": fiveg.count / (Swift.max(1, template__58.keys.count))]
         if 813595 == template__58.count {
            break
         }
      } while ((1.93 / (Swift.max(4, workdayv))) > 2.93) && (813595 == template__58.count)
         dimn ^= dimn
      repeat {
         template__58 = [fiveg: 3]
         if 3658452 == template__58.count {
            break
         }
      } while (!template__58.values.contains { $0 as? Double == workdayv }) && (3658452 == template__58.count)
          var detailY: String! = String(cString: [98,108,111,98,0], encoding: .utf8)!
          var passwordL: Bool = false
         template__58 = ["\(passwordL)": detailY.count]
          var firstn: Double = 1.0
          _ = firstn
          var navigationu: Float = 0.0
          _ = navigationu
         template__58["\(dimn)"] = padding4.count
         firstn += Double(2)
         navigationu -= (Float(Int(workdayv > 282194577.0 || workdayv < -282194577.0 ? 44.0 : workdayv)))
          var folderG: Int = 2
          _ = folderG
          var previewt: Double = 2.0
          _ = previewt
         fiveg = "\(dimn | 1)"
         folderG |= 2 ^ fiveg.count
         previewt += (Double(Int(workdayv > 393254661.0 || workdayv < -393254661.0 ? 75.0 : workdayv) + folderG))
      repeat {
          var preferredP: String! = String(cString: [119,109,97,118,111,105,99,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &preferredP) { pointer in
    
         }
         padding4 = "\((fiveg == (String(cString:[118,0], encoding: .utf8)!) ? Int(workdayv > 46241156.0 || workdayv < -46241156.0 ? 43.0 : workdayv) : fiveg.count))"
         preferredP.append("\(preferredP.count)")
         if (String(cString:[116,97,115,122,0], encoding: .utf8)!) == padding4 {
            break
         }
      } while ((String(cString:[116,97,115,122,0], encoding: .utf8)!) == padding4) && (padding4.contains("\(template__58.count)"))
         padding4.append("\(dimn | 1)")
      sideC.append("\(((String(cString:[107,0], encoding: .utf8)!) == padding4 ? template__58.count : padding4.count))")
   }

        super.layoutSubviews()
        let total0: CGFloat = 8
        let fill: CGFloat = 8
        let sel = total0 * 3 + fill * 2
        var check = (bounds.width - sel) / 2
        let language = (bounds.height - total0) / 2
        for dot in dotViews {
            dot.frame = CGRect(x: check, y: language, width: total0, height: total0)
            check += total0 + fill
        }
    }
}
