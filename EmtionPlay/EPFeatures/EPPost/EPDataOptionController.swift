
import Foundation

import Toast_Swift
import UIKit
import UniformTypeIdentifiers

class EPDataOptionController: EPFriendChatController {

    private enum PickedMedia {
        case image(UIImage)
        case video(URL)
    }

    private enum ToastMessage {
        static let emptyContent = cornerMapMail([-114,-78,-69,-65,-83,-69,-2,-69,-80,-86,-69,-84,-2,-89,-79,-85,-84,-2,-67,-79,-80,-86,-69,-80,-86,-2,-72,-73,-84,-83,-86,-16,-34],0xDE,false)
        static let noMedia = cornerMapMail([-113,-77,-70,-66,-84,-70,-1,-66,-69,-69,-1,-66,-1,-81,-73,-80,-85,-80,-1,-80,-83,-1,-87,-74,-69,-70,-80,-15,-33],0xDF,false)
        static let insufficientCoins = cornerMapMail([-47,-25,-3,-88,-26,-19,-19,-20,-88,-23,-4,-88,-28,-19,-23,-5,-4,-88,-71,-72,-88,-21,-25,-31,-26,-5,-90,-88,-40,-28,-19,-23,-5,-19,-88,-6,-19,-21,-32,-23,-6,-17,-19,-88,-18,-31,-6,-5,-4,-90,-120],0x88,false)
        static let publishFailed = cornerMapMail([101,66,74,79,70,71,3,87,76,3,83,86,65,79,74,80,75,13,3,115,79,70,66,80,70,3,87,81,90,3,66,68,66,74,77,13,35],0x23,false)
        static let publishSuccess = cornerMapMail([122,69,89,94,79,78,10,89,95,73,73,79,89,89,76,95,70,70,83,11,42],0x2A,false)
    }

    private static let postCost = 10

    private var pickedMedia: PickedMedia?

    init() {
        super.init(nibName: nil, bundle: nil)
        hidesBottomBarWhenPushed = true
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError(cornerMapMail([75,76,75,86,10,65,77,70,71,80,24,11,2,74,67,81,2,76,77,86,2,64,71,71,76,2,75,79,82,78,71,79,71,76,86,71,70,34],0x22,false))
    }

@discardableResult
 func confirmDurationCloseSecondTask(catalogHeight: Double) -> Bool {
    var bubblek: [Any]! = [71, 36, 3]
    _ = bubblek
    var purchaseR: Double = 5.0
    var welcomer: Bool = false
    _ = welcomer
   for _ in 0 ..< 3 {
      purchaseR /= Swift.max(1, (Double(2 + (welcomer ? 3 : 5))))
   }
   for _ in 0 ..< 3 {
       var titlesO: [Any]! = [36, 32, 38]
       var interval_o5a: String! = String(cString: [117,110,118,111,116,101,0], encoding: .utf8)!
       _ = interval_o5a
       var roomsX: [String: Any]! = [String(cString: [108,101,97,121,0], encoding: .utf8)!:18, String(cString: [99,111,109,112,111,115,101,114,0], encoding: .utf8)!:48, String(cString: [98,105,110,107,100,97,116,97,0], encoding: .utf8)!:63]
      withUnsafeMutablePointer(to: &roomsX) { pointer in
             _ = pointer.pointee
      }
       var chat7: String! = String(cString: [108,108,97,117,100,100,115,112,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var unlockedH: [Any]! = [17, 81, 86]
          var colorU: [Any]! = [true]
          var seedsQ: [Any]! = [82, 52]
          _ = seedsQ
         interval_o5a = "\(interval_o5a.count >> (Swift.min(labs(1), 4)))"
         unlockedH = [1]
         colorU.append(roomsX.count & 1)
         seedsQ = [colorU.count]
      }
      for _ in 0 ..< 1 {
          var camera6: String! = String(cString: [114,97,110,100,111,109,0], encoding: .utf8)!
         interval_o5a = "\(camera6.count % 2)"
      }
      while (interval_o5a != chat7) {
         chat7 = "\(titlesO.count)"
         break
      }
         roomsX[interval_o5a] = interval_o5a.count
          var progressp: Bool = false
          var visiblew: Float = 3.0
         withUnsafeMutablePointer(to: &visiblew) { pointer in
                _ = pointer.pointee
         }
          var publishw: Bool = false
          _ = publishw
         interval_o5a.append("\(1)")
         progressp = !progressp && roomsX.values.count >= 47
         visiblew /= Swift.max(Float(chat7.count), 4)
         publishw = (((progressp ? chat7.count : 26) * chat7.count) >= 26)
      while (roomsX.keys.count <= interval_o5a.count) {
         interval_o5a.append("\(1)")
         break
      }
         titlesO.append(3 % (Swift.max(1, titlesO.count)))
       var launch_: String! = String(cString: [97,108,103,111,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &launch_) { pointer in
             _ = pointer.pointee
      }
       var fetch4: String! = String(cString: [97,108,105,118,101,0], encoding: .utf8)!
         launch_ = "\((interval_o5a == (String(cString:[119,0], encoding: .utf8)!) ? titlesO.count : interval_o5a.count))"
         fetch4 = "\(2)"
         roomsX = ["\(roomsX.values.count)": roomsX.values.count & 1]
          var components3: Double = 4.0
         titlesO.append(launch_.count)
         components3 += Double(chat7.count + launch_.count)
      purchaseR /= Swift.max(2, Double(3 * titlesO.count))
   }
   for _ in 0 ..< 2 {
      welcomer = (bubblek.contains { $0 as? Bool == welcomer })
   }
   return welcomer

}





    override func viewDidLoad() {
       var joinQ: String! = String(cString: [103,114,97,112,104,105,99,0], encoding: .utf8)!
      joinQ = "\(joinQ.count)"

        super.viewDidLoad()

        setupUI()
        setupConstraints()
        setupEvents()
    }

@discardableResult
 func commitObjectBubbleFoundReportContact() -> [Any]! {
    var logind: Int = 1
    var chat9: Double = 3.0
    var sheetG: [Any]! = [53, 59, 27]
       var language8: String! = String(cString: [114,117,108,101,115,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
          var halfz: Bool = false
          var user2: Bool = false
          var fireG: Int = 1
         withUnsafeMutablePointer(to: &fireG) { pointer in
    
         }
          var remaining4: Int = 0
          var pickV: Float = 5.0
         language8 = "\(fireG)"
         halfz = fireG <= 20
         user2 = 34 <= language8.count
         remaining4 %= Swift.max(fireG << (Swift.min(5, labs(remaining4))), 4)
         pickV += Float(2)
      }
         language8.append("\(2 + language8.count)")
         language8 = "\((language8 == (String(cString:[89,0], encoding: .utf8)!) ? language8.count : language8.count))"
      chat9 += Double(1)
      sheetG = [(Int(chat9 > 87704722.0 || chat9 < -87704722.0 ? 58.0 : chat9))]
      sheetG.append(3 >> (Swift.min(3, sheetG.count)))
   if 4 >= (logind ^ logind) && (logind ^ 4) >= 5 {
      logind *= logind >> (Swift.min(labs(1), 3))
   }
   return sheetG

}





    private func setupUI() {
       var scale2: Int = 0
    _ = scale2
      scale2 += scale2 - 2


        view.addSubview(backButton)
        view.addSubview(titleView)
        view.addSubview(textContainerView)
        textContainerView.addSubview(textView)
        textContainerView.addSubview(placeholderLabel)
        view.addSubview(addMediaButton)
        view.addSubview(costLabel)
        view.addSubview(submitButton)
    }

@discardableResult
 func ensureBeforeAppendRunningTomorrowCompressionImageView(eraPosts: [String: Any]!) -> UIImageView! {
    var placeholderJ: [String: Any]! = [String(cString: [116,97,112,0], encoding: .utf8)!:String(cString: [100,101,116,101,99,116,101,100,0], encoding: .utf8)!]
    _ = placeholderJ
    var processedp: Float = 3.0
   withUnsafeMutablePointer(to: &processedp) { pointer in
    
   }
   while ((Float(placeholderJ.keys.count) + processedp) < 2.18) {
      processedp += Float(3)
      break
   }
   for _ in 0 ..< 2 {
       var comment6: Double = 2.0
       var bundle8: [String: Any]! = [String(cString: [99,108,97,112,0], encoding: .utf8)!:String(cString: [97,117,116,111,114,101,103,114,101,115,115,105,111,110,0], encoding: .utf8)!, String(cString: [108,97,116,101,98,105,110,100,105,110,103,115,121,109,98,111,108,116,97,98,108,101,0], encoding: .utf8)!:String(cString: [114,101,97,115,111,110,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &bundle8) { pointer in
    
      }
          var targetM: String! = String(cString: [108,111,119,101,114,0], encoding: .utf8)!
         comment6 -= Double(bundle8.values.count)
         targetM = "\(2)"
      for _ in 0 ..< 1 {
          var twoO: Double = 3.0
          var hasa: String! = String(cString: [119,97,108,108,112,97,112,101,114,0], encoding: .utf8)!
         bundle8 = [hasa: ((String(cString:[112,0], encoding: .utf8)!) == hasa ? Int(comment6 > 126579900.0 || comment6 < -126579900.0 ? 29.0 : comment6) : hasa.count)]
         twoO /= Swift.max((Double(bundle8.values.count | Int(twoO > 137057299.0 || twoO < -137057299.0 ? 96.0 : twoO))), 5)
      }
      for _ in 0 ..< 3 {
          var nanosecondz: [String: Any]! = [String(cString: [122,101,114,111,109,118,0], encoding: .utf8)!:35, String(cString: [107,101,121,115,0], encoding: .utf8)!:50]
          var receiveu: [String: Any]! = [String(cString: [105,110,115,101,116,0], encoding: .utf8)!:5, String(cString: [100,99,97,100,97,116,97,0], encoding: .utf8)!:4]
         withUnsafeMutablePointer(to: &receiveu) { pointer in
    
         }
         comment6 /= Swift.max(Double(1), 3)
         nanosecondz["\(comment6)"] = (Int(comment6 > 33120163.0 || comment6 < -33120163.0 ? 70.0 : comment6) % (Swift.max(bundle8.keys.count, 9)))
         receiveu["\(comment6)"] = (Int(comment6 > 34987698.0 || comment6 < -34987698.0 ? 2.0 : comment6) - 3)
      }
      repeat {
         bundle8["\(comment6)"] = (Int(comment6 > 3315089.0 || comment6 < -3315089.0 ? 61.0 : comment6) + bundle8.keys.count)
         if bundle8.count == 217187 {
            break
         }
      } while (bundle8.values.contains { $0 as? Double == comment6 }) && (bundle8.count == 217187)
      if bundle8.count < 3 {
          var deliverJ: [Any]! = [23, 90, 66]
          _ = deliverJ
          var postsJ: String! = String(cString: [102,116,101,108,108,0], encoding: .utf8)!
          var cosP: String! = String(cString: [112,114,105,110,116,102,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &cosP) { pointer in
    
         }
          var hasK: String! = String(cString: [99,111,112,121,99,111,111,107,101,114,0], encoding: .utf8)!
          var areaf: String! = String(cString: [101,120,110,111,100,101,0], encoding: .utf8)!
          _ = areaf
         bundle8[areaf] = areaf.count
         deliverJ.append((Int(comment6 > 340117099.0 || comment6 < -340117099.0 ? 88.0 : comment6)))
         postsJ.append("\((Int(comment6 > 18350457.0 || comment6 < -18350457.0 ? 77.0 : comment6) | 1))")
         cosP.append("\(postsJ.count)")
         hasK.append("\((postsJ == (String(cString:[101,0], encoding: .utf8)!) ? postsJ.count : hasK.count))")
      }
       var outfit4: String! = String(cString: [115,99,104,101,100,0], encoding: .utf8)!
       var infoa: String! = String(cString: [117,110,105,110,116,101,114,112,114,101,116,101,100,0], encoding: .utf8)!
         outfit4 = "\(infoa.count)"
         infoa = "\((infoa == (String(cString:[103,0], encoding: .utf8)!) ? infoa.count : outfit4.count))"
      placeholderJ["\(comment6)"] = (Int(processedp > 228194984.0 || processedp < -228194984.0 ? 63.0 : processedp) * 3)
   }
     let reportEmpty: String! = String(cString: [102,97,114,0], encoding: .utf8)!
     let fanSafe: Double = 51.0
     var seedModel: Double = 55.0
    var intlEctangleUse: UIImageView! = UIImageView()
    intlEctangleUse.animationRepeatCount = 7
    intlEctangleUse.image = UIImage(named:String(cString: [115,104,97,114,101,100,0], encoding: .utf8)!)
    intlEctangleUse.contentMode = .scaleAspectFit
    intlEctangleUse.alpha = 0.6;
    intlEctangleUse.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    intlEctangleUse.frame = CGRect(x: 101, y: 192, width: 0, height: 0)

    
    return intlEctangleUse

}





    private func setupConstraints() {
       var thresholdQ: String! = String(cString: [109,111,118,101,0], encoding: .utf8)!
    _ = thresholdQ
       var networkq: String! = String(cString: [109,97,114,107,0], encoding: .utf8)!
       var unreadc: String! = String(cString: [99,105,110,101,112,97,107,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &unreadc) { pointer in
    
      }
         unreadc = "\(unreadc.count << (Swift.min(labs(2), 1)))"
      if 4 == unreadc.count {
          var formp: String! = String(cString: [115,116,114,105,100,101,98,0], encoding: .utf8)!
          var max_b0L: Float = 0.0
         unreadc.append("\(1)")
         formp = "\((unreadc == (String(cString:[74,0], encoding: .utf8)!) ? Int(max_b0L > 13121678.0 || max_b0L < -13121678.0 ? 82.0 : max_b0L) : unreadc.count))"
         max_b0L += Float(formp.count)
      }
         networkq = "\(unreadc.count)"
         unreadc = "\(unreadc.count | 3)"
         networkq = "\(unreadc.count + networkq.count)"
       var control4: Double = 5.0
      withUnsafeMutablePointer(to: &control4) { pointer in
             _ = pointer.pointee
      }
       var genZ: Double = 4.0
         control4 /= Swift.max(5, Double(networkq.count))
         genZ -= (Double(Int(genZ > 100356371.0 || genZ < -100356371.0 ? 98.0 : genZ) - Int(control4 > 286653352.0 || control4 < -286653352.0 ? 40.0 : control4)))
      thresholdQ.append("\(thresholdQ.count / (Swift.max(networkq.count, 10)))")

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(10)
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.size.equalTo(44)
        }

        titleView.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.centerY.equalTo(backButton)
        }

        textContainerView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.top.equalTo(backButton.snp.bottom).offset(20)
            make.height.equalTo(180)
        }

        textView.snp.makeConstraints { make in
            make.edges.equalToSuperview().inset(12)
        }

        placeholderLabel.snp.makeConstraints { make in
            make.top.leading.equalTo(textView).offset(4)
            make.trailing.lessThanOrEqualTo(textView).offset(-4)
        }

        addMediaButton.snp.makeConstraints { make in
            make.leading.equalTo(textContainerView)
            make.top.equalTo(textContainerView.snp.bottom).offset(16)
            make.size.equalTo(132)
        }

        submitButton.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.bottom.equalTo(view.safeAreaLayoutGuide).inset(75)
            make.width.equalTo(267)
            make.height.equalTo(74)
        }

        costLabel.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.leading.trailing.equalToSuperview().inset(20)
            make.bottom.equalTo(submitButton.snp.top).offset(-15)
        }
    }

    private func setupEvents() {
       var fivel: String! = String(cString: [100,105,110,102,0], encoding: .utf8)!
    _ = fivel
    var bundleu: Double = 0.0
   if 5.3 >= (bundleu + 4.27) {
      fivel = "\(fivel.count)"
   }

   for _ in 0 ..< 2 {
      bundleu -= (Double(Int(bundleu > 173828508.0 || bundleu < -173828508.0 ? 71.0 : bundleu) >> (Swift.min(labs(Int(bundleu > 132438182.0 || bundleu < -132438182.0 ? 10.0 : bundleu)), 1))))
   }
        backButton.addTarget(self, action: #selector(clickBackButton), for: .touchUpInside)
        addMediaButton.addTarget(self, action: #selector(onAddMediaTapped), for: .touchUpInside)
        submitButton.addTarget(self, action: #selector(onSubmitTapped), for: .touchUpInside)
        textView.delegate = self
    }

@discardableResult
 func failureResignIndicator(offCorner: [Any]!, register_gData: [String: Any]!) -> Float {
    var scriptsE: Int = 5
    var n_widthE: [Any]! = [String(cString: [117,115,101,0], encoding: .utf8)!]
    var joina: Float = 2.0
   withUnsafeMutablePointer(to: &joina) { pointer in
          _ = pointer.pointee
   }
   while ((scriptsE & n_widthE.count) <= 1 && (1 & scriptsE) <= 4) {
      scriptsE -= scriptsE
      break
   }
   while (3 <= n_widthE.count) {
       var seedss: Int = 5
       var receiveh: Float = 1.0
       var twoY: String! = String(cString: [108,111,99,97,108,101,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
          var requestn: String! = String(cString: [102,97,117,108,116,0], encoding: .utf8)!
          var ownerI: String! = String(cString: [109,97,103,110,105,116,117,100,101,0], encoding: .utf8)!
          var contact_: Double = 0.0
         seedss -= (Int(contact_ > 392784896.0 || contact_ < -392784896.0 ? 22.0 : contact_))
         requestn.append("\((2 - Int(contact_ > 359806856.0 || contact_ < -359806856.0 ? 9.0 : contact_)))")
         ownerI.append("\(((String(cString:[66,0], encoding: .utf8)!) == ownerI ? ownerI.count : seedss))")
      }
         receiveh -= Float(twoY.count | 1)
      repeat {
         receiveh /= Swift.max(1, Float(3))
         if receiveh == 1190216.0 {
            break
         }
      } while (receiveh == 1190216.0) && (4 == (twoY.count + Int(receiveh > 324041834.0 || receiveh < -324041834.0 ? 39.0 : receiveh)))
      for _ in 0 ..< 2 {
          var credentials: Int = 1
          var j_playerV: String! = String(cString: [112,108,111,116,116,101,114,0], encoding: .utf8)!
          var arear: String! = String(cString: [115,116,114,114,101,115,101,116,0], encoding: .utf8)!
         receiveh -= Float(twoY.count)
         credentials &= j_playerV.count
         j_playerV = "\(2 + twoY.count)"
         arear = "\(2 * credentials)"
      }
         receiveh -= Float(1)
      while (1 <= (3 << (Swift.min(3, twoY.count))) && (twoY.count | 3) <= 1) {
         receiveh += (Float(twoY == (String(cString:[98,0], encoding: .utf8)!) ? twoY.count : seedss))
         break
      }
      while (Int(receiveh) == seedss) {
         receiveh -= Float(seedss)
         break
      }
      if (1 / (Swift.max(4, twoY.count))) == 1 {
         twoY = "\((Int(receiveh > 356171764.0 || receiveh < -356171764.0 ? 40.0 : receiveh)))"
      }
         twoY.append("\(seedss)")
      scriptsE += 2
      break
   }
       var normalized1: String! = String(cString: [102,117,108,108,98,97,110,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &normalized1) { pointer in
             _ = pointer.pointee
      }
      if !normalized1.contains(normalized1) {
         normalized1 = "\(1 & normalized1.count)"
      }
      for _ in 0 ..< 2 {
         normalized1 = "\(1 >> (Swift.min(3, normalized1.count)))"
      }
       var appearance6: [String: Any]! = [String(cString: [98,117,108,108,101,116,0], encoding: .utf8)!:82, String(cString: [115,112,101,99,105,102,105,99,0], encoding: .utf8)!:50, String(cString: [109,106,112,101,103,98,0], encoding: .utf8)!:75]
      withUnsafeMutablePointer(to: &appearance6) { pointer in
             _ = pointer.pointee
      }
         appearance6 = ["\(appearance6.keys.count)": 2]
      joina -= (Float(Int(joina > 120797537.0 || joina < -120797537.0 ? 71.0 : joina) / 1))
   return joina

}





    @objc private func clickBackButton() {
       var class_3m: Double = 4.0
   withUnsafeMutablePointer(to: &class_3m) { pointer in
          _ = pointer.pointee
   }
      class_3m -= Double(2)

        navigationController?.popViewController(animated: true)
    }

    @objc private func onAddMediaTapped() {
       var release_mI: String! = String(cString: [115,104,114,105,110,107,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &release_mI) { pointer in
    
   }
      release_mI.append("\(1 - release_mI.count)")

        guard UIImagePickerController.isSourceTypeAvailable(.photoLibrary) else { return }
        let infor = UIImagePickerController()
        infor.sourceType = .photoLibrary
        infor.delegate = self
        infor.allowsEditing = true
        infor.mediaTypes = [UTType.image.identifier, UTType.movie.identifier]
        present(infor, animated: true)
    }

@discardableResult
 func actualReportColumnImageView(inforAchie: [Any]!, activePublished: [Any]!) -> UIImageView! {
    var passwordE: [String: Any]! = [String(cString: [116,114,101,97,116,0], encoding: .utf8)!:79, String(cString: [105,100,101,97,108,0], encoding: .utf8)!:93]
    var posts2: String! = String(cString: [98,111,97,116,0], encoding: .utf8)!
   if 4 <= passwordE.values.count {
      posts2 = "\(posts2.count)"
   }
      passwordE["\(posts2)"] = posts2.count
     let min_6fFriend_js: Int = 70
     var publishPixel: [Any]! = [49, 6]
     let weekdayPublished: UIButton! = UIButton(frame:CGRect(x: 256, y: 443, width: 0, height: 0))
    var storeframeEpsilonOpcode = UIImageView(frame:CGRect.zero)
    storeframeEpsilonOpcode.frame = CGRect(x: 52, y: 115, width: 0, height: 0)
    storeframeEpsilonOpcode.alpha = 0.9;
    storeframeEpsilonOpcode.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    storeframeEpsilonOpcode.contentMode = .scaleAspectFit
    storeframeEpsilonOpcode.animationRepeatCount = 0
    storeframeEpsilonOpcode.image = UIImage(named:String(cString: [100,105,109,0], encoding: .utf8)!)
    weekdayPublished.frame = CGRect(x: 292, y: 103, width: 0, height: 0)
    weekdayPublished.alpha = 0.9;
    weekdayPublished.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    weekdayPublished.setImage(UIImage(named:String(cString: [103,111,97,108,0], encoding: .utf8)!), for: .normal)
    weekdayPublished.setTitle("", for: .normal)
    weekdayPublished.setBackgroundImage(UIImage(named:String(cString: [117,95,109,97,110,97,103,101,114,0], encoding: .utf8)!), for: .normal)
    weekdayPublished.titleLabel?.font = UIFont.systemFont(ofSize:11)
    

    
    return storeframeEpsilonOpcode

}





    @objc private func onSubmitTapped() {
       var backx: Bool = false
    var displayk: String! = String(cString: [116,104,114,101,97,100,115,108,105,99,101,0], encoding: .utf8)!
   if backx {
       var processedP: Double = 3.0
       var readR: String! = String(cString: [114,101,115,105,122,101,0], encoding: .utf8)!
       _ = readR
      for _ in 0 ..< 1 {
         readR = "\((Int(processedP > 327582321.0 || processedP < -327582321.0 ? 93.0 : processedP)))"
      }
          var buttonsz: String! = String(cString: [104,112,114,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &buttonsz) { pointer in
    
         }
          var energyD: String! = String(cString: [116,119,111,112,111,105,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &energyD) { pointer in
    
         }
          var f_titleA: String! = String(cString: [99,111,114,110,101,114,115,0], encoding: .utf8)!
          _ = f_titleA
         readR.append("\(3)")
         buttonsz.append("\(f_titleA.count | readR.count)")
         energyD = "\((Int(processedP > 263165149.0 || processedP < -263165149.0 ? 31.0 : processedP)))"
         f_titleA.append("\(energyD.count / 1)")
         readR = "\((readR.count + Int(processedP > 240438399.0 || processedP < -240438399.0 ? 26.0 : processedP)))"
         processedP += (Double(readR == (String(cString:[104,0], encoding: .utf8)!) ? readR.count : Int(processedP > 194137196.0 || processedP < -194137196.0 ? 82.0 : processedP)))
         processedP -= (Double(Int(processedP > 222642028.0 || processedP < -222642028.0 ? 15.0 : processedP)))
          var makeH: Bool = false
         readR = "\((Int(processedP > 65439586.0 || processedP < -65439586.0 ? 91.0 : processedP) + (makeH ? 1 : 3)))"
      backx = (displayk.count >> (Swift.min(readR.count, 5))) >= 34
   }

   for _ in 0 ..< 2 {
      displayk.append("\(2 ^ displayk.count)")
   }
        let trailing = textView.text.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !trailing.isEmpty else {
            view.makeToast(ToastMessage.emptyContent)
            return
        }
        guard pickedMedia != nil else {
            view.makeToast(ToastMessage.noMedia)
            return
        }
        guard let currentUser = EPRankMenu.shared.user, currentUser.coins >= Self.postCost else {
            view.makeToast(ToastMessage.insufficientCoins)
            return
        }

        guard let post = buildPost(content: trailing, user: currentUser) else {
            view.makeToast(ToastMessage.publishFailed)
            return
        }
        guard EPManagerStore.shared.addPost(post, toUserId: currentUser.userId) else {
            view.makeToast(ToastMessage.publishFailed)
            return
        }
        EPManagerStore.shared.incrementBadgePush(userId: currentUser.userId)
        let outgoing = currentUser.coins - Self.postCost
        guard EPManagerStore.shared.updateUser(userId: currentUser.userId, coins: outgoing) else {
            view.makeToast(ToastMessage.publishFailed)
            return
        }
        EPRankMenu.shared.refreshFromDatabase()
        
        EPCamera.shared.fetchHuaPl { result in
            switch result {
            case .success(_):
                self.view.makeToast(ToastMessage.publishSuccess)
                self.navigationController?.popViewController(animated: true)
            case .failure(_):
                self.view.makeToast(ToastMessage.publishSuccess)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    private func buildPost(content: String, user: EPUserLaunch) -> EPHomeFeed? {
       var thumbI: [String: Any]! = [String(cString: [102,102,118,108,0], encoding: .utf8)!:66, String(cString: [118,105,101,119,115,0], encoding: .utf8)!:61]
   if 5 <= (3 >> (Swift.min(4, thumbI.values.count))) && 2 <= (thumbI.values.count >> (Swift.min(labs(3), 3))) {
       var hours8: [Any]! = [[String(cString: [118,105,100,101,111,100,115,112,0], encoding: .utf8)!:46]]
       var accesso: String! = String(cString: [98,101,116,104,115,111,102,116,118,105,100,101,111,0], encoding: .utf8)!
       var commentso: Int = 2
       _ = commentso
       var statE: String! = String(cString: [105,109,112,111,115,116,101,114,0], encoding: .utf8)!
       _ = statE
      if 1 >= hours8.count {
         statE = "\(statE.count | 1)"
      }
         commentso ^= commentso
      repeat {
         hours8 = [3]
         if hours8.count == 162546 {
            break
         }
      } while ((commentso >> (Swift.min(hours8.count, 5))) == 2) && (hours8.count == 162546)
          var achie7: String! = String(cString: [109,97,120,106,0], encoding: .utf8)!
         hours8 = [commentso]
         achie7.append("\(accesso.count)")
      thumbI = ["\(hours8.count)": (accesso == (String(cString:[104,0], encoding: .utf8)!) ? hours8.count : accesso.count)]
   }

        let key = "\(cornerMapMail([-77,-84,-80,-73,-100,-61],0xC3,false))\(Int(Date().timeIntervalSince1970))_\(UUID().uuidString.prefix(6).lowercased())"
        var profile = ""
        var mic = ""

        switch pickedMedia {
        case .image(let image):
            guard let base = SS_PublishedPostMedia.savePhoto(image) else { return nil }
            profile = base
        case .video(let url):
            let tool = url.startAccessingSecurityScopedResource()
            defer {
                if tool { url.stopAccessingSecurityScopedResource() }
            }
            guard let base = SS_PublishedPostMedia.saveVideo(from: url) else { return nil }
            mic = base
        case .none:
            return nil
        }

        return EPHomeFeed(
            postId: key,
            userId: user.userId,
            authorName: user.name,
            authorAvatar: user.avatar,
            coverImage: cornerMapMail([6,25,5,2,41,2,19,27,6,118],0x76,false),
            img: profile,
            video: mic,
            content: content,
            isLiked: false,
            likeCount: 0,
            commentCount: 0,
            comments: []
        )
    }

    private func applyMediaPreview(image: UIImage) {
       var deliveredQ: Float = 1.0
    _ = deliveredQ
   for _ in 0 ..< 2 {
       var postsu: String! = String(cString: [115,117,98,115,97,109,112,0], encoding: .utf8)!
       var total2: String! = String(cString: [97,105,114,105,110,103,0], encoding: .utf8)!
       _ = total2
       var error9: Int = 2
       var lendok: Double = 0.0
      withUnsafeMutablePointer(to: &lendok) { pointer in
    
      }
         total2.append("\(error9 + postsu.count)")
         total2.append("\((total2 == (String(cString:[121,0], encoding: .utf8)!) ? Int(lendok > 125412296.0 || lendok < -125412296.0 ? 10.0 : lendok) : total2.count))")
      repeat {
          var settingP: String! = String(cString: [117,105,111,116,111,109,98,117,102,0], encoding: .utf8)!
         error9 ^= settingP.count % 2
         if 2084946 == error9 {
            break
         }
      } while ((4 >> (Swift.min(1, labs(error9)))) <= 4) && (2084946 == error9)
          var normalized1: String! = String(cString: [114,101,116,114,105,101,114,0], encoding: .utf8)!
         lendok /= Swift.max((Double(2 * Int(lendok > 235569656.0 || lendok < -235569656.0 ? 43.0 : lendok))), 5)
         normalized1.append("\(error9)")
         total2 = "\((Int(lendok > 379912962.0 || lendok < -379912962.0 ? 58.0 : lendok)))"
      repeat {
         lendok += Double(error9 % (Swift.max(total2.count, 4)))
         if 1272136.0 == lendok {
            break
         }
      } while (1272136.0 == lendok) && ((3 >> (Swift.min(1, postsu.count))) > 5 && 2 > (postsu.count + 3))
      if 1 <= (postsu.count + Int(lendok > 240107568.0 || lendok < -240107568.0 ? 65.0 : lendok)) && (lendok + Double(postsu.count)) <= 2.55 {
          var mailf: [Any]! = [59, 67, 77]
          var gainv: String! = String(cString: [109,105,110,105,109,97,108,108,121,0], encoding: .utf8)!
          _ = gainv
         lendok /= Swift.max((Double(1 + Int(lendok > 325669712.0 || lendok < -325669712.0 ? 84.0 : lendok))), 1)
         mailf = [((String(cString:[115,0], encoding: .utf8)!) == total2 ? error9 : total2.count)]
         gainv = "\(gainv.count & error9)"
      }
       var imgN: Double = 0.0
         error9 ^= postsu.count
      while (2.50 == (imgN + 1.53)) {
         imgN -= (Double(Int(imgN > 236819740.0 || imgN < -236819740.0 ? 49.0 : imgN)))
         break
      }
      while (total2 == String(cString:[50,0], encoding: .utf8)! && postsu != String(cString:[87,0], encoding: .utf8)!) {
          var y_productsx: String! = String(cString: [114,117,110,108,111,111,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &y_productsx) { pointer in
                _ = pointer.pointee
         }
          var otherE: String! = String(cString: [101,99,111,117,110,116,0], encoding: .utf8)!
          var replyN: String! = String(cString: [99,104,101,99,107,97,115,109,0], encoding: .utf8)!
          var containerP: Bool = true
          _ = containerP
         total2.append("\((3 % (Swift.max(5, Int(imgN > 43594746.0 || imgN < -43594746.0 ? 96.0 : imgN)))))")
         y_productsx.append("\(((containerP ? 3 : 4)))")
         otherE.append("\(3)")
         replyN.append("\((1 & Int(imgN > 339586755.0 || imgN < -339586755.0 ? 75.0 : imgN)))")
         containerP = (error9 | total2.count) <= 62
         break
      }
      repeat {
          var badgesr: Double = 1.0
          var overlap8: String! = String(cString: [103,100,112,114,0], encoding: .utf8)!
          _ = overlap8
         error9 &= overlap8.count * 1
         badgesr -= Double(1)
         if error9 == 702198 {
            break
         }
      } while (error9 == 702198) && ((imgN * 3.91) == 3.74 && (3.91 * imgN) == 4.99)
      deliveredQ -= Float(3)
   }

        addMediaButton.setImage(image, for: .normal)
        addMediaButton.imageView?.contentMode = .scaleAspectFill
    }

@discardableResult
 func directionalBounceOrientation() -> Double {
    var energyq: String! = String(cString: [104,101,97,114,116,115,0], encoding: .utf8)!
    var qinitialM: Float = 5.0
   repeat {
      energyq = "\(1)"
      if energyq == (String(cString:[98,118,56,112,121,97,110,105,0], encoding: .utf8)!) {
         break
      }
   } while (energyq == (String(cString:[98,118,56,112,121,97,110,105,0], encoding: .utf8)!)) && ((energyq.count / (Swift.max(3, 9))) == 3 && (qinitialM * 2.54) == 4.54)
      qinitialM += (Float(energyq.count + Int(qinitialM > 246059747.0 || qinitialM < -246059747.0 ? 86.0 : qinitialM)))
     let commentsTemplate_3: String! = String(cString: [100,101,99,111,109,112,97,110,100,0], encoding: .utf8)!
    var artifactsAskSearch:Double = 0

    return artifactsAskSearch

}





    private func updatePlaceholderVisibility() {
       var progressm: [Any]! = [88, 31, 12]
   for _ in 0 ..< 3 {
       var optionM: Bool = false
       var previousD: Bool = false
       _ = previousD
       var update_eL: String! = String(cString: [99,111,115,116,105,0], encoding: .utf8)!
       var weekday3: Bool = false
      withUnsafeMutablePointer(to: &weekday3) { pointer in
             _ = pointer.pointee
      }
       var gaing: [Any]! = [66, 72, 45]
          var incomingo: [Any]! = [39, 38, 6]
         gaing = [1]
         incomingo = [1]
      while (optionM) {
         optionM = !optionM && gaing.count >= 15
         break
      }
      repeat {
         update_eL.append("\(3)")
         if update_eL.count == 2094307 {
            break
         }
      } while (!update_eL.hasSuffix("\(optionM)")) && (update_eL.count == 2094307)
      repeat {
         weekday3 = gaing.count <= update_eL.count
         if weekday3 ? !weekday3 : weekday3 {
            break
         }
      } while (weekday3 ? !weekday3 : weekday3) && (weekday3)
      for _ in 0 ..< 3 {
          var presentationO: [Any]! = [String(cString: [113,116,114,108,101,0], encoding: .utf8)!, String(cString: [115,116,97,98,105,108,105,122,101,0], encoding: .utf8)!, String(cString: [112,114,101,100,111,119,110,108,111,97,100,0], encoding: .utf8)!]
          var authJ: String! = String(cString: [99,119,110,100,0], encoding: .utf8)!
          _ = authJ
          var remainingh: String! = String(cString: [115,99,114,111,108,108,97,98,108,101,0], encoding: .utf8)!
          var permissionF: [String: Any]! = [String(cString: [105,110,105,116,105,97,108,0], encoding: .utf8)!:99, String(cString: [100,117,109,112,105,110,102,111,0], encoding: .utf8)!:50]
          _ = permissionF
         gaing.append(permissionF.values.count)
         presentationO = [((weekday3 ? 3 : 1) % 2)]
         authJ = "\(((String(cString:[72,0], encoding: .utf8)!) == authJ ? (weekday3 ? 1 : 5) : authJ.count))"
         remainingh = "\(presentationO.count + 1)"
      }
      repeat {
          var energyP: Int = 0
         gaing.append(((String(cString:[53,0], encoding: .utf8)!) == update_eL ? update_eL.count : (weekday3 ? 1 : 2)))
         energyP &= 1
         if 2022452 == gaing.count {
            break
         }
      } while (3 >= gaing.count) && (2022452 == gaing.count)
         previousD = !optionM
      repeat {
         optionM = gaing.count == 13
         if optionM ? !optionM : optionM {
            break
         }
      } while (optionM) && (optionM ? !optionM : optionM)
         previousD = !previousD && !optionM
      while (!optionM) {
         optionM = !weekday3
         break
      }
          var half8: String! = String(cString: [103,114,111,119,105,110,103,0], encoding: .utf8)!
          var did1: String! = String(cString: [110,101,103,111,116,105,97,116,105,111,110,0], encoding: .utf8)!
         update_eL.append("\((1 & (previousD ? 1 : 4)))")
         half8 = "\(gaing.count)"
         did1 = "\((did1 == (String(cString:[111,0], encoding: .utf8)!) ? did1.count : (optionM ? 3 : 1)))"
      while (optionM) {
          var presentatione: String! = String(cString: [118,97,99,117,117,109,0], encoding: .utf8)!
         optionM = weekday3
         presentatione = "\(((previousD ? 1 : 4)))"
         break
      }
         weekday3 = (gaing.contains { $0 as? Bool == optionM })
         optionM = (weekday3 ? optionM : weekday3)
      for _ in 0 ..< 1 {
          var preferredD: Bool = true
          _ = preferredD
          var messageO: Bool = false
          var minuteU: String! = String(cString: [115,101,110,116,101,110,99,101,0], encoding: .utf8)!
          var descL: Bool = false
          _ = descL
         optionM = (((optionM ? gaing.count : 57) - gaing.count) > 57)
         preferredD = gaing.count > 52
         messageO = update_eL.count <= 79 && messageO
         minuteU = "\(2 + minuteU.count)"
         descL = !messageO
      }
      progressm.append((2 << (Swift.min(labs((optionM ? 1 : 2)), 2))))
   }

        placeholderLabel.isHidden = !(textView.text?.isEmpty ?? true)
    }

    private let backButton: UIButton = {
       var layoutL: Double = 4.0
    _ = layoutL
    var overlayF: String! = String(cString: [98,108,105,110,100,101,100,0], encoding: .utf8)!
      layoutL /= Swift.max((Double(Int(layoutL > 80347647.0 || layoutL < -80347647.0 ? 95.0 : layoutL))), 5)

        let button = UIButton(type: .custom)
   for _ in 0 ..< 2 {
      overlayF.append("\(overlayF.count << (Swift.min(labs(1), 1)))")
   }
        button.setImage(cornerMapMail([8,4,6,6,4,5,52,9,10,8,0,107],0x6B,false).toImage, for: .normal)
        return button
    }()

    private let titleView: UIImageView = {
       var publishedH: [Any]! = [83, 40]
   if publishedH.contains { $0 as? Int == publishedH.count } {
      publishedH = [3]
   }

        let view = UIImageView()
        view.contentMode = .scaleAspectFit
        view.image = cornerMapMail([-81,-72,-79,-72,-68,-82,-72,-126,-87,-76,-87,-79,-72,-35],0xDD,false).toImage
        return view
    }()

    private let textContainerView: UIView = {
       var changeK: Float = 2.0
   withUnsafeMutablePointer(to: &changeK) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 3 {
      changeK /= Swift.max(2, (Float(Int(changeK > 40444711.0 || changeK < -40444711.0 ? 8.0 : changeK) << (Swift.min(labs(Int(changeK > 22339398.0 || changeK < -22339398.0 ? 24.0 : changeK)), 3)))))
   }

        let view = UIView()
        view.backgroundColor = .white
        view.layer.cornerRadius = 12
        view.layer.masksToBounds = true
        return view
    }()

    private lazy var textView: UITextView = {
       var refreshQ: Double = 4.0
    var removeu: String! = String(cString: [115,112,101,97,107,101,114,115,0], encoding: .utf8)!
    _ = removeu
   repeat {
      removeu = "\((Int(refreshQ > 249736055.0 || refreshQ < -249736055.0 ? 2.0 : refreshQ)))"
      if removeu == (String(cString:[117,105,105,55,51,49,113,106,0], encoding: .utf8)!) {
         break
      }
   } while (removeu == (String(cString:[117,105,105,55,51,49,113,106,0], encoding: .utf8)!)) && (refreshQ < 5.18)

        let areaView = UITextView()
       var posts8: Double = 1.0
       _ = posts8
      while ((posts8 / (Swift.max(posts8, 6))) >= 4.72 && 2.67 >= (posts8 / 4.72)) {
         posts8 /= Swift.max((Double(3 & Int(posts8 > 32778743.0 || posts8 < -32778743.0 ? 49.0 : posts8))), 1)
         break
      }
         posts8 -= (Double(2 * Int(posts8 > 294667301.0 || posts8 < -294667301.0 ? 40.0 : posts8)))
      if posts8 == 5.67 {
          var buttonX: [String: Any]! = [String(cString: [109,97,105,108,99,104,105,109,112,0], encoding: .utf8)!:4, String(cString: [119,114,105,116,101,114,0], encoding: .utf8)!:7, String(cString: [112,114,111,114,101,115,100,115,112,0], encoding: .utf8)!:30]
          var requestq: String! = String(cString: [97,120,112,0], encoding: .utf8)!
          var file4: Double = 0.0
          var error7: [Any]! = [String(cString: [116,121,112,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &error7) { pointer in
    
         }
         posts8 += Double(2)
         buttonX["\(file4)"] = (Int(file4 > 67895520.0 || file4 < -67895520.0 ? 63.0 : file4) ^ error7.count)
         requestq.append("\(3 | buttonX.keys.count)")
         error7.append(((String(cString:[108,0], encoding: .utf8)!) == requestq ? Int(posts8 > 263719562.0 || posts8 < -263719562.0 ? 29.0 : posts8) : requestq.count))
      }
      refreshQ += Double(removeu.count | 1)
        areaView.font = .systemFont(ofSize: 16, weight: .regular)
        areaView.textColor = .black
        areaView.backgroundColor = .clear
        areaView.textContainerInset = .zero
        areaView.textContainer.lineFragmentPadding = 0
        return areaView
    }()

    private let placeholderLabel: UILabel = {
       var firstN: Float = 0.0
    var appW: Double = 4.0
      appW /= Swift.max(2, (Double(Int(appW > 348083572.0 || appW < -348083572.0 ? 6.0 : appW) * Int(firstN > 215133579.0 || firstN < -215133579.0 ? 83.0 : firstN))))

        let label = UILabel()
      appW += Double(1)
        label.text = cornerMapMail([-31,-48,-39,-39,-107,-40,-48,-107,-44,-41,-38,-64,-63,-107,-52,-38,-64,-57,-107,-63,-57,-38,-64,-41,-39,-48,-58,-101,-75],0xB5,false)
      firstN -= (Float(Int(firstN > 373491578.0 || firstN < -373491578.0 ? 45.0 : firstN)))
        label.font = .systemFont(ofSize: 16, weight: .regular)
        label.textColor = cornerMapMail([47,53,53,53,53,53,53,12],0xC,false).toColor
        label.numberOfLines = 0
        return label
    }()

    private let addMediaButton: UIButton = {
       var delayv: Bool = true
    var success6: String! = String(cString: [103,104,97,115,104,0], encoding: .utf8)!
      delayv = !success6.hasSuffix("\(delayv)")

        let button = UIButton(type: .custom)
   while (success6.count < 4 || !delayv) {
      success6 = "\((1 << (Swift.min(5, labs((delayv ? 3 : 4))))))"
      break
   }
        button.backgroundColor = .white
        button.setImage(cornerMapMail([-89,-80,-71,-80,-76,-90,-80,-118,-76,-79,-79,-43],0xD5,false).toImage, for: .normal)
        button.layer.cornerRadius = 16
        button.layer.masksToBounds = true
        return button
    }()

    private let costLabel: UILabel = {
       var offe: String! = String(cString: [99,111,108,117,109,110,0], encoding: .utf8)!
    _ = offe
   for _ in 0 ..< 2 {
      offe.append("\(offe.count << (Swift.min(labs(2), 5)))")
   }

        let label = UILabel()
        label.text = cornerMapMail([-85,-112,-110,-111,-99,-107,-105,-112,-103,-34,-102,-121,-112,-97,-109,-105,-99,-34,-114,-111,-115,-118,-105,-112,-103,-34,-99,-111,-115,-118,-115,-34,-49,-50,-34,-103,-111,-110,-102,-34,-99,-111,-105,-112,-115,-48,-2],0xFE,false)
        label.font = .systemFont(ofSize: 20, weight: .semibold)
        label.textColor = cornerMapMail([47,53,53,53,53,53,53,12],0xC,false).toColor
        label.textAlignment = .center
        label.numberOfLines = 0
        return label
    }()

    private let submitButton: UIButton = {
       var extension_gys: Double = 5.0
      extension_gys /= Swift.max((Double(Int(extension_gys > 311676669.0 || extension_gys < -311676669.0 ? 51.0 : extension_gys) | Int(extension_gys > 251889870.0 || extension_gys < -251889870.0 ? 26.0 : extension_gys))), 1)

        let button = UIButton(type: .custom)
        button.setImage(cornerMapMail([8,31,22,31,27,9,31,37,24,15,14,14,21,20,122],0x7A,false).toImage, for: .normal)
        return button
    }()
}


extension EPDataOptionController: UITextViewDelegate {

@discardableResult
 func uniformConstraintTrackBasicBlack(likerField: String!, navInput: Bool) -> [Any]! {
    var dirw: String! = String(cString: [99,102,115,116,114,101,97,109,0], encoding: .utf8)!
    var micU: Bool = true
    _ = micU
    var accessd: [Any]! = [String(cString: [108,106,112,101,103,0], encoding: .utf8)!]
      dirw.append("\(3)")
   while (!dirw.hasPrefix("\(accessd.count)")) {
      accessd.append(((micU ? 5 : 2) >> (Swift.min(labs(1), 2))))
      break
   }
      micU = accessd.count >= 46
   return accessd

}






    func textViewDidChange(_ textView: UITextView) {
       var outs: String! = String(cString: [105,110,116,101,114,108,97,99,101,100,0], encoding: .utf8)!
    _ = outs
       var sureI: Double = 2.0
      withUnsafeMutablePointer(to: &sureI) { pointer in
             _ = pointer.pointee
      }
       var four5: String! = String(cString: [112,101,111,112,108,101,0], encoding: .utf8)!
       var frame_f8: Int = 2
         sureI -= Double(frame_f8 - 1)
      repeat {
         sureI /= Swift.max(4, Double(1 << (Swift.min(labs(frame_f8), 5))))
         if 4069345.0 == sureI {
            break
         }
      } while (frame_f8 <= Int(sureI)) && (4069345.0 == sureI)
         sureI /= Swift.max(4, Double(3))
         four5.append("\(2)")
      repeat {
          var color2: Float = 4.0
          var infoM: String! = String(cString: [105,100,101,110,116,105,102,121,0], encoding: .utf8)!
          var codesk: [String: Any]! = [String(cString: [112,98,107,100,102,0], encoding: .utf8)!:String(cString: [99,111,110,116,97,99,116,0], encoding: .utf8)!, String(cString: [111,117,116,103,111,105,110,103,0], encoding: .utf8)!:String(cString: [97,116,114,97,99,112,108,117,115,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &codesk) { pointer in
    
         }
          var file4: Double = 2.0
          var labele: String! = String(cString: [116,114,101,101,99,111,100,101,114,0], encoding: .utf8)!
          _ = labele
         sureI += (Double(Int(sureI > 224478001.0 || sureI < -224478001.0 ? 18.0 : sureI)))
         color2 -= (Float(labele == (String(cString:[83,0], encoding: .utf8)!) ? labele.count : Int(sureI > 214052730.0 || sureI < -214052730.0 ? 69.0 : sureI)))
         infoM = "\(1 - labele.count)"
         codesk = ["\(codesk.count)": 3 / (Swift.max(5, codesk.count))]
         file4 += (Double((String(cString:[78,0], encoding: .utf8)!) == four5 ? frame_f8 : four5.count))
         if sureI == 4660299.0 {
            break
         }
      } while (four5.count <= (Int(sureI > 335965450.0 || sureI < -335965450.0 ? 11.0 : sureI))) && (sureI == 4660299.0)
       var calendar1: String! = String(cString: [99,97,116,101,103,111,114,121,0], encoding: .utf8)!
       var g_width1: String! = String(cString: [97,102,102,105,110,105,116,105,101,115,0], encoding: .utf8)!
         sureI /= Swift.max(Double(calendar1.count), 3)
         sureI += Double(3 ^ four5.count)
      while (g_width1.count < calendar1.count) {
         g_width1 = "\((Int(sureI > 211778689.0 || sureI < -211778689.0 ? 95.0 : sureI)))"
         break
      }
      outs = "\(frame_f8)"

        updatePlaceholderVisibility()
    }
}


extension EPDataOptionController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {

@discardableResult
 func revocationLengthPlayModelCount() -> String! {
    var o_title5: [Any]! = [String(cString: [98,108,105,110,107,0], encoding: .utf8)!, String(cString: [113,110,111,115,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &o_title5) { pointer in
    
   }
    var providerE: String! = String(cString: [115,99,97,108,101,109,111,100,101,0], encoding: .utf8)!
    var statX: String! = String(cString: [100,115,99,112,0], encoding: .utf8)!
      providerE.append("\(statX.count | 3)")
   while (3 >= (providerE.count / 2) || 2 >= (providerE.count / (Swift.max(1, o_title5.count)))) {
       var remainingO: Double = 5.0
       _ = remainingO
       var hour0: Double = 2.0
       var items: [String: Any]! = [String(cString: [100,116,115,109,0], encoding: .utf8)!:14, String(cString: [111,110,118,101,114,115,97,116,105,111,110,0], encoding: .utf8)!:59]
       var day_: [Any]! = [[87, 36]]
       _ = day_
       var nicknameA: String! = String(cString: [113,117,111,116,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &nicknameA) { pointer in
             _ = pointer.pointee
      }
      while (4 >= (Int(hour0 > 207428639.0 || hour0 < -207428639.0 ? 72.0 : hour0) * nicknameA.count) || 2.87 >= (5.90 * hour0)) {
         hour0 -= (Double(Int(remainingO > 195403441.0 || remainingO < -195403441.0 ? 43.0 : remainingO)))
         break
      }
         day_.append(day_.count)
         remainingO -= Double(nicknameA.count ^ day_.count)
      repeat {
         day_ = [1 - nicknameA.count]
         if 2215127 == day_.count {
            break
         }
      } while (2215127 == day_.count) && (remainingO < Double(day_.count))
      for _ in 0 ..< 2 {
          var anchorA: Double = 2.0
          var tappedJ: String! = String(cString: [115,111,100,105,115,99,111,110,110,101,99,116,0], encoding: .utf8)!
          var appX: Int = 5
         day_.append((Int(remainingO > 328753541.0 || remainingO < -328753541.0 ? 29.0 : remainingO) | tappedJ.count))
         anchorA /= Swift.max(Double(1), 2)
         appX >>= Swift.min(labs((Int(hour0 > 385929544.0 || hour0 < -385929544.0 ? 99.0 : hour0))), 5)
      }
         nicknameA.append("\(day_.count)")
      if 2.62 == (1.53 * remainingO) || 2 == (Int(remainingO > 371712667.0 || remainingO < -371712667.0 ? 55.0 : remainingO) * 4) {
         remainingO += (Double(day_.count << (Swift.min(4, labs(Int(hour0 > 36710000.0 || hour0 < -36710000.0 ? 72.0 : hour0))))))
      }
       var option5: Float = 3.0
       _ = option5
          var viewsX: Double = 3.0
         withUnsafeMutablePointer(to: &viewsX) { pointer in
    
         }
         option5 /= Swift.max(Float(day_.count & 2), 3)
         viewsX -= Double(items.count / (Swift.max(10, day_.count)))
      while ((3 + nicknameA.count) == 3) {
          var userm: Float = 0.0
         withUnsafeMutablePointer(to: &userm) { pointer in
    
         }
          var authorizationY: [Any]! = [60, 80]
          var storei: Double = 1.0
          var normalizedj: String! = String(cString: [109,98,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &normalizedj) { pointer in
                _ = pointer.pointee
         }
          var httpw: String! = String(cString: [97,108,97,99,0], encoding: .utf8)!
         option5 -= (Float((String(cString:[56,0], encoding: .utf8)!) == nicknameA ? nicknameA.count : normalizedj.count))
         userm += Float(authorizationY.count)
         authorizationY = [3]
         storei /= Swift.max(2, (Double(nicknameA == (String(cString:[51,0], encoding: .utf8)!) ? nicknameA.count : Int(option5 > 106671597.0 || option5 < -106671597.0 ? 53.0 : option5))))
         httpw = "\((Int(option5 > 32653249.0 || option5 < -32653249.0 ? 95.0 : option5)))"
         break
      }
      while (3.82 == option5) {
          var ownv: String! = String(cString: [101,120,116,110,0], encoding: .utf8)!
          var user8: Bool = false
          var totalP: Double = 0.0
          var likerF: String! = String(cString: [116,101,120,116,0], encoding: .utf8)!
         hour0 -= (Double(Int(totalP > 20425431.0 || totalP < -20425431.0 ? 29.0 : totalP)))
         ownv = "\((Int(option5 > 293599890.0 || option5 < -293599890.0 ? 60.0 : option5)))"
         user8 = (items.values.count % (Swift.max(3, likerF.count))) < 10
         likerF.append("\((items.values.count ^ Int(option5 > 26788582.0 || option5 < -26788582.0 ? 60.0 : option5)))")
         break
      }
         day_.append(1)
         option5 -= (Float(Int(remainingO > 270804942.0 || remainingO < -270804942.0 ? 90.0 : remainingO)))
         nicknameA.append("\((Int(hour0 > 190506220.0 || hour0 < -190506220.0 ? 37.0 : hour0)))")
         nicknameA = "\((nicknameA == (String(cString:[65,0], encoding: .utf8)!) ? nicknameA.count : day_.count))"
      o_title5.append(providerE.count * 1)
      break
   }
      providerE = "\(statX.count)"
   return providerE

}






    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
       var ownere: String! = String(cString: [99,111,117,110,116,115,0], encoding: .utf8)!
    var legacyD: Double = 0.0
      legacyD += (Double(ownere == (String(cString:[113,0], encoding: .utf8)!) ? ownere.count : Int(legacyD > 386247420.0 || legacyD < -386247420.0 ? 81.0 : legacyD)))
   while (3 < (ownere.count << (Swift.min(labs(3), 2))) || (legacyD / 2.10) < 3.68) {
      ownere = "\(((String(cString:[112,0], encoding: .utf8)!) == ownere ? ownere.count : Int(legacyD > 254120673.0 || legacyD < -254120673.0 ? 3.0 : legacyD)))"
      break
   }

        picker.dismiss(animated: true)
    }

@discardableResult
 func compactEachSceneGeneral(deliveredBlank: Bool, purchasingComponents: String!, statusOverlay: String!) -> Double {
    var unix8: String! = String(cString: [110,105,100,110,105,115,116,0], encoding: .utf8)!
    var layoutr: Bool = true
   repeat {
      layoutr = !layoutr
      if layoutr ? !layoutr : layoutr {
         break
      }
   } while (layoutr ? !layoutr : layoutr) && (layoutr)
   repeat {
       var emptyB: [String: Any]! = [String(cString: [98,119,103,116,0], encoding: .utf8)!:58, String(cString: [100,101,110,111,114,109,97,108,0], encoding: .utf8)!:87]
      withUnsafeMutablePointer(to: &emptyB) { pointer in
    
      }
       var stopv: Double = 1.0
         stopv -= Double(1)
         emptyB["\(stopv)"] = emptyB.values.count
         stopv -= Double(3 + emptyB.keys.count)
      while ((emptyB.keys.count * Int(stopv > 310411539.0 || stopv < -310411539.0 ? 82.0 : stopv)) >= 1) {
         stopv -= Double(3 >> (Swift.min(5, emptyB.values.count)))
         break
      }
         emptyB["\(stopv)"] = 1
         stopv -= Double(emptyB.keys.count * 1)
      unix8 = "\((Int(stopv > 282883309.0 || stopv < -282883309.0 ? 27.0 : stopv) * 1))"
      if unix8.count == 3099521 {
         break
      }
   } while (unix8.count == 3099521) && (layoutr && 1 <= unix8.count)
     let nickImg: Int = 27
     let normalizedPast: String! = String(cString: [101,120,116,101,114,110,0], encoding: .utf8)!
    var bitsLatm:Double = 0
    bitsLatm -= Double(nickImg)

    return bitsLatm

}






    func imagePickerController(
        _ picker: UIImagePickerController,
        didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]
    ) {
       var confirmi: Float = 2.0
   repeat {
       var sessionL: Float = 4.0
       _ = sessionL
       var disk7: String! = String(cString: [99,111,110,118,101,114,116,101,100,0], encoding: .utf8)!
       _ = disk7
       var thresholdR: [Any]! = [84, 0, 97]
       _ = thresholdR
       var collectionZ: [Any]! = [91, 79, 41]
       _ = collectionZ
       var placeholder9: Bool = false
      repeat {
         sessionL /= Swift.max(3, Float(3 + thresholdR.count))
         if sessionL == 4359938.0 {
            break
         }
      } while (sessionL == 4359938.0) && ((sessionL * 3.32) <= 2.52 && 1 <= (5 + thresholdR.count))
      repeat {
         collectionZ = [(disk7 == (String(cString:[83,0], encoding: .utf8)!) ? Int(sessionL > 170977678.0 || sessionL < -170977678.0 ? 8.0 : sessionL) : disk7.count)]
         if collectionZ.count == 4566630 {
            break
         }
      } while (collectionZ.count == 4566630) && ((collectionZ.count - 2) == 4)
      for _ in 0 ..< 3 {
         placeholder9 = 67 == collectionZ.count
      }
      while (2.84 < (5.43 * sessionL) && 4.48 < (5.43 * sessionL)) {
          var mapY: Int = 3
          _ = mapY
          var window_jX: Double = 3.0
         withUnsafeMutablePointer(to: &window_jX) { pointer in
    
         }
          var pixelV: Int = 2
          var local_vf_: String! = String(cString: [100,101,99,105,109,97,108,115,0], encoding: .utf8)!
         disk7 = "\(3)"
         mapY -= ((String(cString:[82,0], encoding: .utf8)!) == local_vf_ ? local_vf_.count : mapY)
         window_jX -= (Double(Int(window_jX > 286033706.0 || window_jX < -286033706.0 ? 31.0 : window_jX)))
         pixelV |= 3
         break
      }
      while ((thresholdR.count * collectionZ.count) < 2 && (collectionZ.count * 2) < 5) {
         collectionZ = [collectionZ.count]
         break
      }
      while (disk7.count < 1) {
          var float_jp: String! = String(cString: [98,101,110,105,103,110,0], encoding: .utf8)!
          var commentu: Bool = true
          var localizationE: String! = String(cString: [114,115,112,0], encoding: .utf8)!
         placeholder9 = commentu
         float_jp = "\((Int(sessionL > 1970487.0 || sessionL < -1970487.0 ? 97.0 : sessionL) / 2))"
         localizationE.append("\((2 - (placeholder9 ? 4 : 1)))")
         break
      }
      while (2.73 < sessionL) {
         sessionL -= (Float(Int(sessionL > 302400234.0 || sessionL < -302400234.0 ? 59.0 : sessionL) << (Swift.min(2, labs(3)))))
         break
      }
      for _ in 0 ..< 3 {
         thresholdR = [1]
      }
          var groupi: [Any]! = [24]
         placeholder9 = (thresholdR.contains { $0 as? Bool == placeholder9 })
         groupi = [((placeholder9 ? 2 : 2) / (Swift.max(2, 1)))]
         placeholder9 = placeholder9 && collectionZ.count <= 51
         sessionL /= Swift.max(5, Float(thresholdR.count % (Swift.max(1, 5))))
         sessionL += (Float(3 % (Swift.max(7, (placeholder9 ? 2 : 2)))))
      while (!placeholder9) {
         placeholder9 = sessionL <= 58.6 || 18 <= collectionZ.count
         break
      }
         sessionL /= Swift.max(5, Float(3))
      for _ in 0 ..< 3 {
         placeholder9 = 88.13 > sessionL && 92 > disk7.count
      }
      confirmi += (Float((placeholder9 ? 5 : 4) << (Swift.min(collectionZ.count, 4))))
      if 2110671.0 == confirmi {
         break
      }
   } while (confirmi < 3.95) && (2110671.0 == confirmi)

        picker.dismiss(animated: true)

        if let mediaURL = info[.mediaURL] as? URL {
            pickedMedia = .video(mediaURL)
            if let thumb = SS_BundleResourceMedia.videoFirstFrame(url: mediaURL) {
                applyMediaPreview(image: thumb)
            } else {
                addMediaButton.setImage(cornerMapMail([-89,-80,-71,-80,-76,-90,-80,-118,-76,-79,-79,-43],0xD5,false).toImage, for: .normal)
            }
            return
        }

        if let image = (info[.editedImage] ?? info[.originalImage]) as? UIImage {
            pickedMedia = .image(image)
            applyMediaPreview(image: image)
        }
    }
}
