
import Foundation

import UIKit

enum SettingRowType: CaseIterable {
    case contact
    case policy
    case guide
    case blacklist
    case logout
    case deleteAccount

    var imageName: String {
       var likedC: Double = 0.0
    var layoutK: String! = String(cString: [117,112,109,105,120,0], encoding: .utf8)!
      layoutK = "\(2)"

        switch self {
        case .contact: return cornerMapMail([26,12,29,29,0,7,14,54,10,6,7,29,8,10,29,105],0x69,false)
        case .policy: return cornerMapMail([18,4,21,21,8,15,6,62,17,14,13,8,2,24,97],0x61,false)
        case .guide: return cornerMapMail([25,15,30,30,3,4,13,53,13,31,3,14,15,106],0x6A,false)
        case .blacklist: return cornerMapMail([-9,-31,-16,-16,-19,-22,-29,-37,-26,-24,-27,-25,-17,-124],0x84,false)
        case .logout: return cornerMapMail([-85,-67,-84,-84,-79,-74,-65,-121,-73,-83,-84,-40],0xD8,false)
        case .deleteAccount: return cornerMapMail([-111,-121,-106,-106,-117,-116,-123,-67,-122,-121,-114,-30],0xE2,false)
        }
      layoutK = "\(1)"
   for _ in 0 ..< 3 {
      likedC -= (Double(2 | Int(likedC > 63441231.0 || likedC < -63441231.0 ? 91.0 : likedC)))
   }
    }
}

class EPResourceController: EPFriendChatController {

    private enum Layout {
        static let horizontalInset: CGFloat = 16
        static let tableTopSpacing: CGFloat = 24
        static let rowSpacing: CGFloat = 14
        
        static let rowImageHeight: CGFloat = 67
        static var rowHeight: CGFloat {
       var sendq: String! = String(cString: [110,101,117,116,114,97,108,0], encoding: .utf8)!
   repeat {
      sendq = "\(sendq.count)"
      if sendq == (String(cString:[111,114,97,54,0], encoding: .utf8)!) {
         break
      }
   } while (sendq == (String(cString:[111,114,97,54,0], encoding: .utf8)!)) && (2 == sendq.count)
     return rowImageHeight + rowSpacing }
    }

    private var tableHeightConstraint: Constraint?

    init() {
        super.init(nibName: nil, bundle: nil)
        hidesBottomBarWhenPushed = true
    }

    @available(*, unavailable)
    required init?(coder: NSCoder) {
        fatalError(cornerMapMail([29,26,29,0,92,23,27,16,17,6,78,93,84,28,21,7,84,26,27,0,84,22,17,17,26,84,29,25,4,24,17,25,17,26,0,17,16,116],0x74,false))
    }

    private let rows = SettingRowType.allCases

@discardableResult
 func addSecurityDrawDomainNetwork(avatarsAuthor: Double) -> Int {
    var using_3m: String! = String(cString: [112,111,108,105,99,121,0], encoding: .utf8)!
    var avatark: String! = String(cString: [109,112,101,103,97,117,100,105,111,100,101,99,104,101,97,100,101,114,0], encoding: .utf8)!
    var loado: Int = 2
   withUnsafeMutablePointer(to: &loado) { pointer in
    
   }
       var availablea: String! = String(cString: [115,104,97,107,101,0], encoding: .utf8)!
       var lockedP: String! = String(cString: [104,109,97,99,0], encoding: .utf8)!
       var formattert: String! = String(cString: [101,108,108,105,112,116,105,99,0], encoding: .utf8)!
      if 2 == formattert.count {
          var kindu: Bool = true
          _ = kindu
          var memberv: String! = String(cString: [115,112,97,114,115,101,0], encoding: .utf8)!
          _ = memberv
          var groupf: [String: Any]! = [String(cString: [110,101,99,101,115,115,97,114,121,0], encoding: .utf8)!:String(cString: [98,108,97,109,101,100,0], encoding: .utf8)!, String(cString: [99,97,114,111,117,115,101,108,0], encoding: .utf8)!:String(cString: [115,105,122,101,98,105,116,114,97,116,101,0], encoding: .utf8)!, String(cString: [105,110,116,101,114,120,121,0], encoding: .utf8)!:String(cString: [115,97,108,116,101,100,0], encoding: .utf8)!]
          var picker9: String! = String(cString: [115,101,103,102,101,97,116,117,114,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &picker9) { pointer in
    
         }
         availablea.append("\(formattert.count % (Swift.max(picker9.count, 2)))")
         kindu = (formattert.count >> (Swift.min(2, groupf.keys.count))) >= 7
         memberv = "\(picker9.count)"
         groupf[lockedP] = groupf.values.count
      }
          var cosx: Double = 5.0
          var confirmO: Bool = true
          var color7: Double = 0.0
         lockedP = "\((Int(cosx > 186160939.0 || cosx < -186160939.0 ? 3.0 : cosx) % (Swift.max(formattert.count, 7))))"
         confirmO = (Double(lockedP.count) + color7) <= 100.36
         color7 /= Swift.max(5, (Double(Int(color7 > 382208874.0 || color7 < -382208874.0 ? 44.0 : color7) & 1)))
      while (availablea == String(cString:[107,0], encoding: .utf8)!) {
         lockedP.append("\(lockedP.count)")
         break
      }
       var amountL: [Any]! = [47, 48]
      withUnsafeMutablePointer(to: &amountL) { pointer in
             _ = pointer.pointee
      }
       var respondern: [Any]! = [false]
       _ = respondern
          var default_wo: String! = String(cString: [112,117,116,105,110,116,95,105,95,52,56,0], encoding: .utf8)!
          var using_mj_: Float = 4.0
         formattert.append("\(default_wo.count)")
         using_mj_ -= Float(availablea.count + 1)
         amountL.append(amountL.count)
      repeat {
         amountL.append(amountL.count | 1)
         if amountL.count == 4032923 {
            break
         }
      } while (amountL.count == 4032923) && ((respondern.count & amountL.count) < 4 && 3 < (respondern.count & 4))
       var micL: Bool = false
          var validx: Double = 5.0
          var picker5: Int = 4
          var insufficientQ: [Any]! = [87, 49, 81]
         withUnsafeMutablePointer(to: &insufficientQ) { pointer in
    
         }
         micL = (respondern.contains { $0 as? Bool == micL })
         validx -= (Double(lockedP == (String(cString:[107,0], encoding: .utf8)!) ? amountL.count : lockedP.count))
         picker5 &= 3
         insufficientQ = [1]
      avatark.append("\(3 % (Swift.max(4, using_3m.count)))")
      using_3m.append("\(loado * using_3m.count)")
      loado -= using_3m.count
   return loado

}





    override func viewDidLoad() {
       var secondg: String! = String(cString: [115,101,99,116,105,110,115,0], encoding: .utf8)!
      secondg.append("\(secondg.count & secondg.count)")

        super.viewDidLoad()

        setupUI()
        setupConstraints()
        setupEvents()
    }

    override func viewDidLayoutSubviews() {
       var inset3: [Any]! = [72, 32]
   withUnsafeMutablePointer(to: &inset3) { pointer in
    
   }
   if 2 > (inset3.count ^ inset3.count) {
       var items1: Int = 3
       var confirmj: [Any]! = [99, 8]
       _ = confirmj
       var min_83a: [Any]! = [4, 82, 54]
      if confirmj.count == items1 {
         confirmj.append(confirmj.count - min_83a.count)
      }
      repeat {
         confirmj = [confirmj.count]
         if 2119593 == confirmj.count {
            break
         }
      } while (2119593 == confirmj.count) && (3 > (min_83a.count % (Swift.max(5, 9))) || 5 > (confirmj.count % (Swift.max(min_83a.count, 5))))
      inset3 = [inset3.count]
   }

        super.viewDidLayoutSubviews()
        updateTableMetricsIfNeeded()
    }

@discardableResult
 func globalLayoutDrawNoneManagerString(amountAccess: Double, timestampPosts: Bool) -> String! {
    var items_: Int = 3
    var avatarn: Float = 0.0
    var openz: String! = String(cString: [100,105,115,116,114,105,98,117,116,105,111,110,115,0], encoding: .utf8)!
       var personm: String! = String(cString: [98,97,115,101,100,0], encoding: .utf8)!
       var largeU: Double = 4.0
       var workP: Double = 1.0
         workP -= (Double(1 & Int(largeU > 6284367.0 || largeU < -6284367.0 ? 40.0 : largeU)))
      for _ in 0 ..< 2 {
         personm = "\((Int(largeU > 41376676.0 || largeU < -41376676.0 ? 13.0 : largeU) ^ Int(workP > 360768761.0 || workP < -360768761.0 ? 30.0 : workP)))"
      }
      repeat {
         workP += (Double(Int(largeU > 215506795.0 || largeU < -215506795.0 ? 20.0 : largeU) / (Swift.max(personm.count, 2))))
         if 3098264.0 == workP {
            break
         }
      } while (3098264.0 == workP) && (3.66 > (Double(Int(largeU) + 5)))
      while (personm.hasSuffix("\(largeU)")) {
         largeU /= Swift.max(4, (Double(3 >> (Swift.min(5, labs(Int(workP > 285304133.0 || workP < -285304133.0 ? 51.0 : workP)))))))
         break
      }
      if (Int(largeU > 351271970.0 || largeU < -351271970.0 ? 85.0 : largeU) - personm.count) > 1 || (1 - personm.count) > 3 {
         personm = "\((personm.count * Int(workP > 113234952.0 || workP < -113234952.0 ? 95.0 : workP)))"
      }
         workP -= Double(2)
         workP -= (Double(Int(largeU > 191261255.0 || largeU < -191261255.0 ? 81.0 : largeU) >> (Swift.min(labs(Int(workP > 100032068.0 || workP < -100032068.0 ? 1.0 : workP)), 3))))
         personm = "\((3 * Int(largeU > 120976028.0 || largeU < -120976028.0 ? 91.0 : largeU)))"
         largeU -= (Double(2 + Int(largeU > 10977867.0 || largeU < -10977867.0 ? 7.0 : largeU)))
      items_ >>= Swift.min(labs((Int(largeU > 47870354.0 || largeU < -47870354.0 ? 27.0 : largeU) & personm.count)), 5)
      openz.append("\(((String(cString:[90,0], encoding: .utf8)!) == openz ? openz.count : Int(avatarn > 321422961.0 || avatarn < -321422961.0 ? 44.0 : avatarn)))")
   return openz

}





    private func updateTableMetricsIfNeeded() {
       var seedS: Double = 0.0
   for _ in 0 ..< 2 {
      seedS += (Double(Int(seedS > 228491956.0 || seedS < -228491956.0 ? 25.0 : seedS)))
   }

        let scroll = view.bounds.width - Layout.horizontalInset * 2
        guard scroll > 0 else { return }

        let mode = Self.rowHeight(forTableWidth: scroll)
        guard tableView.rowHeight != mode else { return }

        tableView.rowHeight = mode
        tableView.estimatedRowHeight = mode
        tableHeightConstraint?.update(offset: mode * CGFloat(rows.count))
        tableView.reloadData()
    }

@discardableResult
static func tintBasicCheckThanAppearAspect() -> String! {
    var collectionX: [Any]! = [String(cString: [114,101,102,101,114,101,110,99,101,100,0], encoding: .utf8)!, String(cString: [111,117,98,108,101,0], encoding: .utf8)!, String(cString: [103,114,101,101,116,105,110,103,0], encoding: .utf8)!]
    var bubbleo: Float = 5.0
    var profileE: String! = String(cString: [99,104,107,108,105,115,116,0], encoding: .utf8)!
      profileE.append("\(2 | collectionX.count)")
   if (bubbleo * 5.35) > 4.45 && 4.85 > (bubbleo * 5.35) {
      collectionX.append(profileE.count)
   }
   for _ in 0 ..< 1 {
       var conversationD: String! = String(cString: [101,120,112,114,101,115,115,0], encoding: .utf8)!
       var hourR: Double = 4.0
      withUnsafeMutablePointer(to: &hourR) { pointer in
    
      }
       var nanok: [Any]! = [92, 56, 98]
       var saveF: Double = 4.0
      withUnsafeMutablePointer(to: &saveF) { pointer in
             _ = pointer.pointee
      }
      while (1.83 < (saveF - 1.29)) {
         saveF -= Double(3)
         break
      }
       var model2: Int = 2
       var updatedO: Int = 2
       var labeln: String! = String(cString: [99,108,99,112,0], encoding: .utf8)!
       var min_u1o: Int = 1
       var hoursX: Int = 4
      if 4 == (min_u1o % (Swift.max(labeln.count, 6))) {
         min_u1o >>= Swift.min(labs(hoursX), 1)
      }
      for _ in 0 ..< 1 {
         nanok.append(labeln.count * 1)
      }
      for _ in 0 ..< 3 {
         hourR += Double(3)
      }
         hoursX %= Swift.max(2, (labeln == (String(cString:[106,0], encoding: .utf8)!) ? labeln.count : nanok.count))
         min_u1o += (3 >> (Swift.min(labs(Int(saveF > 135156692.0 || saveF < -135156692.0 ? 74.0 : saveF)), 4)))
         saveF -= Double(conversationD.count)
         saveF -= (Double(conversationD == (String(cString:[82,0], encoding: .utf8)!) ? nanok.count : conversationD.count))
      for _ in 0 ..< 1 {
          var friend_t5q: String! = String(cString: [115,102,116,112,0], encoding: .utf8)!
          var min_67: String! = String(cString: [114,101,116,97,105,110,0], encoding: .utf8)!
          var report_: [Any]! = [[59, 68]]
          var inewsx: String! = String(cString: [112,111,105,110,116,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &inewsx) { pointer in
                _ = pointer.pointee
         }
         hoursX -= friend_t5q.count << (Swift.min(labs(1), 1))
         min_67 = "\(friend_t5q.count)"
         report_.append(inewsx.count & min_67.count)
         inewsx = "\((Int(saveF > 131139825.0 || saveF < -131139825.0 ? 8.0 : saveF) & 2))"
      }
         model2 -= updatedO
         updatedO += conversationD.count
      bubbleo -= Float(profileE.count)
   }
   return profileE

}





    private static func rowHeight(forTableWidth tableWidth: CGFloat) -> CGFloat {
       var controlx: String! = String(cString: [116,105,110,116,101,114,108,97,99,101,0], encoding: .utf8)!
   if !controlx.hasPrefix(controlx) {
      controlx.append("\(controlx.count)")
   }

        guard let image = SettingRowType.contact.imageName.toImage, image.size.width > 0 else {
            return Layout.rowHeight
        }
        let pending = tableWidth * image.size.height / image.size.width
        return pending + Layout.rowSpacing
    }

@discardableResult
 func verbalTwoCornerLocaleUnknownYearView(titleEmpty: String!, configurationPick: Bool, mutualMode: String!) -> UIView! {
    var tomorrowe: String! = String(cString: [98,97,99,107,98,114,111,117,110,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &tomorrowe) { pointer in
    
   }
    var submitK: String! = String(cString: [101,118,101,114,0], encoding: .utf8)!
      submitK = "\(submitK.count - 3)"
   if submitK.hasSuffix(tomorrowe) {
      submitK.append("\(submitK.count & tomorrowe.count)")
   }
     var hourNearest: Double = 39.0
     let orphanTomorrow: String! = String(cString: [109,98,101,100,116,108,115,0], encoding: .utf8)!
     let viewFormat: [Any]! = [String(cString: [107,101,114,110,101,114,0], encoding: .utf8)!]
    var oneofThreestateSign: UIView! = UIView()
    oneofThreestateSign.frame = CGRect(x: 142, y: 89, width: 0, height: 0)
    oneofThreestateSign.alpha = 0.3;
    oneofThreestateSign.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return oneofThreestateSign

}





    private func setupUI() {
       var workdayd: Float = 2.0
    _ = workdayd
    var reviseM: [Any]! = [25, 41]
   repeat {
      reviseM.append(reviseM.count)
      if reviseM.count == 3437433 {
         break
      }
   } while (4 == reviseM.count) && (reviseM.count == 3437433)

   if (1 & reviseM.count) >= 2 && (workdayd + 1.33) >= 5.39 {
      reviseM = [(1 | Int(workdayd > 83845512.0 || workdayd < -83845512.0 ? 35.0 : workdayd))]
   }
        view.addSubview(backButton)
        view.addSubview(titleView)
        view.addSubview(tableView)
    }

@discardableResult
 func bottomLocationStringHomeScrollView(achievementManager: Double, sessionPast: Int, workdayInitials: String!) -> UIScrollView! {
    var decodeN: Float = 0.0
    var author8: [Any]! = [37, 13]
   for _ in 0 ..< 3 {
      decodeN -= Float(2)
   }
       var refresh3: [String: Any]! = [String(cString: [101,99,109,117,108,116,0], encoding: .utf8)!:7, String(cString: [121,112,114,101,100,105,99,116,105,111,110,0], encoding: .utf8)!:19, String(cString: [102,117,116,101,120,0], encoding: .utf8)!:37]
      withUnsafeMutablePointer(to: &refresh3) { pointer in
    
      }
       var dotz: Float = 4.0
      for _ in 0 ..< 3 {
         refresh3["\(dotz)"] = (Int(dotz > 203745642.0 || dotz < -203745642.0 ? 88.0 : dotz))
      }
         refresh3["\(dotz)"] = 1 + refresh3.values.count
          var users3: String! = String(cString: [115,116,117,100,105,111,0], encoding: .utf8)!
         refresh3["\(dotz)"] = (users3 == (String(cString:[90,0], encoding: .utf8)!) ? Int(dotz > 154253552.0 || dotz < -154253552.0 ? 25.0 : dotz) : users3.count)
         dotz /= Swift.max(1, (Float(Int(dotz > 374327269.0 || dotz < -374327269.0 ? 81.0 : dotz))))
         dotz += (Float(Int(dotz > 342171117.0 || dotz < -342171117.0 ? 18.0 : dotz)))
      while ((5 ^ refresh3.values.count) < 1 || (refresh3.values.count - Int(dotz > 198075359.0 || dotz < -198075359.0 ? 49.0 : dotz)) < 5) {
         dotz += (Float(refresh3.count - Int(dotz > 179211088.0 || dotz < -179211088.0 ? 28.0 : dotz)))
         break
      }
      decodeN /= Swift.max(2, (Float(refresh3.count + Int(decodeN > 130203203.0 || decodeN < -130203203.0 ? 27.0 : decodeN))))
      author8.append(author8.count << (Swift.min(labs(1), 4)))
     let voiceSelected: String! = String(cString: [113,115,111,114,116,0], encoding: .utf8)!
     var activePerson: Float = 52.0
     var scriptsLocal_1: UIView! = UIView(frame:CGRect(x: 208, y: 402, width: 0, height: 0))
     let mappedRange: UILabel! = UILabel(frame:CGRect.zero)
    var wallStrengthSegtree:UIScrollView! = UIScrollView(frame:CGRect.zero)
    wallStrengthSegtree.frame = CGRect(x: 141, y: 116, width: 0, height: 0)
    wallStrengthSegtree.alpha = 0.7;
    wallStrengthSegtree.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    wallStrengthSegtree.showsVerticalScrollIndicator = true
    wallStrengthSegtree.showsHorizontalScrollIndicator = true
    wallStrengthSegtree.delegate = nil
    wallStrengthSegtree.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    wallStrengthSegtree.alwaysBounceVertical = false
    wallStrengthSegtree.alwaysBounceHorizontal = false
    scriptsLocal_1.alpha = 0.1;
    scriptsLocal_1.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    scriptsLocal_1.frame = CGRect(x: 85, y: 5, width: 0, height: 0)
    
    wallStrengthSegtree.addSubview(scriptsLocal_1)
    mappedRange.frame = CGRect(x: 262, y: 216, width: 0, height: 0)
    mappedRange.alpha = 1.0;
    mappedRange.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    mappedRange.font = UIFont.systemFont(ofSize:20)
    mappedRange.text = ""
    mappedRange.textColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    mappedRange.textAlignment = .left
    
    wallStrengthSegtree.addSubview(mappedRange)

    
    return wallStrengthSegtree

}





    private func setupConstraints() {
       var mutualq: String! = String(cString: [118,97,108,117,101,0], encoding: .utf8)!
    var requestu: Float = 2.0
      requestu *= (Float(mutualq.count / (Swift.max(4, Int(requestu > 153138224.0 || requestu < -153138224.0 ? 72.0 : requestu)))))

        backButton.snp.makeConstraints { make in
   for _ in 0 ..< 2 {
       var description_rgB: String! = String(cString: [112,97,115,115,112,111,114,116,0], encoding: .utf8)!
       _ = description_rgB
       var coins6: Int = 0
       var codesO: String! = String(cString: [101,120,99,108,117,100,105,110,103,0], encoding: .utf8)!
       var delete__pU: String! = String(cString: [103,114,97,100,105,101,110,116,0], encoding: .utf8)!
       _ = delete__pU
       var changeh: Bool = true
      while (delete__pU.contains("\(changeh)")) {
         delete__pU = "\(codesO.count)"
         break
      }
         codesO.append("\(3)")
          var fromw: String! = String(cString: [109,117,108,116,105,99,111,100,101,99,0], encoding: .utf8)!
         delete__pU.append("\(((changeh ? 1 : 3)))")
         fromw.append("\(fromw.count)")
      while (!description_rgB.contains("\(coins6)")) {
         coins6 &= codesO.count << (Swift.min(labs(3), 4))
         break
      }
      if (coins6 * codesO.count) <= 5 && 4 <= (5 * coins6) {
         coins6 %= Swift.max((description_rgB == (String(cString:[65,0], encoding: .utf8)!) ? description_rgB.count : coins6), 4)
      }
          var formu: String! = String(cString: [107,97,116,0], encoding: .utf8)!
         delete__pU.append("\((codesO == (String(cString:[68,0], encoding: .utf8)!) ? codesO.count : coins6))")
         formu = "\(2 / (Swift.max(2, delete__pU.count)))"
         delete__pU.append("\(coins6)")
      repeat {
         codesO = "\(delete__pU.count + 3)"
         if (String(cString:[99,114,54,56,0], encoding: .utf8)!) == codesO {
            break
         }
      } while (codesO.contains("\(changeh)")) && ((String(cString:[99,114,54,56,0], encoding: .utf8)!) == codesO)
      repeat {
         codesO.append("\(1 * coins6)")
         if codesO.count == 4059716 {
            break
         }
      } while (codesO.count > 1 && 1 > delete__pU.count) && (codesO.count == 4059716)
       var tomorrowh: Bool = false
         coins6 %= Swift.max(1, 2 * description_rgB.count)
          var vinitialsw: Double = 0.0
          var localizatione: Double = 4.0
          _ = localizatione
         tomorrowh = tomorrowh && 62.34 < vinitialsw
         localizatione += Double(codesO.count)
         changeh = codesO.count < 66
      repeat {
          var sheetV: Double = 4.0
          var attributesg: String! = String(cString: [112,97,99,107,105,110,103,0], encoding: .utf8)!
          var cornerY: String! = String(cString: [108,105,98,116,103,118,111,105,112,0], encoding: .utf8)!
          var groupk: [String: Any]! = [String(cString: [110,117,109,101,114,97,108,0], encoding: .utf8)!:33, String(cString: [117,100,116,97,0], encoding: .utf8)!:8]
          var detaill: [String: Any]! = [String(cString: [115,121,110,116,104,101,115,105,122,101,114,0], encoding: .utf8)!:65]
         withUnsafeMutablePointer(to: &detaill) { pointer in
    
         }
         codesO.append("\(groupk.count ^ 3)")
         sheetV /= Swift.max((Double((tomorrowh ? 2 : 2))), 1)
         attributesg = "\(3 & coins6)"
         cornerY.append("\(3)")
         detaill = [delete__pU: (Int(sheetV > 88463815.0 || sheetV < -88463815.0 ? 50.0 : sheetV))]
         if codesO.count == 4084767 {
            break
         }
      } while (!changeh || codesO.count == 2) && (codesO.count == 4084767)
      while (description_rgB.count <= 4) {
         changeh = changeh && tomorrowh
         break
      }
      mutualq.append("\(codesO.count / (Swift.max(2, 7)))")
   }
            make.leading.equalToSuperview().offset(10)
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.size.equalTo(44)
        }

        titleView.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.centerY.equalTo(backButton)
        }

        tableView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(Layout.horizontalInset)
            make.top.equalTo(titleView.snp.bottom).offset(Layout.tableTopSpacing)
            tableHeightConstraint = make.height.equalTo(Layout.rowHeight * CGFloat(rows.count)).constraint
        }
    }

    private func setupEvents() {
       var liker5: String! = String(cString: [108,115,104,105,102,116,0], encoding: .utf8)!
    var locked2: String! = String(cString: [103,101,116,120,118,97,114,0], encoding: .utf8)!
       var navx: [String: Any]! = [String(cString: [116,105,108,101,109,107,97,0], encoding: .utf8)!:34, String(cString: [99,97,115,116,0], encoding: .utf8)!:9, String(cString: [115,116,97,114,0], encoding: .utf8)!:31]
       var int__gw: String! = String(cString: [102,105,108,101,104,101,97,100,101,114,0], encoding: .utf8)!
       _ = int__gw
       var emptys: String! = String(cString: [102,97,113,0], encoding: .utf8)!
      while (int__gw.count > emptys.count) {
         int__gw = "\(emptys.count)"
         break
      }
      repeat {
          var d_imageR: String! = String(cString: [97,110,115,119,101,114,0], encoding: .utf8)!
          var voiceE: Float = 3.0
         emptys = "\(emptys.count)"
         d_imageR.append("\(int__gw.count)")
         voiceE -= Float(int__gw.count)
         if 907106 == emptys.count {
            break
         }
      } while (1 > (navx.values.count * 5)) && (907106 == emptys.count)
      while (3 > (5 - navx.count) || 5 > (5 - emptys.count)) {
          var dirV: [Any]! = [44, 54]
          var updatesM: String! = String(cString: [119,101,98,109,100,101,99,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &updatesM) { pointer in
    
         }
          var didB: String! = String(cString: [97,112,112,115,0], encoding: .utf8)!
         navx = ["\(dirV.count)": dirV.count - didB.count]
         updatesM = "\((updatesM == (String(cString:[88,0], encoding: .utf8)!) ? didB.count : updatesM.count))"
         break
      }
          var reportX: [Any]! = [String(cString: [108,111,111,112,98,97,99,107,0], encoding: .utf8)!, String(cString: [116,101,109,112,108,97,116,101,100,0], encoding: .utf8)!]
          var pushx: [Any]! = [49, 30, 18]
         navx = ["\(navx.values.count)": navx.keys.count]
         reportX = [pushx.count >> (Swift.min(labs(3), 5))]
         pushx.append(2 ^ emptys.count)
          var fani: Float = 2.0
          _ = fani
         int__gw.append("\((1 ^ Int(fani > 177359221.0 || fani < -177359221.0 ? 82.0 : fani)))")
       var push3: String! = String(cString: [101,108,101,109,101,110,116,119,105,115,101,0], encoding: .utf8)!
       var request5: String! = String(cString: [115,108,105,99,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &request5) { pointer in
             _ = pointer.pointee
      }
          var regexx: Bool = false
          var remainingG: Bool = true
          var rootc: [Any]! = [10.0]
         navx[request5] = 1
         regexx = int__gw.count > 22
         remainingG = push3.count <= request5.count
         rootc = [request5.count * rootc.count]
      repeat {
         navx[push3] = push3.count / 2
         if 3651185 == navx.count {
            break
         }
      } while (3651185 == navx.count) && ((navx.keys.count ^ 1) > 1)
      while (emptys.hasSuffix(int__gw)) {
          var unreadA: String! = String(cString: [108,105,98,0], encoding: .utf8)!
          _ = unreadA
          var cornera: Float = 2.0
          var bundleA: [Any]! = [75, 76, 85]
         withUnsafeMutablePointer(to: &bundleA) { pointer in
                _ = pointer.pointee
         }
          var frame_nH: Bool = false
          _ = frame_nH
         emptys.append("\(3 & int__gw.count)")
         unreadA = "\(3)"
         cornera /= Swift.max((Float((frame_nH ? 1 : 1) / 3)), 5)
         bundleA = [push3.count]
         break
      }
      locked2 = "\(int__gw.count << (Swift.min(labs(1), 1)))"
   for _ in 0 ..< 1 {
       var camerav: String! = String(cString: [119,104,105,116,101,115,0], encoding: .utf8)!
       var postsC: String! = String(cString: [99,104,97,110,110,101,108,109,97,112,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         camerav.append("\(2)")
      }
      while (camerav == String(cString:[77,0], encoding: .utf8)!) {
         postsC = "\(postsC.count + 2)"
         break
      }
      liker5.append("\(liker5.count)")
   }

        backButton.addTarget(self, action: #selector(clickBackButton), for: .touchUpInside)
    }

@discardableResult
 func flatRunningOutsideSectionImageView(weekDay: [Any]!, outfitAnimation: String!, timestampRoot: [String: Any]!) -> UIImageView! {
    var interitemK: String! = String(cString: [112,114,105,110,116,97,98,108,101,0], encoding: .utf8)!
    var modeA: Int = 4
      interitemK = "\(1)"
   repeat {
      modeA >>= Swift.min(3, labs(modeA))
      if 4894045 == modeA {
         break
      }
   } while (modeA < modeA) && (4894045 == modeA)
     let fallbackForm: UIButton! = UIButton(frame:CGRect.zero)
     let fanYears: UIButton! = UIButton(frame:CGRect.zero)
     let rendererDeleted: [String: Any]! = [String(cString: [99,114,97,115,104,101,100,0], encoding: .utf8)!:79, String(cString: [110,101,119,108,105,110,101,115,0], encoding: .utf8)!:14]
    var beganDiscardedDivmod: UIImageView! = UIImageView(frame:CGRect(x: 52, y: 220, width: 0, height: 0))
    beganDiscardedDivmod.animationRepeatCount = 8
    beganDiscardedDivmod.image = UIImage(named:String(cString: [102,111,117,114,0], encoding: .utf8)!)
    beganDiscardedDivmod.contentMode = .scaleAspectFit
    beganDiscardedDivmod.frame = CGRect(x: 171, y: 314, width: 0, height: 0)
    beganDiscardedDivmod.alpha = 1.0;
    beganDiscardedDivmod.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    fallbackForm.frame = CGRect(x: 188, y: 291, width: 0, height: 0)
    fallbackForm.alpha = 0.7;
    fallbackForm.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    fallbackForm.titleLabel?.font = UIFont.systemFont(ofSize:15)
    fallbackForm.setImage(UIImage(named:String(cString: [112,97,110,101,108,0], encoding: .utf8)!), for: .normal)
    fallbackForm.setTitle("", for: .normal)
    fallbackForm.setBackgroundImage(UIImage(named:String(cString: [114,101,115,116,111,114,101,0], encoding: .utf8)!), for: .normal)
    
    fanYears.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    fanYears.alpha = 0.1
    fanYears.frame = CGRect(x: 313, y: 260, width: 0, height: 0)
    fanYears.titleLabel?.font = UIFont.systemFont(ofSize:14)
    fanYears.setImage(UIImage(named:String(cString: [100,105,100,0], encoding: .utf8)!), for: .normal)
    fanYears.setTitle("", for: .normal)
    fanYears.setBackgroundImage(UIImage(named:String(cString: [119,101,108,99,111,109,101,0], encoding: .utf8)!), for: .normal)
    

    
    return beganDiscardedDivmod

}





    @objc private func clickBackButton() {
       var logino: Bool = false
      logino = (!logino ? logino : !logino)

        navigationController?.popViewController(animated: true)
    }

    private func presentDeleteAccountConfirmation() {
       var todaye: Bool = true
   while (!todaye) {
      todaye = todaye && todaye
      break
   }

        let old = UIAlertController(
            title: cornerMapMail([-117,-86,-93,-86,-69,-86,-17,-114,-84,-84,-96,-70,-95,-69,-49],0xCF,false),
            message: cornerMapMail([123,71,70,92,15,88,70,67,67,15,95,74,93,66,78,65,74,65,91,67,86,15,75,74,67,74,91,74,15,86,64,90,93,15,78,76,76,64,90,65,91,15,78,65,75,15,78,67,67,15,93,74,67,78,91,74,75,15,75,78,91,78,3,15,70,65,76,67,90,75,70,65,72,15,95,64,92,91,92,3,15,76,71,78,91,92,3,15,73,64,67,67,64,88,92,3,15,77,67,64,76,68,92,3,15,78,65,75,15,67,70,68,74,92,1,15,123,71,70,92,15,78,76,91,70,64,65,15,76,78,65,65,64,91,15,77,74,15,90,65,75,64,65,74,1,47],0x2F,false),
            preferredStyle: .alert
        )
        old.addAction(UIAlertAction(title: cornerMapMail([122,88,87,90,92,85,57],0x39,false), style: .cancel))
        old.addAction(UIAlertAction(title: cornerMapMail([49,16,25,16,1,16,117],0x75,false), style: .destructive) { [weak self] _ in
            self?.performDeleteAccount()
        })
        present(old, animated: true)
    }

    private func performLogout() {
       var productD: String! = String(cString: [101,120,116,101,110,115,105,111,110,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &productD) { pointer in
    
   }
      productD.append("\(productD.count)")

        EPRankMenu.shared.logout()
        EPRankMenu.shared.switchToWelcomeInterface()
    }

@discardableResult
 func ensureKeyAppearLayer(fontLocked: [String: Any]!) -> Double {
    var unixe: String! = String(cString: [101,102,102,101,99,116,105,118,101,0], encoding: .utf8)!
    var onlyK: String! = String(cString: [112,117,109,112,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &onlyK) { pointer in
    
   }
   repeat {
       var reverseG: [String: Any]! = [String(cString: [100,101,115,99,101,110,116,0], encoding: .utf8)!:11, String(cString: [109,101,116,104,111,100,115,0], encoding: .utf8)!:49]
      for _ in 0 ..< 1 {
         reverseG["\(reverseG.values.count)"] = 2 << (Swift.min(1, reverseG.count))
      }
      repeat {
          var back_: String! = String(cString: [99,108,101,97,114,105,0], encoding: .utf8)!
          var yearsX: String! = String(cString: [109,100,97,121,0], encoding: .utf8)!
          var namez: String! = String(cString: [115,116,114,115,101,112,0], encoding: .utf8)!
          var nickL: String! = String(cString: [101,118,101,114,121,119,104,101,114,101,0], encoding: .utf8)!
          _ = nickL
         reverseG = [yearsX: yearsX.count]
         back_ = "\(reverseG.count % (Swift.max(nickL.count, 2)))"
         namez.append("\(back_.count)")
         nickL.append("\(yearsX.count)")
         if 2955309 == reverseG.count {
            break
         }
      } while (2955309 == reverseG.count) && (2 >= (reverseG.keys.count + 4))
      for _ in 0 ..< 3 {
          var kindn: String! = String(cString: [116,102,114,97,0], encoding: .utf8)!
          var fieldY: Int = 2
         withUnsafeMutablePointer(to: &fieldY) { pointer in
    
         }
          var calendarb: String! = String(cString: [111,112,116,105,109,105,115,109,0], encoding: .utf8)!
          var sessionp: String! = String(cString: [99,97,116,101,103,111,114,105,122,101,0], encoding: .utf8)!
         reverseG["\(sessionp)"] = sessionp.count % (Swift.max(3, 1))
         kindn = "\(3)"
         fieldY /= Swift.max(fieldY * calendarb.count, 3)
         calendarb = "\(calendarb.count + fieldY)"
      }
      unixe.append("\((unixe == (String(cString:[99,0], encoding: .utf8)!) ? unixe.count : onlyK.count))")
      if unixe == (String(cString:[51,100,52,115,95,111,95,0], encoding: .utf8)!) {
         break
      }
   } while (!onlyK.hasPrefix("\(unixe.count)")) && (unixe == (String(cString:[51,100,52,115,95,111,95,0], encoding: .utf8)!))
     var lockedMonths: Int = 3
    var shortlyFadeInspect:Double = 0
    lockedMonths = 9
    shortlyFadeInspect *= Double(lockedMonths)

    return shortlyFadeInspect

}





    private func performDeleteAccount() {
       var failedy: Double = 3.0
   withUnsafeMutablePointer(to: &failedy) { pointer in
    
   }
      failedy -= (Double(Int(failedy > 310126934.0 || failedy < -310126934.0 ? 97.0 : failedy) >> (Swift.min(4, labs(1)))))

        guard EPRankMenu.shared.deleteAccountAndSignOut() else {
            let old = UIAlertController(
                title: nil,
                message: cornerMapMail([67,100,108,105,96,97,37,113,106,37,97,96,105,96,113,96,37,100,102,102,106,112,107,113,43,37,85,105,96,100,118,96,37,113,119,124,37,100,98,100,108,107,43,5],0x5,false),
                preferredStyle: .alert
            )
            old.addAction(UIAlertAction(title: cornerMapMail([125,121,50],0x32,false), style: .default))
            present(old, animated: true)
            return
        }
        EPRankMenu.shared.switchToWelcomeInterface()
    }

    private let backButton: UIButton = {
       var secondI: String! = String(cString: [104,111,116,111,0], encoding: .utf8)!
   if secondI != secondI {
       var likern: [String: Any]! = [String(cString: [109,117,108,116,105,112,108,105,99,97,116,105,111,110,0], encoding: .utf8)!:String(cString: [104,105,103,104,108,105,103,116,101,100,0], encoding: .utf8)!, String(cString: [97,101,118,97,108,0], encoding: .utf8)!:String(cString: [114,101,115,105,100,117,97,108,115,0], encoding: .utf8)!, String(cString: [99,117,100,97,117,112,108,111,97,100,0], encoding: .utf8)!:String(cString: [100,100,118,97,0], encoding: .utf8)!]
       var millisecondD: Int = 1
       _ = millisecondD
       var description_xx: String! = String(cString: [97,118,99,105,0], encoding: .utf8)!
       var processede: Double = 4.0
      withUnsafeMutablePointer(to: &processede) { pointer in
             _ = pointer.pointee
      }
      if likern["\(processede)"] == nil {
         likern = ["\(processede)": (1 >> (Swift.min(labs(Int(processede > 47085277.0 || processede < -47085277.0 ? 2.0 : processede)), 2)))]
      }
      for _ in 0 ..< 3 {
         millisecondD %= Swift.max((Int(processede > 116743121.0 || processede < -116743121.0 ? 23.0 : processede)), 1)
      }
         processede += Double(likern.count)
      for _ in 0 ..< 3 {
         description_xx.append("\((Int(processede > 49871124.0 || processede < -49871124.0 ? 33.0 : processede)))")
      }
      while ((3.75 * processede) > 3.46 && (likern.count % (Swift.max(1, 8))) > 2) {
         processede += Double(1)
         break
      }
       var off3: String! = String(cString: [114,101,111,114,100,101,114,105,110,103,0], encoding: .utf8)!
       var fallbackJ: String! = String(cString: [101,109,97,105,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &fallbackJ) { pointer in
    
      }
      if 3 > (off3.count - 1) && (Double(off3.count) * processede) > 5.25 {
         off3.append("\(fallbackJ.count)")
      }
      if (millisecondD + description_xx.count) > 2 && (millisecondD + 2) > 4 {
          var main_sG: Double = 5.0
          var timestampt: Double = 2.0
          _ = timestampt
          var responseL: String! = String(cString: [115,117,98,0], encoding: .utf8)!
         millisecondD &= 2
         main_sG -= (Double(Int(processede > 188482437.0 || processede < -188482437.0 ? 37.0 : processede)))
         timestampt += (Double((String(cString:[97,0], encoding: .utf8)!) == description_xx ? millisecondD : description_xx.count))
         responseL = "\((2 + Int(timestampt > 75029087.0 || timestampt < -75029087.0 ? 77.0 : timestampt)))"
      }
       var contenti: [String: Any]! = [String(cString: [104,97,110,100,115,0], encoding: .utf8)!:24.0]
      withUnsafeMutablePointer(to: &contenti) { pointer in
             _ = pointer.pointee
      }
       var viewerJ: [String: Any]! = [String(cString: [112,105,120,101,108,0], encoding: .utf8)!:30, String(cString: [99,111,112,121,0], encoding: .utf8)!:100]
          var endh: Double = 0.0
         fallbackJ.append("\(fallbackJ.count)")
         endh /= Swift.max(Double(1), 5)
          var message1: String! = String(cString: [110,111,99,97,115,101,0], encoding: .utf8)!
          _ = message1
         off3.append("\(((String(cString:[76,0], encoding: .utf8)!) == off3 ? off3.count : contenti.count))")
         message1.append("\(((String(cString:[52,0], encoding: .utf8)!) == off3 ? off3.count : viewerJ.values.count))")
      while (likern.keys.contains("\(processede)")) {
          var container_: Float = 4.0
          var fanD: Double = 5.0
          var inset9: Bool = true
         processede -= (Double(1 + Int(processede > 127121193.0 || processede < -127121193.0 ? 63.0 : processede)))
         container_ += (Float(2 >> (Swift.min(1, labs(Int(container_ > 81217030.0 || container_ < -81217030.0 ? 64.0 : container_))))))
         fanD -= (Double(Int(processede > 139975752.0 || processede < -139975752.0 ? 36.0 : processede) % (Swift.max(1, description_xx.count))))
         inset9 = likern.count == 60 || 60 == description_xx.count
         break
      }
      secondI.append("\(millisecondD / 1)")
   }

        let button = UIButton(type: .custom)
        button.setImage(cornerMapMail([-89,-85,-87,-87,-85,-86,-101,-90,-91,-89,-81,-60],0xC4,false).toImage, for: .normal)
        return button
    }()

    private let titleView: UIImageView = {
       var trailingK: String! = String(cString: [118,99,100,115,112,0], encoding: .utf8)!
    _ = trailingK
      trailingK.append("\(trailingK.count + trailingK.count)")

        let view = UIImageView()
        view.image = cornerMapMail([107,125,108,108,113,118,127,71,108,113,108,116,125,24],0x18,false).toImage
        view.contentMode = .scaleAspectFit
        return view
    }()

    private lazy var tableView: UITableView = {
       var insetY: Double = 0.0
   repeat {
      insetY += (Double(Int(insetY > 105348480.0 || insetY < -105348480.0 ? 98.0 : insetY)))
      if insetY == 2723962.0 {
         break
      }
   } while (insetY == 2723962.0) && (insetY == insetY)

        let tableView = UITableView(frame: .zero, style: .plain)
        tableView.backgroundColor = .clear
        tableView.separatorStyle = .none
        tableView.showsVerticalScrollIndicator = false
        tableView.isScrollEnabled = false
        tableView.bounces = false
        tableView.contentInsetAdjustmentBehavior = .never
        tableView.directionalLayoutMargins = .zero
        tableView.cellLayoutMarginsFollowReadableWidth = false
        if #available(iOS 15.0, *) {
            tableView.sectionHeaderTopPadding = 0
        }
        tableView.rowHeight = Layout.rowHeight
        tableView.estimatedRowHeight = Layout.rowHeight
        tableView.dataSource = self
        tableView.delegate = self
        tableView.register(EPNewsDataCell.self, forCellReuseIdentifier: EPNewsDataCell.reuseID)
        return tableView
    }()
}


extension EPResourceController: UITableViewDataSource, UITableViewDelegate {

@discardableResult
 func availableAreaOnlySatisfyImageView(moreLogged: [Any]!, trimmedBadges: Float, oneFrame_ns: [Any]!) -> UIImageView! {
    var roomsz: Bool = true
    var k_badgeB: String! = String(cString: [115,99,101,110,101,0], encoding: .utf8)!
   if roomsz {
      k_badgeB = "\(2 % (Swift.max(1, k_badgeB.count)))"
   }
       var f_countA: String! = String(cString: [109,112,115,117,98,0], encoding: .utf8)!
       var http3: String! = String(cString: [100,121,97,100,105,99,0], encoding: .utf8)!
       var followingK: String! = String(cString: [108,105,115,116,101,100,0], encoding: .utf8)!
          var areaE: Int = 5
          var targets: String! = String(cString: [115,116,115,122,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &targets) { pointer in
                _ = pointer.pointee
         }
          var presentedk: Double = 1.0
          _ = presentedk
         f_countA = "\(f_countA.count + 1)"
         areaE &= targets.count
         targets = "\(followingK.count / (Swift.max(10, areaE)))"
         presentedk -= Double(targets.count + f_countA.count)
      if f_countA.count < 5 {
          var publishQ: Float = 2.0
         f_countA.append("\(f_countA.count)")
         publishQ -= Float(3 << (Swift.min(2, followingK.count)))
      }
      if followingK != f_countA {
         f_countA.append("\(1 * http3.count)")
      }
          var peer2: String! = String(cString: [114,101,99,118,109,115,103,0], encoding: .utf8)!
          var nicknameL: Int = 5
         http3.append("\(1 - http3.count)")
         peer2.append("\(1)")
         nicknameL += ((String(cString:[115,0], encoding: .utf8)!) == http3 ? nicknameL : http3.count)
      while (http3.hasSuffix("\(f_countA.count)")) {
         f_countA.append("\(f_countA.count)")
         break
      }
          var safex: String! = String(cString: [115,104,97,114,112,101,110,0], encoding: .utf8)!
         followingK = "\(2 + followingK.count)"
         safex.append("\(3)")
         http3 = "\(2)"
          var appX: String! = String(cString: [100,105,115,97,98,108,101,114,0], encoding: .utf8)!
          var originu: String! = String(cString: [115,105,122,101,115,0], encoding: .utf8)!
         f_countA = "\(3 + appX.count)"
         originu.append("\(3)")
         http3.append("\(http3.count * f_countA.count)")
      roomsz = http3.count > k_badgeB.count
     let hourDid: Double = 30.0
     var remainingMode: UILabel! = UILabel()
     let trimmedTwo: [String: Any]! = [String(cString: [118,111,98,115,117,98,0], encoding: .utf8)!:12, String(cString: [117,118,108,99,0], encoding: .utf8)!:0, String(cString: [100,105,97,103,111,110,97,108,0], encoding: .utf8)!:24]
     let interfaceFolder: UILabel! = UILabel(frame:CGRect(x: 179, y: 79, width: 0, height: 0))
    var addrinfoButterflies = UIImageView(frame:CGRect.zero)
    addrinfoButterflies.animationRepeatCount = 2
    addrinfoButterflies.image = UIImage(named:String(cString: [103,101,110,0], encoding: .utf8)!)
    addrinfoButterflies.contentMode = .scaleAspectFit
    addrinfoButterflies.frame = CGRect(x: 87, y: 88, width: 0, height: 0)
    addrinfoButterflies.alpha = 0.3;
    addrinfoButterflies.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    remainingMode.alpha = 0.1;
    remainingMode.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    remainingMode.frame = CGRect(x: 99, y: 81, width: 0, height: 0)
    remainingMode.font = UIFont.systemFont(ofSize:13)
    remainingMode.text = ""
    remainingMode.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    remainingMode.textAlignment = .left
    
    interfaceFolder.frame = CGRect(x: 233, y: 120, width: 0, height: 0)
    interfaceFolder.alpha = 0.4;
    interfaceFolder.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    interfaceFolder.font = UIFont.systemFont(ofSize:17)
    interfaceFolder.text = ""
    interfaceFolder.textColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    interfaceFolder.textAlignment = .right
    

    
    return addrinfoButterflies

}






    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
       var firstw: String! = String(cString: [99,111,108,0], encoding: .utf8)!
      firstw.append("\(firstw.count / (Swift.max(3, 3)))")

        tableView.deselectRow(at: indexPath, animated: true)
        switch rows[indexPath.row] {
        case .blacklist:
            navigationController?.pushViewController(EPSetupController(mode: .black), animated: true)
        case .logout:
            performLogout()
        case .deleteAccount:
            presentDeleteAccountConfirmation()
        case .guide:
            if let doc = URL(string: cornerMapMail([-46,-50,-50,-54,-55,-128,-107,-107,-34,-43,-39,-55,-108,-35,-43,-43,-35,-42,-33,-108,-39,-43,-41,-107,-34,-43,-39,-49,-41,-33,-44,-50,-107,-34,-107,-117,-56,-56,-114,-16,-24,-17,-49,-34,-50,-7,-53,-7,-11,-126,-33,-61,-126,-10,-33,-50,-41,-120,-115,-3,-125,-118,-33,-47,-55,-41,-49,-37,-12,-29,-15,-37,-7,-23,-126,-17,-20,-17,-43,-107,-33,-34,-45,-50,-123,-49,-55,-54,-121,-55,-46,-37,-56,-45,-44,-35,-70],0xBA,false)) {
                UIApplication.shared.open(doc, options: [:], completionHandler: nil)
            }
        case .policy:
            if let doc = URL(string: cornerMapMail([-118,-106,-106,-110,-111,-40,-51,-51,-122,-115,-127,-111,-52,-123,-115,-115,-123,-114,-121,-52,-127,-115,-113,-51,-122,-115,-127,-105,-113,-121,-116,-106,-51,-122,-51,-45,-87,-120,-101,-79,-107,-84,-108,-87,-83,-46,-48,-74,-102,-117,-118,-101,-72,-111,-83,-42,-82,-106,-76,-108,-115,-113,-107,-122,-74,-42,-83,-70,-44,-84,-92,-46,-46,-92,-38,-78,-108,-46,-89,-51,-121,-122,-117,-106,-35,-105,-111,-110,-33,-111,-118,-125,-112,-117,-116,-123,-30],0xE2,false)) {
                UIApplication.shared.open(doc, options: [:], completionHandler: nil)
            }
        case .contact:
            navigationController?.pushViewController(EPFriendsListController(), animated: true)
        default:
            break
        }
    }


    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
       var incrementf: Double = 1.0
      incrementf += (Double(1 % (Swift.max(2, Int(incrementf > 26252357.0 || incrementf < -26252357.0 ? 36.0 : incrementf)))))

        guard let cell = tableView.dequeueReusableCell(
            withIdentifier: EPNewsDataCell.reuseID,
            for: indexPath
        ) as? EPNewsDataCell else {
            return UITableViewCell()
        }
        let a_title = tableView.bounds.width > 0
            ? tableView.bounds.width
            : view.bounds.width - Layout.horizontalInset * 2
        cell.configure(imageName: rows[indexPath.row].imageName, tableWidth: a_title)
        return cell
    }

@discardableResult
 func resolveSatisfyMutualCapture() -> Float {
    var optionB: Double = 3.0
   withUnsafeMutablePointer(to: &optionB) { pointer in
          _ = pointer.pointee
   }
    var menuF: String! = String(cString: [101,110,97,98,108,101,100,0], encoding: .utf8)!
    var existingF: Float = 1.0
      existingF /= Swift.max(5, (Float(Int(optionB > 73432300.0 || optionB < -73432300.0 ? 34.0 : optionB))))
      optionB -= (Double(Int(existingF > 12618834.0 || existingF < -12618834.0 ? 36.0 : existingF) / (Swift.max(9, menuF.count))))
   for _ in 0 ..< 3 {
       var lendoB: Bool = false
       var rangew: Float = 1.0
       var scaleq: Float = 5.0
       _ = scaleq
       var pushG: Int = 3
       var kindR: Float = 5.0
      withUnsafeMutablePointer(to: &kindR) { pointer in
             _ = pointer.pointee
      }
      if 4.98 > kindR || (kindR + 4.98) > 2.28 {
         lendoB = !lendoB
      }
      for _ in 0 ..< 1 {
         rangew += Float(2)
      }
         rangew += (Float(Int(kindR > 106445416.0 || kindR < -106445416.0 ? 98.0 : kindR) | pushG))
          var layoutj: Bool = false
          _ = layoutj
          var kindd: Double = 0.0
          var b_layerK: [Any]! = [34, 88, 34]
         withUnsafeMutablePointer(to: &b_layerK) { pointer in
    
         }
         kindR *= (Float(Int(scaleq > 13263138.0 || scaleq < -13263138.0 ? 87.0 : scaleq)))
         layoutj = pushG <= 34
         kindd -= (Double(2 | Int(kindd > 114235099.0 || kindd < -114235099.0 ? 5.0 : kindd)))
         b_layerK.append((Int(kindR > 361606532.0 || kindR < -361606532.0 ? 33.0 : kindR) + 3))
          var kindZ: String! = String(cString: [115,115,105,109,118,0], encoding: .utf8)!
         scaleq /= Swift.max(3, (Float(Int(kindR > 151622853.0 || kindR < -151622853.0 ? 89.0 : kindR) << (Swift.min(kindZ.count, 3)))))
         kindR -= (Float(Int(rangew > 43045420.0 || rangew < -43045420.0 ? 36.0 : rangew)))
      repeat {
          var resourcef: String! = String(cString: [111,110,101,112,97,115,115,0], encoding: .utf8)!
          var network8: String! = String(cString: [111,117,116,100,97,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &network8) { pointer in
    
         }
          var trailingf: String! = String(cString: [99,111,110,102,105,114,109,101,100,0], encoding: .utf8)!
          var publishl: String! = String(cString: [110,97,110,111,98,101,110,99,104,109,97,114,107,0], encoding: .utf8)!
         scaleq += Float(3)
         resourcef = "\(1)"
         network8 = "\(2)"
         trailingf.append("\((Int(rangew > 345683713.0 || rangew < -345683713.0 ? 36.0 : rangew)))")
         publishl = "\(trailingf.count >> (Swift.min(labs(3), 5)))"
         if 4267934.0 == scaleq {
            break
         }
      } while (scaleq <= 4.24) && (4267934.0 == scaleq)
         kindR += (Float((lendoB ? 5 : 4) * pushG))
         pushG |= ((lendoB ? 3 : 5) / (Swift.max(Int(scaleq > 274981223.0 || scaleq < -274981223.0 ? 6.0 : scaleq), 6)))
         rangew /= Swift.max((Float(Int(rangew > 125361830.0 || rangew < -125361830.0 ? 41.0 : rangew) * (lendoB ? 5 : 3))), 4)
      if !lendoB {
          var rankt: String! = String(cString: [100,121,110,108,111,99,107,0], encoding: .utf8)!
          var selg: [Any]! = [15, 25]
          var deviceY: String! = String(cString: [117,115,101,99,0], encoding: .utf8)!
          var textO: String! = String(cString: [118,105,101,119,101,100,0], encoding: .utf8)!
         pushG -= (Int(rangew > 316617942.0 || rangew < -316617942.0 ? 12.0 : rangew) % 2)
         rankt = "\(2)"
         selg = [(Int(scaleq > 79425271.0 || scaleq < -79425271.0 ? 100.0 : scaleq) + 3)]
         deviceY.append("\((Int(scaleq > 256364220.0 || scaleq < -256364220.0 ? 65.0 : scaleq)))")
         textO.append("\(deviceY.count << (Swift.min(labs(1), 2)))")
      }
      repeat {
          var y_objectB: String! = String(cString: [97,108,108,111,99,97,116,101,100,0], encoding: .utf8)!
          _ = y_objectB
         scaleq /= Swift.max(2, (Float(Int(rangew > 105849065.0 || rangew < -105849065.0 ? 100.0 : rangew) % (Swift.max(7, (lendoB ? 3 : 2))))))
         y_objectB.append("\(1)")
         if scaleq == 1226662.0 {
            break
         }
      } while ((pushG << (Swift.min(labs(4), 5))) == 5 && (scaleq * Float(pushG)) == 3.48) && (scaleq == 1226662.0)
      if (3.77 * rangew) == 3.42 || (3.77 + rangew) == 5.78 {
         rangew /= Swift.max(5, (Float(Int(rangew > 240236831.0 || rangew < -240236831.0 ? 43.0 : rangew) >> (Swift.min(4, labs(2))))))
      }
       var blankS: String! = String(cString: [108,97,112,110,100,122,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &blankS) { pointer in
             _ = pointer.pointee
      }
       var likerL: String! = String(cString: [99,116,111,114,0], encoding: .utf8)!
          var credentiall: [String: Any]! = [String(cString: [108,117,109,97,0], encoding: .utf8)!:47, String(cString: [97,114,109,111,110,121,0], encoding: .utf8)!:97]
          _ = credentiall
          var only6: String! = String(cString: [114,101,98,117,99,107,101,116,0], encoding: .utf8)!
          var default_i_V: Double = 4.0
         rangew -= (Float(Int(scaleq > 247245258.0 || scaleq < -247245258.0 ? 81.0 : scaleq) >> (Swift.min(labs(2), 2))))
         credentiall = ["\(scaleq)": (Int(scaleq > 214909729.0 || scaleq < -214909729.0 ? 94.0 : scaleq))]
         only6 = "\(((lendoB ? 5 : 4) & only6.count))"
         default_i_V /= Swift.max((Double(Int(default_i_V > 11930752.0 || default_i_V < -11930752.0 ? 17.0 : default_i_V) >> (Swift.min(likerL.count, 1)))), 1)
         blankS = "\(3)"
      menuF.append("\((menuF == (String(cString:[71,0], encoding: .utf8)!) ? (lendoB ? 1 : 1) : menuF.count))")
   }
   return existingF

}






    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
       var weekdayl: String! = String(cString: [108,108,110,119,0], encoding: .utf8)!
    var welcomeI: Float = 4.0
      welcomeI += (Float(2 * Int(welcomeI > 292093403.0 || welcomeI < -292093403.0 ? 8.0 : welcomeI)))
       var disky: Bool = true
       var dimM: Double = 0.0
       _ = dimM
       var didg: String! = String(cString: [97,99,99,101,112,116,0], encoding: .utf8)!
       _ = didg
         didg.append("\(didg.count)")
      if didg.count <= 1 || disky {
         disky = didg == (String(cString:[89,0], encoding: .utf8)!)
      }
      repeat {
         dimM /= Swift.max(Double(didg.count), 2)
         if dimM == 750877.0 {
            break
         }
      } while (dimM == 750877.0) && (5 >= didg.count)
       var reviseX: String! = String(cString: [119,101,98,109,105,100,115,0], encoding: .utf8)!
       _ = reviseX
      if (1.34 / (Swift.max(5, dimM))) <= 1.74 {
          var formatK: String! = String(cString: [116,101,116,114,97,104,101,100,114,97,108,0], encoding: .utf8)!
          var sheetz: [Any]! = [3, 0, 68]
         didg.append("\((Int(dimM > 29039268.0 || dimM < -29039268.0 ? 94.0 : dimM) + 3))")
         formatK.append("\((Int(dimM > 169933108.0 || dimM < -169933108.0 ? 19.0 : dimM) | 3))")
         sheetz = [sheetz.count / (Swift.max(formatK.count, 10))]
      }
         dimM -= (Double((String(cString:[66,0], encoding: .utf8)!) == didg ? (disky ? 5 : 5) : didg.count))
         didg = "\(((String(cString:[73,0], encoding: .utf8)!) == didg ? Int(dimM > 292114289.0 || dimM < -292114289.0 ? 22.0 : dimM) : didg.count))"
         disky = (didg.count % (Swift.max(reviseX.count, 10))) >= 85
         dimM /= Swift.max(Double(2 | reviseX.count), 3)
      welcomeI -= Float(weekdayl.count)

   repeat {
      weekdayl.append("\(1 ^ weekdayl.count)")
      if weekdayl.count == 4453260 {
         break
      }
   } while (weekdayl.count == 4453260) && (!weekdayl.contains(weekdayl))
        let scroll = tableView.bounds.width
        return Self.rowHeight(forTableWidth: scroll > 0 ? scroll : view.bounds.width - Layout.horizontalInset * 2)
    }


    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
       var reverseb: String! = String(cString: [99,112,117,117,115,101,100,0], encoding: .utf8)!
   repeat {
      reverseb = "\(reverseb.count)"
      if reverseb.count == 1915697 {
         break
      }
   } while (reverseb.count == 1915697) && (reverseb == String(cString:[52,0], encoding: .utf8)!)

return         rows.count
    }
}


private final class EPNewsDataCell: UITableViewCell {

    static let reuseID = cornerMapMail([38,51,45,6,20,16,39,2,23,2,32,6,15,15,99],0x63,false)

    private var imageHeightConstraint: Constraint?

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        selectionStyle = .none
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        layoutMargins = .zero
        contentView.layoutMargins = .zero
        preservesSuperviewLayoutMargins = false
        contentView.preservesSuperviewLayoutMargins = false
        separatorInset = .zero

        contentView.addSubview(rowImageView)
        rowImageView.snp.makeConstraints { make in
            make.top.leading.trailing.equalToSuperview()
            imageHeightConstraint = make.height.equalTo(67).constraint
        }
        rowImageView.setContentHuggingPriority(.required, for: .vertical)
        rowImageView.setContentCompressionResistancePriority(.defaultLow, for: .vertical)
    }

    required init?(coder: NSCoder) {
        fatalError(cornerMapMail([29,26,29,0,92,23,27,16,17,6,78,93,84,28,21,7,84,26,27,0,84,22,17,17,26,84,29,25,4,24,17,25,17,26,0,17,16,116],0x74,false))
    }

@discardableResult
 func bubbleUnknownResponseParent() -> Double {
    var provider_: Int = 5
    _ = provider_
    var colorr: Double = 2.0
    _ = colorr
    var dataf: Double = 4.0
    _ = dataf
       var achievementl: Bool = true
      repeat {
          var permissiont: Double = 1.0
         achievementl = permissiont > 99.14
         if achievementl ? !achievementl : achievementl {
            break
         }
      } while (achievementl ? !achievementl : achievementl) && (achievementl)
         achievementl = (achievementl ? achievementl : !achievementl)
         achievementl = !achievementl && achievementl
      dataf /= Swift.max(1, (Double(Int(colorr > 102316777.0 || colorr < -102316777.0 ? 40.0 : colorr) << (Swift.min(4, labs((achievementl ? 4 : 2)))))))
   if 4 < (provider_ - 1) {
       var collectionk: [String: Any]! = [String(cString: [102,97,115,116,116,101,115,116,0], encoding: .utf8)!:false]
       _ = collectionk
       var reuseO: Bool = true
       _ = reuseO
      repeat {
          var blockedF: String! = String(cString: [102,114,111,122,101,110,0], encoding: .utf8)!
          _ = blockedF
         collectionk = ["\(collectionk.values.count)": (2 | (reuseO ? 3 : 5))]
         blockedF = "\(((reuseO ? 2 : 2) % 3))"
         if 736980 == collectionk.count {
            break
         }
      } while (collectionk.values.count > 2) && (736980 == collectionk.count)
      for _ in 0 ..< 3 {
          var previewO: Bool = false
          var max_9w: String! = String(cString: [101,110,99,104,0], encoding: .utf8)!
         reuseO = reuseO || collectionk.count >= 9
         previewO = (collectionk.keys.count ^ max_9w.count) > 66
         max_9w.append("\((max_9w == (String(cString:[106,0], encoding: .utf8)!) ? max_9w.count : (previewO ? 1 : 5)))")
      }
          var trackp: String! = String(cString: [100,101,109,97,116,101,114,105,97,108,105,122,101,0], encoding: .utf8)!
         reuseO = trackp.hasPrefix("\(reuseO)")
         reuseO = nil != collectionk["\(reuseO)"]
      if 4 >= collectionk.values.count {
         reuseO = collectionk["\(reuseO)"] != nil
      }
      repeat {
         reuseO = reuseO || collectionk.count <= 98
         if reuseO ? !reuseO : reuseO {
            break
         }
      } while (reuseO ? !reuseO : reuseO) && (reuseO)
      provider_ /= Swift.max((2 - (reuseO ? 1 : 1)), 3)
   }
   return colorr

}






    func configure(imageName: String, tableWidth: CGFloat) {
       var template_gB: Float = 2.0
    var checkj: Bool = true
    _ = checkj
    var spacing5: String! = String(cString: [114,101,99,105,112,114,111,99,97,108,0], encoding: .utf8)!
      checkj = ((Int(template_gB > 122677817.0 || template_gB < -122677817.0 ? 21.0 : template_gB) + spacing5.count) < 47)

      spacing5.append("\(((checkj ? 3 : 4) % (Swift.max(Int(template_gB > 91981445.0 || template_gB < -91981445.0 ? 82.0 : template_gB), 7))))")
        let activeImage = imageName.toImage
        rowImageView.image = activeImage
        let pending: CGFloat
        if let activeImage, activeImage.size.width > 0 {
            pending = tableWidth * activeImage.size.height / activeImage.size.width
        } else {
            pending = 67
        }
        imageHeightConstraint?.update(offset: pending)
    }

    private let rowImageView: UIImageView = {
       var receiveW: Float = 4.0
    _ = receiveW
    var d_imager: String! = String(cString: [112,111,115,115,105,98,108,121,0], encoding: .utf8)!
      receiveW -= Float(d_imager.count)

        let view = UIImageView()
      receiveW /= Swift.max((Float(Int(receiveW > 359903033.0 || receiveW < -359903033.0 ? 25.0 : receiveW) << (Swift.min(d_imager.count, 4)))), 3)
        view.contentMode = .scaleAspectFill
        view.clipsToBounds = true
        return view
    }()
}
