
import Foundation

import AVFoundation
import UIKit

final class EPEditPermissionView: UIView {

    private let session = AVCaptureSession()
    private var currentInput: AVCaptureDeviceInput?
    private var isUsingFrontCamera = true

    override class var layerClass: AnyClass {
       var sureL: String! = String(cString: [115,116,97,114,115,0], encoding: .utf8)!
   while (sureL != sureL) {
      sureL = "\(sureL.count)"
      break
   }

            return AVCaptureVideoPreviewLayer.self
    }

    private var videoPreviewLayer: AVCaptureVideoPreviewLayer {
       var resolvedk: Float = 3.0
    _ = resolvedk
       var trailingW: [String: Any]! = [String(cString: [105,102,110,115,0], encoding: .utf8)!:90, String(cString: [100,101,113,117,111,116,101,0], encoding: .utf8)!:88, String(cString: [97,99,107,0], encoding: .utf8)!:73]
      repeat {
          var past5: Float = 3.0
         withUnsafeMutablePointer(to: &past5) { pointer in
                _ = pointer.pointee
         }
          var localey: Float = 2.0
         trailingW = ["\(trailingW.keys.count)": trailingW.values.count]
         past5 -= (Float(Int(past5 > 173337849.0 || past5 < -173337849.0 ? 81.0 : past5)))
         localey -= Float(1)
         if 2365215 == trailingW.count {
            break
         }
      } while (2365215 == trailingW.count) && (trailingW.keys.contains("\(trailingW.values.count)"))
         trailingW["\(trailingW.count)"] = trailingW.values.count & trailingW.count
      repeat {
         trailingW = ["\(trailingW.values.count)": trailingW.count]
         if 4685700 == trailingW.count {
            break
         }
      } while (4685700 == trailingW.count) && (trailingW.keys.count > 1)
      resolvedk /= Swift.max(Float(2 >> (Swift.min(5, trailingW.values.count))), 3)

            return layer as! AVCaptureVideoPreviewLayer
    }

    func start() {
       var user1: String! = String(cString: [109,111,109,101,110,116,97,114,121,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      user1 = "\(((String(cString:[49,0], encoding: .utf8)!) == user1 ? user1.count : user1.count))"
   }

        configureInput()
        videoPreviewLayer.session = session
        videoPreviewLayer.videoGravity = .resizeAspectFill

        guard !session.isRunning else { return }
        DispatchQueue.global(qos: .userInitiated).async { [weak self] in
            self?.session.startRunning()
        }
    }

@discardableResult
 func elementStoreLeft(groupWindow_3: Int, alertClick: Float, sheetButtons: Float) -> [String: Any]! {
    var conversationsK: String! = String(cString: [105,110,118,111,99,97,116,105,111,110,0], encoding: .utf8)!
    var previews: String! = String(cString: [107,110,111,99,107,111,117,116,0], encoding: .utf8)!
    var updatedn: [String: Any]! = [String(cString: [115,99,97,110,115,116,97,116,117,115,0], encoding: .utf8)!:[String(cString: [114,101,109,111,116,101,108,121,0], encoding: .utf8)!]]
    _ = updatedn
      previews = "\(conversationsK.count)"
      conversationsK.append("\(conversationsK.count % (Swift.max(1, 9)))")
   for _ in 0 ..< 3 {
      updatedn["\(updatedn.keys.count)"] = 2 ^ updatedn.keys.count
   }
   return updatedn

}





    func stop() {
       var yesterdayD: Int = 1
   while ((yesterdayD / 5) == 2) {
      yesterdayD >>= Swift.min(4, labs(yesterdayD % 1))
      break
   }

        guard session.isRunning else { return }
        DispatchQueue.global(qos: .userInitiated).async { [weak self] in
            self?.session.stopRunning()
        }
    }

    func switchCamera() {
       var minutex: String! = String(cString: [119,104,101,110,0], encoding: .utf8)!
    _ = minutex
   if minutex.contains("\(minutex.count)") {
      minutex = "\(minutex.count ^ minutex.count)"
   }

        isUsingFrontCamera.toggle()
        configureInput()
    }

@discardableResult
 func navigationEntryResumeKindAlignmentScrollView(inewsCalendar: [String: Any]!, dirProvider: Int, optionPath: Double) -> UIScrollView! {
    var resourceB: Double = 3.0
    var safeA: Double = 2.0
      resourceB -= (Double(Int(safeA > 123369901.0 || safeA < -123369901.0 ? 3.0 : safeA)))
       var calendarp: String! = String(cString: [113,95,53,51,0], encoding: .utf8)!
       var priceG: Float = 4.0
      withUnsafeMutablePointer(to: &priceG) { pointer in
             _ = pointer.pointee
      }
      if (5 % (Swift.max(7, calendarp.count))) >= 5 || 4 >= (calendarp.count - 5) {
         calendarp.append("\((Int(priceG > 371671456.0 || priceG < -371671456.0 ? 87.0 : priceG) + 3))")
      }
         priceG += Float(1)
         priceG -= Float(1)
       var seedB: String! = String(cString: [98,105,97,115,101,100,0], encoding: .utf8)!
       _ = seedB
       var overlayW: String! = String(cString: [116,111,109,105,99,0], encoding: .utf8)!
      if seedB != String(cString:[73,0], encoding: .utf8)! {
         overlayW = "\((Int(priceG > 388773904.0 || priceG < -388773904.0 ? 97.0 : priceG)))"
      }
       var uinitial6: Float = 0.0
      withUnsafeMutablePointer(to: &uinitial6) { pointer in
    
      }
         uinitial6 += (Float(1 - Int(priceG > 128920898.0 || priceG < -128920898.0 ? 97.0 : priceG)))
      safeA += (Double(Int(safeA > 94658273.0 || safeA < -94658273.0 ? 7.0 : safeA) / 3))
     let usersStop: String! = String(cString: [102,105,110,100,97,115,115,111,99,0], encoding: .utf8)!
     let codesOptions: UIView! = UIView(frame:CGRect.zero)
     let nanosecondsProduct: Int = 47
    var haveThousands = UIScrollView()
    codesOptions.alpha = 0.3;
    codesOptions.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    codesOptions.frame = CGRect(x: 231, y: 146, width: 0, height: 0)
    
    haveThousands.addSubview(codesOptions)
    haveThousands.alpha = 0.6;
    haveThousands.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    haveThousands.frame = CGRect(x: 50, y: 90, width: 0, height: 0)
    haveThousands.alwaysBounceHorizontal = true
    haveThousands.showsVerticalScrollIndicator = true
    haveThousands.showsHorizontalScrollIndicator = true
    haveThousands.delegate = nil
    haveThousands.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    haveThousands.alwaysBounceVertical = true

    
    return haveThousands

}





    private func configureInput() {
       var detailb: [String: Any]! = [String(cString: [118,105,115,105,98,105,108,105,116,105,116,101,115,0], encoding: .utf8)!:53, String(cString: [116,102,100,116,0], encoding: .utf8)!:86, String(cString: [114,97,100,102,0], encoding: .utf8)!:25]
      detailb["\(detailb.keys.count)"] = detailb.count / 1

        session.beginConfiguration()
        if session.canSetSessionPreset(.high) {
            session.sessionPreset = .high
        }

        if let currentInput {
            session.removeInput(currentInput)
            self.currentInput = nil
        }

        guard let device = captureDevice(position: isUsingFrontCamera ? .front : .back),
              let min_c8 = try? AVCaptureDeviceInput(device: device),
              session.canAddInput(min_c8) else {
            session.commitConfiguration()
            return
        }
        session.addInput(min_c8)
        currentInput = min_c8
        session.commitConfiguration()
    }

    private func captureDevice(position: AVCaptureDevice.Position) -> AVCaptureDevice? {
       var months6: String! = String(cString: [106,111,105,110,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &months6) { pointer in
          _ = pointer.pointee
   }
    var deletedt: String! = String(cString: [116,114,97,110,115,102,111,114,109,101,100,0], encoding: .utf8)!
    _ = deletedt
   repeat {
      months6 = "\(months6.count % (Swift.max(deletedt.count, 8)))"
      if months6.count == 1730697 {
         break
      }
   } while (months6.count == 1730697) && (deletedt == months6)

   repeat {
      months6 = "\(deletedt.count | months6.count)"
      if (String(cString:[108,95,100,55,51,103,100,113,108,0], encoding: .utf8)!) == months6 {
         break
      }
   } while (deletedt != months6) && ((String(cString:[108,95,100,55,51,103,100,113,108,0], encoding: .utf8)!) == months6)
return         AVCaptureDevice.DiscoverySession(
            deviceTypes: [.builtInWideAngleCamera],
            mediaType: .video,
            position: position
        ).devices.first
    }
}
