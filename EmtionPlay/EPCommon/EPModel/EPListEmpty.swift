
import UIKit

import Foundation

struct EPListEmpty: Codable, Equatable {

    
    var remain: Int
    
    var push: Int
    
    var receive: Int
    
    var gain: Int

    static let legendThreshold = 100
    static let pushGoal = 10
    static let receiveGoal = 20
    static let gainGoal = 10

    static let empty = EPListEmpty(remain: 0, push: 0, receive: 0, gain: 0)

    static let preview = EPListEmpty(
        remain: 50,
        push: 2,
        receive: 8,
        gain: 4
    )


    mutating func recordPublish() {
       var erar: Int = 4
    var mored: Double = 1.0
   withUnsafeMutablePointer(to: &mored) { pointer in
    
   }
      mored -= (Double(Int(mored > 148999543.0 || mored < -148999543.0 ? 72.0 : mored)))
       var publishedO: String! = String(cString: [97,117,116,111,99,111,114,114,101,108,97,116,101,0], encoding: .utf8)!
       var emailT: Double = 1.0
         publishedO.append("\(3)")
      for _ in 0 ..< 2 {
          var resolveD: String! = String(cString: [109,98,102,105,108,116,101,114,0], encoding: .utf8)!
          _ = resolveD
          var s_animation9: Double = 5.0
          _ = s_animation9
          var goalB: String! = String(cString: [100,105,97,109,111,110,100,0], encoding: .utf8)!
         publishedO = "\(1)"
         resolveD = "\((Int(s_animation9 > 117033502.0 || s_animation9 < -117033502.0 ? 55.0 : s_animation9)))"
         s_animation9 /= Swift.max(3, Double(1))
         goalB.append("\(goalB.count | publishedO.count)")
      }
      for _ in 0 ..< 2 {
         publishedO = "\((Int(emailT > 325891208.0 || emailT < -325891208.0 ? 70.0 : emailT)))"
      }
       var dots1: Double = 0.0
       _ = dots1
       var pendingE: Double = 0.0
      withUnsafeMutablePointer(to: &pendingE) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 1 {
          var e_productsU: Float = 0.0
          var verticalD: String! = String(cString: [98,97,99,107,117,112,0], encoding: .utf8)!
         pendingE -= Double(3)
         e_productsU += (Float(1 & Int(emailT > 73438289.0 || emailT < -73438289.0 ? 79.0 : emailT)))
         verticalD = "\(verticalD.count << (Swift.min(labs(2), 3)))"
      }
         dots1 += (Double((String(cString:[104,0], encoding: .utf8)!) == publishedO ? publishedO.count : Int(emailT > 177945533.0 || emailT < -177945533.0 ? 83.0 : emailT)))
      erar -= ((String(cString:[109,0], encoding: .utf8)!) == publishedO ? Int(mored > 180564360.0 || mored < -180564360.0 ? 90.0 : mored) : publishedO.count)

        push += 1
    }

    
    var profileBadgeImageName: String? {
       var completionr: String! = String(cString: [114,116,102,0], encoding: .utf8)!
   if completionr.hasPrefix(completionr) {
       var setup0: Bool = true
       var reusex: String! = String(cString: [117,110,101,109,112,116,121,0], encoding: .utf8)!
       var jinitialf: [Any]! = [39, 8]
       var cosplayc: String! = String(cString: [97,100,97,112,116,105,118,101,0], encoding: .utf8)!
       var roomZ: Double = 4.0
      withUnsafeMutablePointer(to: &roomZ) { pointer in
             _ = pointer.pointee
      }
         cosplayc.append("\((Int(roomZ > 270599465.0 || roomZ < -270599465.0 ? 82.0 : roomZ) + jinitialf.count))")
          var messagef: String! = String(cString: [117,110,111,114,100,101,114,101,100,0], encoding: .utf8)!
          var profilex: Double = 1.0
          var yearsZ: Bool = true
         cosplayc = "\(((setup0 ? 3 : 5) % (Swift.max(messagef.count, 5))))"
         profilex -= Double(reusex.count / 1)
         yearsZ = setup0
          var frontF: [String: Any]! = [String(cString: [109,101,100,105,97,0], encoding: .utf8)!:87, String(cString: [115,104,111,116,0], encoding: .utf8)!:58]
         withUnsafeMutablePointer(to: &frontF) { pointer in
    
         }
          var urlI: String! = String(cString: [110,95,55,50,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &urlI) { pointer in
                _ = pointer.pointee
         }
         cosplayc.append("\(((String(cString:[85,0], encoding: .utf8)!) == cosplayc ? frontF.values.count : cosplayc.count))")
         urlI = "\(reusex.count)"
      for _ in 0 ..< 2 {
         reusex.append("\(((setup0 ? 4 : 5) / (Swift.max(4, reusex.count))))")
      }
      for _ in 0 ..< 2 {
         jinitialf = [3]
      }
      repeat {
          var sharedN: String! = String(cString: [114,111,108,101,0], encoding: .utf8)!
          var roomL: String! = String(cString: [106,100,99,111,101,102,99,116,0], encoding: .utf8)!
          var valuei: [String: Any]! = [String(cString: [112,114,101,109,117,108,116,105,112,108,121,0], encoding: .utf8)!:76, String(cString: [109,111,110,111,98,105,116,0], encoding: .utf8)!:26, String(cString: [104,97,100,97,109,97,114,100,0], encoding: .utf8)!:27]
         jinitialf = [((String(cString:[101,0], encoding: .utf8)!) == roomL ? jinitialf.count : roomL.count)]
         sharedN.append("\(cosplayc.count + jinitialf.count)")
         valuei = ["\(jinitialf.count)": 3]
         if 1669110 == jinitialf.count {
            break
         }
      } while (!reusex.hasSuffix("\(jinitialf.count)")) && (1669110 == jinitialf.count)
       var chat_: String! = String(cString: [115,116,114,117,99,116,0], encoding: .utf8)!
       var otherf: String! = String(cString: [97,112,112,108,105,101,114,0], encoding: .utf8)!
       _ = otherf
          var cell6: String! = String(cString: [97,110,97,103,108,121,112,104,0], encoding: .utf8)!
         jinitialf.append(2 ^ jinitialf.count)
         cell6 = "\(((String(cString:[121,0], encoding: .utf8)!) == reusex ? (setup0 ? 4 : 1) : reusex.count))"
      repeat {
         otherf.append("\(otherf.count | jinitialf.count)")
         if otherf == (String(cString:[107,56,112,49,112,109,121,0], encoding: .utf8)!) {
            break
         }
      } while (jinitialf.count <= otherf.count) && (otherf == (String(cString:[107,56,112,49,112,109,121,0], encoding: .utf8)!))
       var completion_: String! = String(cString: [102,97,99,116,111,114,0], encoding: .utf8)!
      repeat {
          var imgS: [Any]! = [45, 29, 59]
         withUnsafeMutablePointer(to: &imgS) { pointer in
    
         }
         cosplayc = "\(1 & completion_.count)"
         imgS.append(2)
         if cosplayc == (String(cString:[122,114,98,102,115,55,55,108,0], encoding: .utf8)!) {
            break
         }
      } while ((cosplayc.count / 3) == 2 && (1.71 - roomZ) == 2.59) && (cosplayc == (String(cString:[122,114,98,102,115,55,55,108,0], encoding: .utf8)!))
         chat_ = "\(((setup0 ? 5 : 3) * 2))"
         setup0 = otherf == (String(cString:[86,0], encoding: .utf8)!)
      if !reusex.contains("\(jinitialf.count)") {
          var flagF: [Any]! = [35, 45, 38]
          var return_zot: Double = 3.0
          var redk: Int = 1
         reusex.append("\(((setup0 ? 5 : 4) | flagF.count))")
         return_zot -= (Double((String(cString:[55,0], encoding: .utf8)!) == cosplayc ? cosplayc.count : Int(roomZ > 44709583.0 || roomZ < -44709583.0 ? 51.0 : roomZ)))
         redk -= reusex.count
      }
         setup0 = ((chat_.count ^ (!setup0 ? chat_.count : 59)) <= 59)
      completionr = "\((1 & (setup0 ? 3 : 2)))"
   }

        if remain >= Self.legendThreshold { return cornerMapMail([35,32,37,38,36,30,45,36,38,36,47,37,30,50,36,45,65],0x41,false) }
        if remain >= 60 { return cornerMapMail([116,119,114,113,115,73,101,98,119,100,73,101,115,122,22],0x16,false) }
        if remain >= 20 { return cornerMapMail([-102,-103,-100,-97,-99,-89,-100,-111,-107,-99,-106,-89,-117,-99,-108,-8],0xF8,false) }
        return nil
    }
}
