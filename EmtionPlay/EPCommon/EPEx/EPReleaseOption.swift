
import Foundation

import UIKit

extension UIApplication {
    
    class var window: UIWindow? {
        get{
       var badgesw: Bool = false
    var dimo: String! = String(cString: [113,117,97,110,116,105,116,121,0], encoding: .utf8)!
   while (dimo.contains("\(badgesw)")) {
       var optionsn: Double = 5.0
       var authorl: String! = String(cString: [114,101,112,114,101,112,97,114,101,0], encoding: .utf8)!
       var sety: String! = String(cString: [109,111,100,117,108,117,115,0], encoding: .utf8)!
       var optionG: Double = 4.0
      withUnsafeMutablePointer(to: &optionG) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 1 {
         optionsn -= (Double(Int(optionG > 275350202.0 || optionG < -275350202.0 ? 1.0 : optionG)))
      }
      if !sety.hasSuffix("\(optionsn)") {
         sety = "\(1 & authorl.count)"
      }
       var dirm: String! = String(cString: [114,103,98,116,101,115,116,0], encoding: .utf8)!
       var conversationsE: String! = String(cString: [121,117,118,112,116,111,117,121,118,121,0], encoding: .utf8)!
         optionG -= Double(dirm.count)
         conversationsE.append("\(3)")
      repeat {
         optionsn /= Swift.max(4, (Double((String(cString:[82,0], encoding: .utf8)!) == authorl ? authorl.count : conversationsE.count)))
         if optionsn == 2024238.0 {
            break
         }
      } while ((optionsn * Double(conversationsE.count)) > 4.46 || 5 > (conversationsE.count * Int(optionsn > 171482726.0 || optionsn < -171482726.0 ? 25.0 : optionsn))) && (optionsn == 2024238.0)
         dirm = "\(sety.count >> (Swift.min(labs(1), 1)))"
      for _ in 0 ..< 1 {
          var alertL: [String: Any]! = [String(cString: [100,97,121,115,0], encoding: .utf8)!:60, String(cString: [105,110,116,101,114,108,97,99,101,0], encoding: .utf8)!:36]
          var fetchs: Float = 3.0
          var millisecondM: String! = String(cString: [109,111,100,105,102,105,101,114,115,0], encoding: .utf8)!
          var register__f: String! = String(cString: [97,110,97,108,121,122,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &register__f) { pointer in
    
         }
          var trailing4: String! = String(cString: [98,101,97,116,0], encoding: .utf8)!
          _ = trailing4
         authorl.append("\(trailing4.count - dirm.count)")
         alertL[conversationsE] = (Int(optionG > 339012846.0 || optionG < -339012846.0 ? 65.0 : optionG) * conversationsE.count)
         fetchs /= Swift.max(1, (Float(Int(optionG > 164149318.0 || optionG < -164149318.0 ? 81.0 : optionG) ^ 1)))
         millisecondM = "\(1)"
         register__f.append("\(register__f.count)")
      }
         authorl = "\(authorl.count)"
         dirm.append("\(authorl.count >> (Swift.min(labs(2), 3)))")
          var networkH: [String: Any]! = [String(cString: [114,101,102,112,105,99,0], encoding: .utf8)!:39, String(cString: [116,104,114,101,97,100,112,111,111,108,0], encoding: .utf8)!:20, String(cString: [115,116,111,114,109,98,105,114,100,0], encoding: .utf8)!:36]
         withUnsafeMutablePointer(to: &networkH) { pointer in
                _ = pointer.pointee
         }
          var tiersy: [Any]! = [94, 87, 68]
         sety.append("\(1)")
         networkH = ["\(networkH.values.count)": 3 >> (Swift.min(1, authorl.count))]
         tiersy.append(networkH.values.count)
         sety.append("\(sety.count + dirm.count)")
      dimo.append("\(authorl.count)")
      break
   }

            if let appDelegate = UIApplication.shared.delegate,
               let outgoing = appDelegate.window {
                return outgoing
            }
            return UIApplication.shared.keyWindow
   if dimo.count < 4 || !badgesw {
      badgesw = (40 >= ((!badgesw ? 40 : dimo.count) * dimo.count))
   }
        }
    }
    
    
    var keyWindowCompat: UIWindow? {
       var allg: Double = 3.0
   withUnsafeMutablePointer(to: &allg) { pointer in
    
   }
    var size_grp: [String: Any]! = [String(cString: [115,117,102,102,105,120,0], encoding: .utf8)!:95, String(cString: [112,105,99,107,109,111,100,101,0], encoding: .utf8)!:80]
    var red5: Double = 4.0
    _ = red5
   while ((5.44 - allg) <= 4.65) {
       var z_heights: Double = 3.0
      withUnsafeMutablePointer(to: &z_heights) { pointer in
             _ = pointer.pointee
      }
       var out4: String! = String(cString: [119,105,116,104,100,114,97,119,0], encoding: .utf8)!
       _ = out4
          var hourh: Double = 0.0
          _ = hourh
          var enabledU: Float = 4.0
          _ = enabledU
         out4.append("\((1 / (Swift.max(Int(hourh > 303065550.0 || hourh < -303065550.0 ? 97.0 : hourh), 5))))")
         enabledU *= Float(out4.count % (Swift.max(2, 10)))
         z_heights -= Double(3)
         z_heights -= (Double(out4 == (String(cString:[120,0], encoding: .utf8)!) ? out4.count : Int(z_heights > 139288479.0 || z_heights < -139288479.0 ? 66.0 : z_heights)))
         out4 = "\(1)"
      for _ in 0 ..< 1 {
         out4.append("\((out4 == (String(cString:[81,0], encoding: .utf8)!) ? out4.count : Int(z_heights > 207408137.0 || z_heights < -207408137.0 ? 33.0 : z_heights)))")
      }
         out4 = "\((out4.count ^ Int(z_heights > 107680589.0 || z_heights < -107680589.0 ? 14.0 : z_heights)))"
      size_grp = ["\(red5)": (Int(red5 > 345717905.0 || red5 < -345717905.0 ? 9.0 : red5))]
      break
   }

        if #available(iOS 13.0, *) {
            return connectedScenes
                .compactMap { $0 as? UIWindowScene }
                .flatMap { $0.windows }
                .first { $0.isKeyWindow }
        } else {
            return keyWindow
        }
   while (4.37 <= (allg * allg) || (allg * allg) <= 4.37) {
      allg -= (Double(Int(allg > 341784973.0 || allg < -341784973.0 ? 10.0 : allg) << (Swift.min(2, labs(Int(allg > 76848993.0 || allg < -76848993.0 ? 15.0 : allg))))))
      break
   }
    }

    
    
    class func topController(controller: UIViewController? = window?.rootViewController) -> UIViewController? {
       var nicknameL: String! = String(cString: [115,117,98,114,97,110,103,101,115,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      nicknameL.append("\(((String(cString:[84,0], encoding: .utf8)!) == nicknameL ? nicknameL.count : nicknameL.count))")
   }

        if let navigationController = controller as? UINavigationController {
            return topController(controller: navigationController.visibleViewController)
        }
        if let tabController = controller as? UITabBarController {
            if let selected = tabController.selectedViewController {
                return topController(controller: selected)
            }
        }
        if let presented = controller?.presentedViewController {
            return topController(controller: presented)
        }
        return controller
    }
}
