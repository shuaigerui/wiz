
import Foundation
import UIKit

enum SS_PublishedPostMedia {

    private static let folderName = cornerMapMail([-26,-61,-44,-38,-33,-59,-34,-45,-46,-26,-39,-59,-62,-5,-45,-46,-33,-41,-74],0xB6,false)

    static func mediaDirectory() -> URL {
       var coinsa: Bool = true
   for _ in 0 ..< 3 {
      coinsa = (!coinsa ? coinsa : !coinsa)
   }

return         FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]
            .appendingPathComponent(folderName, isDirectory: true)
    }

    private static func ensureDirectory() {
       var mapped8: Int = 5
   withUnsafeMutablePointer(to: &mapped8) { pointer in
    
   }
    var end8: Float = 1.0
      mapped8 ^= 2

   if mapped8 == 3 {
      end8 /= Swift.max((Float(Int(end8 > 212655612.0 || end8 < -212655612.0 ? 85.0 : end8) + mapped8)), 3)
   }
        let back = mediaDirectory()
        try? FileManager.default.createDirectory(at: back, withIntermediateDirectories: true)
    }

    
    static func savePhoto(_ image: UIImage) -> String? {
       var fireP: [Any]! = [57, 58, 45]
    var nanosecond0: String! = String(cString: [114,97,105,116,0], encoding: .utf8)!
       var postc: String! = String(cString: [99,117,98,101,100,0], encoding: .utf8)!
       _ = postc
       var stampB: String! = String(cString: [116,114,105,97,110,103,117,108,97,116,105,111,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &stampB) { pointer in
             _ = pointer.pointee
      }
       var welcomeo: [String: Any]! = [String(cString: [115,117,98,116,105,116,108,101,0], encoding: .utf8)!:[17, 94]]
      withUnsafeMutablePointer(to: &welcomeo) { pointer in
    
      }
          var mapI: Double = 5.0
         withUnsafeMutablePointer(to: &mapI) { pointer in
    
         }
          var for_hq: Double = 1.0
         welcomeo = ["\(for_hq)": postc.count | 1]
         mapI -= (Double(Int(mapI > 277568820.0 || mapI < -277568820.0 ? 75.0 : mapI)))
         welcomeo = [stampB: postc.count]
      while (!stampB.hasPrefix("\(postc.count)")) {
          var date5: Float = 3.0
          _ = date5
          var receivet: String! = String(cString: [102,105,110,0], encoding: .utf8)!
         postc = "\(stampB.count * welcomeo.count)"
         date5 -= (Float(Int(date5 > 255933490.0 || date5 < -255933490.0 ? 47.0 : date5) | 1))
         receivet = "\(((String(cString:[99,0], encoding: .utf8)!) == postc ? postc.count : welcomeo.count))"
         break
      }
         stampB = "\(stampB.count)"
       var modelB: Int = 4
       var productU: Int = 5
         welcomeo = ["\(welcomeo.keys.count)": (stampB == (String(cString:[75,0], encoding: .utf8)!) ? stampB.count : welcomeo.keys.count)]
          var startz: Bool = true
          var outgoingM: Bool = true
          var post0: String! = String(cString: [101,118,114,99,0], encoding: .utf8)!
         modelB %= Swift.max(1, postc.count >> (Swift.min(5, post0.count)))
         startz = stampB.count > welcomeo.count
         outgoingM = !startz
       var achieG: Int = 3
      repeat {
         welcomeo = [stampB: postc.count]
         if welcomeo.count == 560683 {
            break
         }
      } while (welcomeo.count == 560683) && (postc.count >= welcomeo.count)
         productU %= Swift.max(modelB, 4)
         achieG -= modelB >> (Swift.min(3, labs(productU)))
      nanosecond0 = "\(nanosecond0.count >> (Swift.min(labs(2), 2)))"

   while ((3 + fireP.count) == 4) {
      nanosecond0.append("\(1)")
      break
   }
        ensureDirectory()
        guard let data = image.jpegData(compressionQuality: 0.88) else { return nil }
        let renderer = "\(cornerMapMail([100,75,20],0x14,false))\(UUID().uuidString)"
   for _ in 0 ..< 3 {
      fireP = [fireP.count << (Swift.min(4, fireP.count))]
   }
        let nanosecond = mediaDirectory().appendingPathComponent("\(renderer).jpg")
        do {
            try data.write(to: nanosecond, options: .atomic)
            return renderer
        } catch {
            return nil
        }
    }

    
    static func saveVideo(from pickedURL: URL) -> String? {
       var thresholdL: String! = String(cString: [105,115,100,105,103,105,116,0], encoding: .utf8)!
    var setup9: [String: Any]! = [String(cString: [100,105,115,99,114,105,109,105,110,97,116,111,114,0], encoding: .utf8)!:80, String(cString: [119,101,105,103,104,116,101,100,0], encoding: .utf8)!:72, String(cString: [122,109,98,118,0], encoding: .utf8)!:14]
      setup9[thresholdL] = 1 * setup9.values.count

       var closeA: [Any]! = [65, 40, 39]
       var localeM: Double = 3.0
          var previewm: String! = String(cString: [100,101,108,105,118,101,114,101,100,0], encoding: .utf8)!
          var back2: Int = 5
         withUnsafeMutablePointer(to: &back2) { pointer in
                _ = pointer.pointee
         }
          var reviset: String! = String(cString: [116,111,117,112,112,101,114,0], encoding: .utf8)!
         localeM /= Swift.max((Double((String(cString:[114,0], encoding: .utf8)!) == reviset ? back2 : reviset.count)), 2)
         previewm = "\((Int(localeM > 57154245.0 || localeM < -57154245.0 ? 28.0 : localeM) ^ 2))"
          var basex: String! = String(cString: [114,101,100,111,0], encoding: .utf8)!
          var fires: Double = 4.0
          var sharedA: Double = 2.0
         closeA.append((Int(localeM > 177919595.0 || localeM < -177919595.0 ? 66.0 : localeM) * closeA.count))
         basex.append("\((Int(sharedA > 145765391.0 || sharedA < -145765391.0 ? 3.0 : sharedA)))")
         fires -= Double(3 - closeA.count)
         sharedA -= (Double(1 | Int(localeM > 55350021.0 || localeM < -55350021.0 ? 49.0 : localeM)))
       var was7: Double = 3.0
       var urlw: Double = 4.0
      withUnsafeMutablePointer(to: &urlw) { pointer in
             _ = pointer.pointee
      }
      repeat {
          var captionP: Bool = true
          var delegate_yd2: Double = 5.0
          var backK: Bool = false
          var constraint_: Double = 5.0
         urlw += (Double(Int(was7 > 175182576.0 || was7 < -175182576.0 ? 62.0 : was7) + Int(urlw > 274594348.0 || urlw < -274594348.0 ? 3.0 : urlw)))
         captionP = 54.95 > delegate_yd2
         delegate_yd2 -= (Double(Int(delegate_yd2 > 13353311.0 || delegate_yd2 < -13353311.0 ? 84.0 : delegate_yd2) | 2))
         backK = !backK || captionP
         constraint_ -= Double(closeA.count % (Swift.max(2, 6)))
         if 4829028.0 == urlw {
            break
         }
      } while (5.61 < urlw) && (4829028.0 == urlw)
       var present8: Bool = false
      for _ in 0 ..< 2 {
          var leading6: Float = 5.0
          var sendP: String! = String(cString: [117,110,116,105,108,0], encoding: .utf8)!
          _ = sendP
          var messageW: String! = String(cString: [112,99,105,112,104,101,114,116,101,120,116,0], encoding: .utf8)!
          var randomP: Double = 0.0
         was7 -= (Double(Int(urlw > 374933764.0 || urlw < -374933764.0 ? 22.0 : urlw) ^ 1))
         leading6 -= (Float(Int(urlw > 124402326.0 || urlw < -124402326.0 ? 8.0 : urlw) + 3))
         sendP.append("\((2 - Int(leading6 > 369591140.0 || leading6 < -369591140.0 ? 33.0 : leading6)))")
         messageW = "\((Int(urlw > 116291653.0 || urlw < -116291653.0 ? 93.0 : urlw) | closeA.count))"
         randomP -= Double(2)
      }
         present8 = was7 > 20.80 || 72 > closeA.count
      setup9["\(localeM)"] = (thresholdL == (String(cString:[75,0], encoding: .utf8)!) ? thresholdL.count : Int(localeM > 174931045.0 || localeM < -174931045.0 ? 73.0 : localeM))
        ensureDirectory()
        let renderer = "\(cornerMapMail([49,24,71],0x47,false))\(UUID().uuidString)"
        let timestamp = mediaDirectory().appendingPathComponent("\(renderer).mp4")
        do {
            if FileManager.default.fileExists(atPath: timestamp.path) {
                try FileManager.default.removeItem(at: timestamp)
            }
            try FileManager.default.copyItem(at: pickedURL, to: timestamp)
            return renderer
        } catch {
            return nil
        }
    }

    static func imageFileURL(baseName: String) -> URL? {
       var remaininge: String! = String(cString: [115,119,105,116,99,104,0], encoding: .utf8)!
    var release_ci: String! = String(cString: [103,114,111,119,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      remaininge.append("\(((String(cString:[72,0], encoding: .utf8)!) == remaininge ? remaininge.count : release_ci.count))")
   }

        for ext in [cornerMapMail([15,21,2,101],0x65,false), cornerMapMail([-4,-26,-13,-15,-106],0x96,false), cornerMapMail([67,93,84,51],0x33,false)] {
            let shop = mediaDirectory().appendingPathComponent("\(baseName).\(ext)")
       var month4: Int = 2
      withUnsafeMutablePointer(to: &month4) { pointer in
             _ = pointer.pointee
      }
          var ids2: [Any]! = [91, 75]
         withUnsafeMutablePointer(to: &ids2) { pointer in
                _ = pointer.pointee
         }
          var areas: String! = String(cString: [116,111,110,101,0], encoding: .utf8)!
         month4 >>= Swift.min(labs(3 % (Swift.max(4, ids2.count))), 5)
         areas = "\(1 * ids2.count)"
         month4 %= Swift.max(month4 ^ 1, 2)
      for _ in 0 ..< 3 {
         month4 ^= month4
      }
      release_ci = "\(month4 - release_ci.count)"
            if FileManager.default.fileExists(atPath: shop.path) { return shop }
        }
        return nil
    }

    static func videoFileURL(baseName: String) -> URL? {
       var mapd: [String: Any]! = [String(cString: [103,99,109,0], encoding: .utf8)!:90, String(cString: [99,99,105,112,0], encoding: .utf8)!:55]
   if mapd.count > 4 {
      mapd = ["\(mapd.keys.count)": 1 - mapd.values.count]
   }

        for ext in [cornerMapMail([-14,-17,-85,-97],0x9F,false), cornerMapMail([107,105,112,6],0x6,false), cornerMapMail([26,67,1,119],0x77,false)] {
            let shop = mediaDirectory().appendingPathComponent("\(baseName).\(ext)")
            if FileManager.default.fileExists(atPath: shop.path) { return shop }
        }
        return nil
    }

    
}

extension SS_BundleResourceMedia {


    
    static func resolveVideoURL(baseName: String) -> URL? {
       var unread2: String! = String(cString: [112,114,111,118,105,100,101,114,0], encoding: .utf8)!
    _ = unread2
    var tracky: String! = String(cString: [100,101,102,105,110,101,115,0], encoding: .utf8)!
    _ = tracky
   if tracky == String(cString:[115,0], encoding: .utf8)! {
       var rankl: Double = 4.0
       var tomorrow1: Double = 4.0
       var passwordA: String! = String(cString: [99,114,111,115,115,111,118,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &passwordA) { pointer in
    
      }
       var lnew_m3: String! = String(cString: [103,101,116,99,117,116,0], encoding: .utf8)!
      while (lnew_m3.count <= 1) {
         lnew_m3.append("\((2 * Int(rankl > 218694198.0 || rankl < -218694198.0 ? 1.0 : rankl)))")
         break
      }
         lnew_m3.append("\((lnew_m3 == (String(cString:[115,0], encoding: .utf8)!) ? Int(tomorrow1 > 258205782.0 || tomorrow1 < -258205782.0 ? 71.0 : tomorrow1) : lnew_m3.count))")
      repeat {
         passwordA.append("\((2 / (Swift.max(2, Int(rankl > 143058647.0 || rankl < -143058647.0 ? 80.0 : rankl)))))")
         if passwordA.count == 4118338 {
            break
         }
      } while (passwordA.count == 4118338) && (!passwordA.hasSuffix("\(tomorrow1)"))
      repeat {
         rankl -= Double(lnew_m3.count & passwordA.count)
         if 3430973.0 == rankl {
            break
         }
      } while (4.53 >= (3.57 + tomorrow1) || 3.57 >= (tomorrow1 + rankl)) && (3430973.0 == rankl)
      for _ in 0 ..< 3 {
          var update_q6i: Float = 4.0
          _ = update_q6i
          var httpz: Double = 5.0
          _ = httpz
          var captionu: [Any]! = [String(cString: [97,114,101,110,97,115,0], encoding: .utf8)!]
          var blueE: String! = String(cString: [104,97,114,101,0], encoding: .utf8)!
          var handleb: String! = String(cString: [110,105,103,104,116,0], encoding: .utf8)!
         passwordA = "\((handleb.count % (Swift.max(3, Int(tomorrow1 > 25274686.0 || tomorrow1 < -25274686.0 ? 26.0 : tomorrow1)))))"
         update_q6i -= (Float(Int(httpz > 272989217.0 || httpz < -272989217.0 ? 69.0 : httpz) << (Swift.min(3, labs(Int(update_q6i > 261427817.0 || update_q6i < -261427817.0 ? 94.0 : update_q6i))))))
         httpz /= Swift.max((Double(Int(update_q6i > 10959766.0 || update_q6i < -10959766.0 ? 98.0 : update_q6i))), 1)
         captionu.append(1 ^ captionu.count)
         blueE = "\(passwordA.count)"
      }
      unread2 = "\((Int(rankl > 261067556.0 || rankl < -261067556.0 ? 54.0 : rankl)))"
   }
   if unread2.count == tracky.count {
      tracky.append("\(unread2.count)")
   }

return         SS_PublishedPostMedia.videoFileURL(baseName: baseName) ?? videoURL(baseName: baseName)
    }


    
    static func resolvePostImageURL(baseName: String) -> URL? {
       var nearestb: String! = String(cString: [97,120,105,120,0], encoding: .utf8)!
    _ = nearestb
    var rectF: String! = String(cString: [99,111,110,118,101,110,105,101,110,99,101,0], encoding: .utf8)!
   repeat {
      nearestb.append("\(1)")
      if nearestb == (String(cString:[98,122,95,119,52,0], encoding: .utf8)!) {
         break
      }
   } while (nearestb == (String(cString:[98,122,95,119,52,0], encoding: .utf8)!)) && (4 <= rectF.count || nearestb.count <= 4)

   while (!rectF.hasSuffix(rectF)) {
      rectF = "\(rectF.count)"
      break
   }
return         SS_PublishedPostMedia.imageFileURL(baseName: baseName)
            ?? friendImageURL(baseName: baseName)
            ?? postImageURL(baseName: baseName)
    }
}
