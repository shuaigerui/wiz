
import UIKit

import Foundation

enum SS_UserVoiceMedia {

    private static let folderName = cornerMapMail([89,127,105,126,90,99,101,111,105,127,12],0xC,false)

    static func mediaDirectory() -> URL {
       var disk6: String! = String(cString: [109,106,112,101,103,97,0], encoding: .utf8)!
   while (4 <= disk6.count) {
       var previousF: Double = 0.0
       var quarter5: Bool = true
       var currentm: Double = 5.0
      withUnsafeMutablePointer(to: &currentm) { pointer in
             _ = pointer.pointee
      }
       var int_rD: String! = String(cString: [115,116,101,112,115,105,122,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &int_rD) { pointer in
             _ = pointer.pointee
      }
       var genM: Bool = false
       var mapB: String! = String(cString: [102,105,116,116,101,100,0], encoding: .utf8)!
      if currentm > 3.60 {
         genM = 83.42 < previousF && quarter5
      }
          var buttonM: Float = 1.0
         int_rD.append("\(1)")
         buttonM += (Float(Int(currentm > 208022447.0 || currentm < -208022447.0 ? 24.0 : currentm)))
         int_rD = "\(mapB.count + int_rD.count)"
         mapB = "\(((genM ? 5 : 3)))"
          var cosplayb: Double = 1.0
         withUnsafeMutablePointer(to: &cosplayb) { pointer in
    
         }
         genM = (Double(mapB.count) * previousF) == 99.53
         cosplayb *= (Double((quarter5 ? 1 : 5)))
      while (mapB == int_rD) {
         int_rD.append("\(mapB.count & 1)")
         break
      }
      repeat {
         mapB.append("\(((quarter5 ? 3 : 2) + Int(currentm > 257587909.0 || currentm < -257587909.0 ? 44.0 : currentm)))")
         if mapB == (String(cString:[109,109,100,106,0], encoding: .utf8)!) {
            break
         }
      } while (mapB == (String(cString:[109,109,100,106,0], encoding: .utf8)!)) && (mapB.count <= 1)
         previousF -= (Double((genM ? 2 : 2) * 2))
      while ((2.43 - currentm) > 3.7 && 5 > (2 >> (Swift.min(1, int_rD.count)))) {
          var mailj: String! = String(cString: [117,100,105,111,0], encoding: .utf8)!
          var idsL: [Any]! = [1, 5]
         withUnsafeMutablePointer(to: &idsL) { pointer in
                _ = pointer.pointee
         }
          var folderY: Int = 3
         currentm /= Swift.max((Double(Int(previousF > 346624096.0 || previousF < -346624096.0 ? 12.0 : previousF))), 2)
         mailj.append("\(idsL.count)")
         idsL = [idsL.count / 1]
         folderY ^= (Int(previousF > 289293654.0 || previousF < -289293654.0 ? 41.0 : previousF) - 2)
         break
      }
      while (5.35 <= currentm) {
         currentm /= Swift.max(5, (Double(Int(currentm > 266993403.0 || currentm < -266993403.0 ? 21.0 : currentm) << (Swift.min(2, labs(Int(previousF > 207952576.0 || previousF < -207952576.0 ? 89.0 : previousF)))))))
         break
      }
         previousF += Double(1)
       var conversationsZ: String! = String(cString: [115,111,99,97,110,116,114,99,118,109,111,114,101,0], encoding: .utf8)!
       _ = conversationsZ
      while (5.84 <= currentm || 5.12 <= (5.84 + currentm)) {
          var publishede: Bool = false
          _ = publishede
          var lastV: Bool = true
          var toolF: String! = String(cString: [115,105,103,110,108,101,0], encoding: .utf8)!
          var logini: [String: Any]! = [String(cString: [102,105,114,101,119,97,108,108,0], encoding: .utf8)!:51.0]
         quarter5 = (!quarter5 ? !lastV : quarter5)
         publishede = (publishede ? genM : !publishede)
         toolF.append("\(3 ^ logini.keys.count)")
         logini = ["\(currentm)": (Int(currentm > 366465835.0 || currentm < -366465835.0 ? 69.0 : currentm) * (lastV ? 1 : 5))]
         break
      }
          var itemI: String! = String(cString: [101,112,115,105,108,111,110,0], encoding: .utf8)!
         quarter5 = (!quarter5 ? genM : !quarter5)
         itemI = "\((1 * (genM ? 4 : 3)))"
         conversationsZ = "\((Int(currentm > 296782692.0 || currentm < -296782692.0 ? 71.0 : currentm) & (quarter5 ? 4 : 3)))"
      disk6.append("\(((quarter5 ? 3 : 5) ^ disk6.count))")
      break
   }

return         FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]
            .appendingPathComponent(folderName, isDirectory: true)
    }

    private static func ensureDirectory() {
       var m_image5: Double = 2.0
      m_image5 -= (Double(Int(m_image5 > 224638207.0 || m_image5 < -224638207.0 ? 60.0 : m_image5) << (Swift.min(labs(Int(m_image5 > 246241590.0 || m_image5 < -246241590.0 ? 83.0 : m_image5)), 3))))

        let back = mediaDirectory()
        try? FileManager.default.createDirectory(at: back, withIntermediateDirectories: true)
    }

    
    static func saveVoiceCopy(from sourceURL: URL, userId: Int64) -> String? {
       var outgoing5: String! = String(cString: [102,108,105,103,104,116,0], encoding: .utf8)!
    var groupv: Double = 2.0
       var hourE: String! = String(cString: [108,105,98,111,112,101,110,106,112,101,103,0], encoding: .utf8)!
       var status4: [String: Any]! = [String(cString: [103,108,111,98,97,108,116,101,109,0], encoding: .utf8)!:37, String(cString: [108,105,110,101,0], encoding: .utf8)!:15]
      withUnsafeMutablePointer(to: &status4) { pointer in
    
      }
       var coinsw: Double = 3.0
       var pathF: String! = String(cString: [100,101,110,121,0], encoding: .utf8)!
       var tapL: String! = String(cString: [111,112,116,105,109,105,122,101,114,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         coinsw /= Swift.max(Double(pathF.count), 1)
      }
      while (4 >= (3 << (Swift.min(3, tapL.count)))) {
         tapL = "\(pathF.count >> (Swift.min(labs(2), 5)))"
         break
      }
       var beforeb: Double = 5.0
          var optionsL: Int = 5
          var sessiony: Bool = false
          var conversationsS: String! = String(cString: [119,97,118,101,102,111,114,109,97,116,101,120,0], encoding: .utf8)!
          _ = conversationsS
         tapL = "\(pathF.count)"
         optionsL -= (optionsL & Int(coinsw > 216933295.0 || coinsw < -216933295.0 ? 73.0 : coinsw))
         conversationsS.append("\(status4.count)")
         pathF.append("\((Int(beforeb > 3315244.0 || beforeb < -3315244.0 ? 77.0 : beforeb) % (Swift.max(2, pathF.count))))")
      while (status4.count <= pathF.count) {
          var publishedR: [Any]! = [4.0]
          var main_y3: String! = String(cString: [109,101,97,110,0], encoding: .utf8)!
          var tapx: String! = String(cString: [112,100,115,0], encoding: .utf8)!
          _ = tapx
         pathF = "\(1 - main_y3.count)"
         publishedR = [status4.keys.count % 1]
         tapx = "\(publishedR.count)"
         break
      }
      while (1 >= status4.values.count) {
         status4[hourE] = pathF.count % 3
         break
      }
          var publishG: Double = 2.0
          _ = publishG
          var deliveredn: Double = 3.0
          var testp: String! = String(cString: [116,97,107,101,111,118,101,114,0], encoding: .utf8)!
         pathF = "\(hourE.count)"
         publishG += Double(status4.keys.count - 2)
         deliveredn /= Swift.max(1, Double(tapL.count))
         testp = "\((tapL == (String(cString:[88,0], encoding: .utf8)!) ? status4.keys.count : tapL.count))"
      outgoing5 = "\((Int(coinsw > 185104101.0 || coinsw < -185104101.0 ? 36.0 : coinsw) - status4.values.count))"

      outgoing5.append("\(3 << (Swift.min(4, outgoing5.count)))")
        ensureDirectory()
      groupv -= (Double(Int(groupv > 255367889.0 || groupv < -255367889.0 ? 30.0 : groupv) - Int(groupv > 382490185.0 || groupv < -382490185.0 ? 84.0 : groupv)))
        let renderer = "\(cornerMapMail([127,124,85,10],0xA,false))\(userId)"
        let timestamp = mediaDirectory().appendingPathComponent("\(renderer).m4a")
        do {
            if FileManager.default.fileExists(atPath: timestamp.path) {
                try FileManager.default.removeItem(at: timestamp)
            }
            try FileManager.default.copyItem(at: sourceURL, to: timestamp)
            return renderer
        } catch {
            return nil
        }
    }

    static func voiceFileURL(baseName: String) -> URL? {
       var amountF: String! = String(cString: [97,118,111,105,100,0], encoding: .utf8)!
    var achievementY: Int = 1
       var mapx: String! = String(cString: [100,105,116,97,110,99,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &mapx) { pointer in
    
      }
       var rectm: Double = 4.0
       var animaten: String! = String(cString: [107,102,119,114,105,116,101,0], encoding: .utf8)!
          var monthsP: Double = 4.0
          var minuteo: Double = 5.0
         animaten = "\((Int(monthsP > 348167277.0 || monthsP < -348167277.0 ? 61.0 : monthsP)))"
         minuteo += (Double(Int(rectm > 138525645.0 || rectm < -138525645.0 ? 79.0 : rectm)))
      repeat {
          var userZ: Float = 5.0
          var previousu: Float = 4.0
          var actionj: Float = 3.0
          _ = actionj
          var float_mI: String! = String(cString: [116,104,101,97,116,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &float_mI) { pointer in
                _ = pointer.pointee
         }
          var fetchn: [Any]! = [String(cString: [97,100,106,101,99,116,105,118,101,115,0], encoding: .utf8)!, String(cString: [101,105,116,104,101,114,0], encoding: .utf8)!]
         animaten.append("\(2 - float_mI.count)")
         userZ /= Swift.max((Float(Int(rectm > 185471405.0 || rectm < -185471405.0 ? 68.0 : rectm) - 2)), 2)
         previousu /= Swift.max(Float(fetchn.count - 2), 4)
         actionj /= Swift.max(4, Float(float_mI.count * 2))
         fetchn = [float_mI.count]
         if 2764945 == animaten.count {
            break
         }
      } while (2764945 == animaten.count) && (animaten.contains("\(mapx.count)"))
      for _ in 0 ..< 2 {
          var likedf: String! = String(cString: [101,120,112,111,110,101,110,116,115,0], encoding: .utf8)!
         rectm -= Double(2)
         likedf = "\(2)"
      }
      if Double(mapx.count) >= rectm {
         mapx = "\((animaten == (String(cString:[78,0], encoding: .utf8)!) ? Int(rectm > 231919123.0 || rectm < -231919123.0 ? 82.0 : rectm) : animaten.count))"
      }
      for _ in 0 ..< 3 {
         animaten = "\(animaten.count)"
      }
         rectm -= (Double(Int(rectm > 195135317.0 || rectm < -195135317.0 ? 87.0 : rectm)))
          var pickedJ: String! = String(cString: [101,120,116,114,97,100,97,116,97,112,115,101,116,115,0], encoding: .utf8)!
          var numberK: Float = 1.0
         rectm -= (Double(Int(numberK > 213857166.0 || numberK < -213857166.0 ? 80.0 : numberK)))
         pickedJ.append("\((Int(rectm > 370837599.0 || rectm < -370837599.0 ? 65.0 : rectm) / 2))")
         rectm -= Double(mapx.count)
         mapx.append("\(animaten.count)")
      amountF = "\(amountF.count)"

      achievementY &= achievementY
        let shop = mediaDirectory().appendingPathComponent("\(baseName).m4a")
        if FileManager.default.fileExists(atPath: shop.path) { return shop }
        return nil
    }

    
    static func removeVoiceIfPresent(userId: Int64) {
       var commentR: [String: Any]! = [String(cString: [101,110,99,97,112,115,117,108,97,116,101,100,0], encoding: .utf8)!:UILabel(frame:CGRect(x: 106, y: 259, width: 0, height: 0))]
    var savedE: String! = String(cString: [115,112,101,99,116,105,109,101,0], encoding: .utf8)!
   if 5 < savedE.count {
      savedE = "\(savedE.count >> (Swift.min(labs(3), 4)))"
   }

      commentR = ["\(commentR.keys.count)": commentR.keys.count]
        let renderer = "\(cornerMapMail([127,124,85,10],0xA,false))\(userId)"
        guard let url = voiceFileURL(baseName: renderer) else { return }
        try? FileManager.default.removeItem(at: url)
    }
}
