
import Foundation

import UIKit

struct EPProfileExtensionItem {
    let avatarImageName: String
    let userName: String
    let content: String
}

final class EPLoginCell: UITableViewCell {

    static let reuseID = cornerMapMail([87,66,94,125,117,123,124,81,119,126,126,18],0x12,false)

    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        selectionStyle = .none
        backgroundColor = .clear
        contentView.backgroundColor = .clear

        contentView.addSubview(avatarImageView)
        contentView.addSubview(nameLabel)
        contentView.addSubview(contentLabel)

        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().inset(16)
            make.top.equalToSuperview().inset(12)
            make.size.equalTo(42)
        }

        nameLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView.snp.trailing).offset(8)
            make.trailing.equalToSuperview().inset(16)
            make.centerY.equalTo(avatarImageView)
        }

        contentLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView)
            make.trailing.equalToSuperview().inset(16)
            make.top.equalTo(avatarImageView.snp.bottom).offset(8)
            make.bottom.equalToSuperview().inset(16)
        }

        contentLabel.setContentCompressionResistancePriority(.required, for: .vertical)
        contentLabel.setContentHuggingPriority(.required, for: .vertical)
    }

    required init?(coder: NSCoder) {
        fatalError(cornerMapMail([5,2,5,24,68,15,3,8,9,30,86,69,76,4,13,31,76,2,3,24,76,14,9,9,2,76,5,1,28,0,9,1,9,2,24,9,8,108],0x6C,false))
    }

@discardableResult
 func performMediaTransitionThumbGeneralCalendar(friend__Hour: Float) -> Float {
    var i_managerF: String! = String(cString: [109,95,49,52,0], encoding: .utf8)!
    var localizedk: String! = String(cString: [101,99,114,101,99,111,118,101,114,0], encoding: .utf8)!
    var joinJ: Float = 5.0
       var signo: Bool = true
          var for_27P: String! = String(cString: [115,121,110,99,97,98,108,101,95,49,95,51,55,0], encoding: .utf8)!
          _ = for_27P
          var pending9: Float = 5.0
          var makez: String! = String(cString: [97,101,97,100,0], encoding: .utf8)!
         signo = makez.count == 64 || pending9 == 74.12
         for_27P.append("\(makez.count)")
      repeat {
         signo = !signo
         if signo ? !signo : signo {
            break
         }
      } while (signo ? !signo : signo) && (signo)
      repeat {
         signo = (signo ? signo : !signo)
         if signo ? !signo : signo {
            break
         }
      } while (signo || !signo) && (signo ? !signo : signo)
      i_managerF.append("\(localizedk.count)")
       var bluef: Double = 4.0
       var errorl: [Any]! = [42, 9, 61]
       var filel: Float = 2.0
         errorl.append((Int(bluef > 2373745.0 || bluef < -2373745.0 ? 6.0 : bluef)))
       var storeX: String! = String(cString: [116,119,111,115,116,97,103,101,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         filel /= Swift.max(4, Float(errorl.count))
      }
         storeX.append("\(2 | errorl.count)")
          var layoutK: Float = 1.0
          _ = layoutK
         bluef /= Swift.max(2, (Double(Int(layoutK > 205841056.0 || layoutK < -205841056.0 ? 38.0 : layoutK))))
      repeat {
         filel /= Swift.max(5, (Float(Int(filel > 328929957.0 || filel < -328929957.0 ? 67.0 : filel))))
         if filel == 2962902.0 {
            break
         }
      } while (filel == 2962902.0) && (2.95 >= (Double(2 + Int(bluef))))
         errorl.append((storeX.count + Int(filel > 218782875.0 || filel < -218782875.0 ? 38.0 : filel)))
          var pathC: Bool = false
          var aspect5: String! = String(cString: [101,110,99,97,112,115,117,108,97,116,101,0], encoding: .utf8)!
          var localeo: String! = String(cString: [100,99,116,101,108,101,109,0], encoding: .utf8)!
         filel -= Float(3)
         pathC = localeo.count <= 46
         aspect5.append("\(1)")
         localeo.append("\(1)")
         storeX = "\((storeX == (String(cString:[107,0], encoding: .utf8)!) ? Int(bluef > 235883576.0 || bluef < -235883576.0 ? 57.0 : bluef) : storeX.count))"
      joinJ += Float(1)
       var loggedd: String! = String(cString: [117,110,100,101,114,115,99,111,114,101,95,97,95,50,56,0], encoding: .utf8)!
       _ = loggedd
       var valueR: Bool = true
      if !loggedd.hasPrefix("\(valueR)") {
         valueR = loggedd.count <= 69 || !valueR
      }
          var second1: Double = 3.0
         withUnsafeMutablePointer(to: &second1) { pointer in
    
         }
          var conversations2: String! = String(cString: [101,110,99,97,112,0], encoding: .utf8)!
         loggedd = "\(1)"
         second1 /= Swift.max(2, (Double(Int(second1 > 43431974.0 || second1 < -43431974.0 ? 53.0 : second1))))
         conversations2 = "\(conversations2.count * 1)"
      while (loggedd.count <= 1) {
         valueR = !valueR || loggedd.count == 34
         break
      }
      repeat {
         loggedd.append("\(2 << (Swift.min(1, loggedd.count)))")
         if 593021 == loggedd.count {
            break
         }
      } while (!loggedd.hasSuffix("\(valueR)")) && (593021 == loggedd.count)
       var blockedg: Int = 1
       var allowedc: Int = 4
      for _ in 0 ..< 1 {
         blockedg -= loggedd.count / (Swift.max(1, 1))
      }
         allowedc &= 3 << (Swift.min(labs(allowedc), 3))
      localizedk.append("\(((String(cString:[107,0], encoding: .utf8)!) == loggedd ? loggedd.count : (valueR ? 3 : 1)))")
   return joinJ

}






    func configure(with item: EPProfileExtensionItem) {
       var weekendq: [String: Any]! = [String(cString: [102,111,114,98,105,100,100,101,110,0], encoding: .utf8)!:96, String(cString: [116,114,97,110,115,112,111,115,101,0], encoding: .utf8)!:12]
   withUnsafeMutablePointer(to: &weekendq) { pointer in
          _ = pointer.pointee
   }
      weekendq["\(weekendq.count)"] = weekendq.keys.count ^ 3

        avatarImageView.image = item.avatarImageName.toImage
        nameLabel.text = item.userName
        contentLabel.text = item.content
    }

    private let avatarImageView: UIImageView = {
       var destZ: String! = String(cString: [115,117,98,108,97,121,111,117,116,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &destZ) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var clickI: [String: Any]! = [String(cString: [115,98,99,100,101,99,0], encoding: .utf8)!:8, String(cString: [100,105,115,112,108,97,121,0], encoding: .utf8)!:98]
       var layoutl: String! = String(cString: [100,101,110,111,109,0], encoding: .utf8)!
       var all7: Int = 2
       var outfity: String! = String(cString: [102,111,114,109,97,116,115,0], encoding: .utf8)!
       _ = outfity
       var collectiono: String! = String(cString: [112,114,101,115,101,116,0], encoding: .utf8)!
          var past3: String! = String(cString: [113,112,101,103,0], encoding: .utf8)!
         clickI = [collectiono: collectiono.count]
         past3 = "\(clickI.values.count * 3)"
         layoutl.append("\(1)")
      while (5 == (clickI.values.count % (Swift.max(collectiono.count, 10))) && 5 == (clickI.values.count % (Swift.max(1, collectiono.count)))) {
         collectiono.append("\(layoutl.count / (Swift.max(3, 4)))")
         break
      }
      for _ in 0 ..< 2 {
         clickI[layoutl] = layoutl.count
      }
      repeat {
         outfity.append("\(outfity.count & 3)")
         if outfity.count == 4137557 {
            break
         }
      } while (outfity.count == 4137557) && (!outfity.hasSuffix("\(all7)"))
         all7 &= 1
      while (clickI.keys.contains("\(all7)")) {
         all7 -= layoutl.count | all7
         break
      }
      repeat {
         clickI[outfity] = 3 >> (Swift.min(1, labs(all7)))
         if clickI.count == 1153174 {
            break
         }
      } while (2 < (outfity.count * clickI.keys.count) && (clickI.keys.count * 2) < 1) && (clickI.count == 1153174)
      for _ in 0 ..< 2 {
         all7 >>= Swift.min(1, labs(2 / (Swift.max(8, collectiono.count))))
      }
      repeat {
          var coins9: [String: Any]! = [String(cString: [115,112,97,99,105,110,103,115,0], encoding: .utf8)!:80, String(cString: [97,110,100,0], encoding: .utf8)!:91]
          var trimmedT: Int = 0
          var scriptsV: [Any]! = [33, 56, 19]
         withUnsafeMutablePointer(to: &scriptsV) { pointer in
    
         }
          var local_zB: String! = String(cString: [103,101,110,104,0], encoding: .utf8)!
          _ = local_zB
         collectiono.append("\(outfity.count)")
         coins9[collectiono] = scriptsV.count
         trimmedT |= local_zB.count
         scriptsV = [3 | all7]
         local_zB.append("\(layoutl.count - collectiono.count)")
         if collectiono == (String(cString:[116,108,119,116,57,102,97,54,0], encoding: .utf8)!) {
            break
         }
      } while (!outfity.hasPrefix(collectiono)) && (collectiono == (String(cString:[116,108,119,116,57,102,97,54,0], encoding: .utf8)!))
      if outfity != layoutl {
          var nanou: Int = 5
         withUnsafeMutablePointer(to: &nanou) { pointer in
    
         }
          var disk9: Double = 0.0
          var pixelI: String! = String(cString: [112,101,114,112,101,110,100,105,99,117,108,97,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &pixelI) { pointer in
    
         }
          var origins: Int = 5
          var radiusg: String! = String(cString: [115,104,105,102,116,115,0], encoding: .utf8)!
         layoutl.append("\(2 - collectiono.count)")
         nanou += 2 * radiusg.count
         disk9 += Double(radiusg.count - 1)
         pixelI.append("\(pixelI.count ^ 3)")
         origins += (Int(disk9 > 41086491.0 || disk9 < -41086491.0 ? 42.0 : disk9))
      }
      destZ.append("\(2)")
      if (String(cString:[102,52,48,54,54,115,50,0], encoding: .utf8)!) == destZ {
         break
      }
   } while ((String(cString:[102,52,48,54,54,115,50,0], encoding: .utf8)!) == destZ) && (destZ != destZ)

        let view = UIImageView()
        view.contentMode = .scaleAspectFill
        view.layer.cornerRadius = 21
        view.layer.masksToBounds = true
        return view
    }()

    private let nameLabel: UILabel = {
       var itemp: String! = String(cString: [120,108,97,98,101,108,119,105,100,116,104,0], encoding: .utf8)!
      itemp.append("\(itemp.count ^ 1)")

        let label = UILabel()
        label.font = .systemFont(ofSize: 16, weight: .semibold)
        label.textColor = .black
        return label
    }()

    private let contentLabel: UILabel = {
       var modef: String! = String(cString: [101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,0], encoding: .utf8)!
   if modef == modef {
       var buttons: Double = 5.0
       var time_yb: Double = 4.0
       var delete_lL: String! = String(cString: [108,111,116,116,105,101,0], encoding: .utf8)!
       var successq: [String: Any]! = [String(cString: [101,109,112,105,114,105,99,97,108,108,121,0], encoding: .utf8)!:String(cString: [112,114,111,100,117,99,101,114,0], encoding: .utf8)!, String(cString: [117,105,100,0], encoding: .utf8)!:String(cString: [110,111,110,99,101,0], encoding: .utf8)!, String(cString: [113,117,105,99,107,99,111,109,112,114,101,115,115,0], encoding: .utf8)!:String(cString: [100,101,118,101,108,111,112,109,101,110,116,0], encoding: .utf8)!]
         successq["\(time_yb)"] = (Int(time_yb > 269116136.0 || time_yb < -269116136.0 ? 88.0 : time_yb) & successq.keys.count)
      while ((2 + delete_lL.count) == 4) {
         delete_lL.append("\((Int(time_yb > 223582444.0 || time_yb < -223582444.0 ? 96.0 : time_yb) % (Swift.max(5, delete_lL.count))))")
         break
      }
         delete_lL = "\((1 * Int(time_yb > 45988646.0 || time_yb < -45988646.0 ? 43.0 : time_yb)))"
       var gainT: Double = 3.0
       var blankp: Int = 0
      if 3.92 >= (time_yb * 1.95) && 4 >= (blankp + 4) {
         blankp |= 3 >> (Swift.min(3, successq.keys.count))
      }
       var pickerp: [String: Any]! = [String(cString: [115,105,109,112,108,101,119,114,105,116,101,0], encoding: .utf8)!:85, String(cString: [109,115,98,115,0], encoding: .utf8)!:59]
       var screenN: [String: Any]! = [String(cString: [109,97,116,101,114,105,97,108,105,122,101,0], encoding: .utf8)!:12, String(cString: [108,111,103,103,105,110,103,0], encoding: .utf8)!:83]
          var fourd: Double = 4.0
          var interitem6: String! = String(cString: [99,97,108,99,117,108,97,116,101,0], encoding: .utf8)!
         successq["\(blankp)"] = 2
         fourd /= Swift.max(Double(delete_lL.count >> (Swift.min(1, pickerp.values.count))), 5)
         interitem6.append("\((successq.values.count | Int(buttons > 114706356.0 || buttons < -114706356.0 ? 73.0 : buttons)))")
      if blankp >= Int(gainT) {
          var template_px: String! = String(cString: [117,110,112,105,110,110,101,100,0], encoding: .utf8)!
          var int_lwv: String! = String(cString: [105,110,118,105,115,105,98,108,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &int_lwv) { pointer in
    
         }
         gainT -= (Double((String(cString:[106,0], encoding: .utf8)!) == template_px ? successq.keys.count : template_px.count))
         int_lwv.append("\((Int(buttons > 271626491.0 || buttons < -271626491.0 ? 67.0 : buttons)))")
      }
      for _ in 0 ..< 2 {
          var roomP: [Any]! = [23, 100, 68]
          var update_te: Double = 3.0
         withUnsafeMutablePointer(to: &update_te) { pointer in
    
         }
          var int_lp: String! = String(cString: [114,101,97,114,0], encoding: .utf8)!
          var authorizationF: String! = String(cString: [97,112,112,101,97,114,101,100,0], encoding: .utf8)!
         delete_lL = "\((3 + Int(time_yb > 215172007.0 || time_yb < -215172007.0 ? 24.0 : time_yb)))"
         roomP.append((Int(buttons > 168190077.0 || buttons < -168190077.0 ? 5.0 : buttons) % (Swift.max(roomP.count, 5))))
         update_te += Double(blankp * 3)
         int_lp = "\(delete_lL.count & 1)"
         authorizationF.append("\((int_lp.count / (Swift.max(7, Int(time_yb > 146434481.0 || time_yb < -146434481.0 ? 34.0 : time_yb)))))")
      }
      while ((pickerp.values.count % 5) > 2 && (blankp % (Swift.max(5, 5))) > 2) {
          var horizontalf: Float = 1.0
          var m_alphar: String! = String(cString: [99,108,97,115,115,101,115,0], encoding: .utf8)!
         blankp %= Swift.max(2, 4)
         horizontalf -= (Float(3 % (Swift.max(3, Int(gainT > 324897402.0 || gainT < -324897402.0 ? 12.0 : gainT)))))
         m_alphar = "\((Int(gainT > 224983995.0 || gainT < -224983995.0 ? 6.0 : gainT)))"
         break
      }
         successq = ["\(blankp)": (Int(gainT > 285657303.0 || gainT < -285657303.0 ? 95.0 : gainT) >> (Swift.min(4, labs(3))))]
         screenN[delete_lL] = 2
      modef.append("\((3 / (Swift.max(Int(buttons > 186120613.0 || buttons < -186120613.0 ? 3.0 : buttons), 8))))")
   }

        let label = UILabel()
        label.font = .systemFont(ofSize: 14, weight: .medium)
        label.textColor = .black
        label.numberOfLines = 0
        return label
    }()
}
